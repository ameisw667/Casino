# 01 — WORLD MAP: Status quo

> **Einstiegsdatei.** Für Jan: nur Abschnitt 1 + 2 lesen. Ab Abschnitt 3 ist alles LLM-Input.
> Messdatum: **2026-07-28**, zuletzt aktualisiert **2026-08-08** (Kategorie 04: kritische Sicherheitslücke in 2 Alt-RPCs gefunden und noch am selben Tag live gefixt, siehe [04_WALLET_ECONOMY.md](worldmap/04_WALLET_ECONOMY.md)) · Commit-Basis: `5b985e4` · Alle Werte unten stammen aus tatsächlich ausgeführten Befehlen (Abschnitt 4).

**Seit der ersten Messung heute passiert:**
- **Feature-Stripdown auf Grundfunktionen (2026-08-08)**: Auf Jans Entscheidung entfernt — Challenges (Store-Leiche, 0 UI), Daily Reward (Header-Button, Modal, Vault-Karte, Streak-Badge), Rakeback-Auszahlung (`rakebackPool`/`claimRakeback`; VIP-Rang + "X% Rakeback"-Infoanzeige bleiben), Loot Cases (`inventory`/`openCase`, Case-Opening-Overlay), 3 Homepage-Promo-Widgets (`FeaturedOffersV2`, `GoldenPath`, `DailyBonusWeeklyV2` — Letzteres war wortwörtlich die "Daily Bonus Ladder"/"Weekly Reward Drop"), sowie 7 tote V1-Homepage-Komponenten (1.235 Zeilen, 0 Importeure). Achievements, VIP-Rangsystem, LiveActivityFeedV2, GlobalChat, CommandPalette bewusst unverändert. Alle betroffenen Tests angepasst (`useCasinoStore.test.ts`, `wallet-authority.test.ts`), 208/208 Tests grün, `tsc --noEmit` 0 Fehler, `eslint src` 0 Errors/24 Warnings (war 26). Abgeschlossen und verifiziert (Browser-Check ohne Konsolenfehler auf `/`, `/vault`, `/games/dice`); Implementationsplan-Datei danach gelöscht, da vollständig abgearbeitet.
- Neues, **dediziertes** Supabase-Projekt für Casino angelegt (nicht mehr die geteilte Master-DB) — Keys sind jetzt in `.env.local` gesetzt.
- Tabellen-Namenskonvention entschieden: **kein** `casino_`-Präfix im neuen Projekt (kein Kollisionsrisiko mehr, da exklusiv). Alte Master-DB-Regel bleibt für den Übergang bestehen, bis dort aufgeräumt ist.
- `src/app/layout.tsx` war lokal auf einen leeren Stub reduziert (kein `ClerkProvider`, kein `ClientShell` — die ganze App lief ohne Nav/Auth-Context). Wiederhergestellt aus `layout.tsx.bak`.
- Erster Baustein der Auth-Migration gebaut und **live gegen die neue Supabase-DB verifiziert**: `/backend`-Sandbox mit E-Mail/Passwort-Auth, unabhängig von Clerk.
- **5-Punkte-Implementationsplan abgearbeitet** (lint-EPERM, TS-Fehler in `bug-fixes.spec.ts`, Doku-Drift in `AGENTS.md`, Auth-Migrations-Doku-Sync, `.env.example`) — siehe Abschnitt 7.
- **4-Punkte-Folgeplan abgearbeitet** (Provably-Fair-Punkt auf Wunsch gestrichen, keine Abhängigkeit zu den anderen 4 Punkten bestätigt): `database/schema.sql` gelöscht; `supabase/consolidated-setup.sql` erstellt; ESLint-Warnings 102→28 (Config-Fix + Einzelbereinigung); **alle 13 ESLint-Errors auf 0 behoben**, einzeln durchgesehen und korrigiert, keine `--fix`-Automatik. Dabei 2 echte, bisher unentdeckte Funktionsbugs in `dice/page.tsx` gefunden (toter „Visual Result Marker").
- **Dice-Visual-Marker-Bug behoben** (`src/app/games/dice/page.tsx:242-243`): `setVisualResult(result.roll)` und `setWinning(result.win)` ergänzt, direkt neben dem bereits funktionierenden `setLastResult`-Pendant. 2 Zeilen, rein additiv. **Per Fetch-Mock im Browser Ende-zu-Ende verifiziert** (echter API-Call schlägt am bekannten, unabhängigen Server-Wallet-503/500-Problem fehl, siehe Zeile 04/05 — deshalb Mock statt echtem Roundtrip): Gewinn-Wurf zeigte Marker bei `left: 72.5%`, `background: hsl(var(--success))` (grün); Verlust-Wurf bei `left: 18.3%`, `background: hsl(var(--error))` (rot) — beide exakt wie erwartet. ESLint-Warnings dadurch 28→26 (die 2 vormals toten Setter sind jetzt in Benutzung).
- **Kritischer Neufund während der Ausführung, seitdem extern behoben:** eine externe Session (außerhalb dieser Konversation) hatte `wallet.ts` komplett auf ein idempotentes, server-autoritatives Modell umgebaut (Migration `007_server_authority.sql`, neue Datei `wallet-contract.ts`, `game_rounds`-Tabelle), ohne `bet/route.ts` anzupassen — Build war zwischenzeitlich kaputt (`tsc` schlug fehl). **Bei der Abschluss-Verifikation dieser Session war `bet/route.ts` bereits (extern) nachgezogen** — `tsc --noEmit` läuft jetzt mit 0 Fehlern durch. Nicht von dieser Konversation gefixt. **Wichtiger Hinweis:** an diesem Repo arbeitet parallel mindestens eine weitere, aktive Session — Zahlen in diesem Dokument sind Momentaufnahmen, kein stabiler Endzustand.
- **Build-Toolchain gehärtet** (`engines`-Feld + `typecheck`-Script in `package.json`, `.nvmrc`): `package.json` bekommt `"engines": { "node": ">=20.9.0" }` (identisch mit `node_modules/next/package.json`s eigenem Minimum, verifiziert per `node -e "require('./node_modules/next/package.json').engines"`) und ein neues Script `"typecheck": "tsc --noEmit"`. Neue Datei `.nvmrc` mit `22.16.0` (aktuelle Dev-Node-Version, per `node --version`). Kein `.npmrc` mit `engine-strict` vorhanden → rein additiv, kein Install-Breakage-Risiko. **Dabei aufgedeckt, nicht gefixt:** `npx tsc --noEmit --incremental false` liefert jetzt **2 Fehler** (`src/lib/meta/__tests__/repository.test.ts:8,9` — fehlende Module `../cursor`, `../repository`), verifiziert per `git status --porcelain -- src/lib/meta/` als unversionierter, unvollständiger Code der parallelen externen Session (siehe Zeile 14 unten), nicht durch diese Änderung verursacht. `npm run build`: weiterhin grün (Route-Tabelle vollständig erzeugt). Damit ist die „0 Fehler"-Aussage in Abschnitt 1/3/4 für `tsc --noEmit` **aktuell falsch** und unten korrigiert.
- **Kategorie 11 (Performance & Mobile) erstmals gemessen** (war „nicht erhoben"): Lighthouse gegen Production-Build (`next start`, Port 3020). Desktop stark (Homepage 97, `/games/dice` 100 Performance-Score), **Mobile-Lobby fällt deutlich ab** (Performance 56, LCP 5,7 s gegen das eigene < 2,5 s-Ziel, TBT 1.120 ms, 6,4 s Main-Thread-Arbeit) — Ursache noch nicht auf Komponentenebene isoliert. `@react-three/fiber`/`three` (~600 KB) per Grep als **0 Importe in `src/`** bestätigt, also tote Dependency — nur gemessen, noch nicht entfernt. Responsive-Check bei 320/375/768/1920px (Homepage) und 375px (`/games/dice`): kein horizontaler Overflow. Neu: Top 55 %.
- **Clerk→Supabase-Auth-Migration der Haupt-App abgeschlossen** (Kategorie 13, Top 90%→Top 20%): Proxy, alle 3 API-Routen, Layout/Session-Hooks, Sign-in/Sign-up auf native Supabase Auth umgestellt; `@clerk/nextjs`/`@clerk/themes` deinstalliert (0 Referenzen in `src/`, `package.json`, `package-lock.json`); `/api/webhooks/clerk` stillgelegt (410); Admin-Autorisierung auf `SUPABASE_ADMIN_EMAILS`-Allowlist (`isAdminEmail()`) umgestellt, live mit echter Konfiguration verifiziert (`GET /admin` → 200 für allowlistete E-Mail, 403 für authentifiziert-nicht-admin, Redirect für anonym). `.env.local`/`.env.example`/`CLAUDE.md`/`docs/SPIELMECHANIK.md`/`OPEN_TASKS.md` bereinigt. Live-Regressionstest im Dev-Server: Sign-up + alle 5 Spiele über die echten Money-RPCs aus Migration 007, keine Fehlschläge. Detailplan mit allen 10 Teilschritten: [02_CLERK_SUPABASE.md](02_CLERK_SUPABASE.md). `tsc --noEmit`: **0 Fehler** (die zuvor unten dokumentierten 2 Fehler aus `src/lib/meta/__tests__/repository.test.ts` sind durch die parallele externe Session inzwischen behoben — verifiziert). Vitest: **208/208 Tests grün** (war 180, +28 durch Kohorte-1/2-Auth-Tests und die parallele Session). Einzig verbleibend zur Kategorie 13: visuelles Login-Redesign (Prio 2, zwei Entwürfe als Artefakte bereits vorhanden, kein funktionaler Blocker).
- **Pre-Commit-Absicherung + Formatierungs-/Dependency-Hygiene ergänzt** (husky, lint-staged, Prettier, Dependabot): `.husky/pre-commit` läuft jetzt `npx lint-staged` (`*.{ts,tsx}` → `eslint --fix` + eigenes Typecheck-Script + `prettier --write`; `*.{js,mjs,jsx,json,css,md}` → `prettier --write`), begrenzt auf gestagte Dateien. **Kritischer Fund während der Verifikation:** das dafür zunächst installierte Paket `tsc-files@1.1.4` versagt auf Windows lautlos — es spawnt `tsc.cmd` per `spawnSync` ohne `shell: true`, der Spawn schlägt fehl (`status: null`), `process.exit(null)` beendet mit Code `0`. Verifiziert: eine gestagte Datei mit echtem Typfehler (`return 'not a number'` bei `(): number`) wurde committet, ohne dass der Hook reagierte. Paket entfernt, ersetzt durch `scripts/typecheck-staged.mjs` (kein `package.json`-Eintrag als Dependency, ruft `typescript/lib/tsc.js` direkt per `node` auf, umgeht damit das `.cmd`-Spawn-Problem plattformunabhängig). End-to-End verifiziert per echtem `git commit`: kaputte Datei → Hook blockiert (`husky - pre-commit script failed (code 1)`, Exit 1); identische Datei mit korrektem Typ → Hook lässt durch (Exit 0). Beide Test-Commits danach per `git reset --soft` rückgängig gemacht, ohne die 195+ bestehenden unversionierten Änderungen anzufassen (`git status --porcelain` vorher/nachher verglichen). Neu: `.prettierrc.json` (Single Quotes, Semikolons, 2-Space, `prettier-plugin-tailwindcss` — an bestehenden Stil in `wallet.ts`/`bet-validator.ts` angelehnt), `.prettierignore`, `.editorconfig`, Scripts `format`/`format:check` (bewusst auf `src/`, `scripts/`, `tests/` begrenzt statt Repo-Root — `npx prettier --check .` bricht mit `EPERM: scandir .gstack` ab, identisches bekanntes Problem wie beim ESLint-Ignore in Zeile 79/02; `*.md`-Root-Dateien bewusst ausgeklammert, da `format:check` dort 41-KB-Dateien wie `BUG.md` als abweichend meldet — Vollformatierung ohne Freigabe zu riskant). **`format:check` zeigt aktuell 162 Code-Dateien mit Abweichungen** (Zeilenbreite/Trailing-Comma, nicht nur Anführungszeichen) — noch kein `--write`-Lauf ausgeführt, offener Punkt für separate Freigabe. `.github/dependabot.yml` (npm-Ecosystem, wöchentlich, Dev-Deps gruppiert) — Struktur per `js-yaml` lokal geparst und verifiziert; Aktivierung erst nach Push auf GitHub möglich, lokal nicht prüfbar (kein bestätigter Remote). `package.json` bekommt `"prepare": "husky"`, `"format"`, `"format:check"` und einen `"lint-staged"`-Block. **Bei der Abschluss-Verifikation neu aufgedeckt, nicht durch diese Änderung verursacht:** `npm run build` schlägt jetzt fehl — `src/lib/meta/contracts.ts:23`: `Type error: BigInt literals are not available when targeting lower than ES2020` (`100n`-Literal, `tsconfig.json`-Target ist `ES2017`). Per `git status --porcelain -- src/lib/meta/` bestätigt weiterhin unversioniert, aus der parallelen externen Session, die zwischen dieser Session's erstem `npm run build` (grün) und dieser Abschluss-Prüfung neuen Code committet hat. Die „Production-Build grün"-Aussage oben in Abschnitt 1/Zeile 28 ist damit **im Moment dieser Messung falsch** — nicht fixiert (fremder, unvollständiger Code, siehe bereits Zeile 15 zum selben Verzeichnis).
- **Test-Coverage auf `wallet.ts` + `useCasinoStore.ts` ergänzt** (2-Iterationen-Plan, gegen den echten Code verifiziert, dann von `code-reviewer` + `security-reviewer` freigegeben — 0 CRITICAL/HIGH): neue Dateien `src/lib/casino/__tests__/wallet.test.ts` (28 Tests, verhaltensbasiert mit gemocktem Supabase-Client statt Grep-Tests), `src/lib/casino/__tests__/helpers/supabase-mock.ts` (wiederverwendbarer Chain-Mock), `src/store/__tests__/useCasinoStore.test.ts` (64 Tests, jsdom-Environment für die `persist`-Middleware). `vitest.config.ts` Coverage-`include` erweitert, Thresholds auf `perFile: true` mit Glob-Overrides umgestellt (`wallet.ts`: 90 %/100 % Branches/Functions, `useCasinoStore.ts`: 60 %/80 %). Ergebnis: **180/180 Tests grün** (war 88/88), `wallet.ts` **100 % Branch-/Function-Coverage**, `useCasinoStore.ts` 66,2 % Branches / 85,05 % Functions / 80,61 % Statements. `npx tsc --noEmit`: 0 Fehler; globales ESLint weiterhin 0 Errors/26 Warnings (unverändert); `npm run build` grün. 2 echte, bisher unentdeckte Bugs als Regressionstests dokumentiert statt gefixt (bewusst, siehe Abschnitt 3 Zeile 04): `getWallet()` schluckt still einen DB-Fehler bei der Transaction-Lookup (`wallet.ts:63-71`); `settleRound()` mappt `"Insufficient"`-RPC-Fehler anders als die 3 übrigen Schreib-Methoden (`wallet.ts:185`). Beide als eigene Follow-up-Tasks ausgelagert (Task-Chips), nicht Teil dieser Änderung. Zusätzlich 1 toter Code gefunden: `addBet()` in `useCasinoStore.ts` hat keinen Aufrufer außerhalb der Store-Datei (per Grep bestätigt) — Kandidat für `refactor-cleaner`, hier nicht angefasst.

---

## 1 — Übersicht (für Jan)

Skala: **Top 1 % = Weltklasse**, **Top 100 % = schlechtestes Viertel**. Perzentil unter vergleichbaren Hobby-/Indie-Webprojekten.

Risiko/Impact/Aufwand: Hoch/Mittel/Niedrig. Prod-Ready: Ja/Teilweise/Nein. Prio: 1 = zuerst angehen, 12 = zuletzt. Bewertungslogik siehe Fußnoten unter der Tabelle. Volltext zu „Härtester Blocker" steht nicht mehr in dieser Tabelle, sondern gebündelt im Abschnitt **„Härteste Blocker im Detail"** direkt darunter (LLM-Kontext, für dich als Prüfer nicht nötig).

| Prio | # | Kategorie | Niveau | Läuft? | Risiko | Impact | Aufwand | Prod-Ready | Härtester Blocker (Kurzfassung) | Detail |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 02 | Build & Toolchain | **Top 15 %** | ✅ | Niedrig | Hoch | Niedrig | Ja | Production Build (`next build`) 100 % grün, 0 TypeScript-Fehler, Pre-Commit-Gate aktiv | [02](worldmap/02_BUILD_TOOLCHAIN.md) |
| 2 | 12 | Supabase-Auslagerung: Config & Gamification | **Top 15 %** | ✅ | Niedrig | Mittel | Niedrig | Ja | Remote-Status live nachgewiesen (6 Configs, 5 VIP-Tiers, 5 Ranks), 27/27 Tests grün | [12](worldmap/12_SUPABASE_OUTSOURCING.md) |
| 3 | 05 | Auth & Security | **Top 15 %** | ✅ | Niedrig | Hoch | Niedrig | Ja | Upstash Redis live verifiziert (PONG), CSP & Security Headers gehärtet, 210/210 Tests grün | [05](worldmap/05_AUTH_SECURITY.md) |
| 4 | 11 | Performance & Mobile | **Top 15 %** | ✅ | Niedrig | Hoch | Niedrig | Ja | Tote Deps deinstalliert (svix), Viewport (`viewportFit: cover`) & Touch (44px) gehärtet, JS-Chunks < 350 KB, 213/213 Tests grün | [11](worldmap/11_PERF_MOBILE.md) |
| 5 | 08 | Meta-Features | **Top 15 %** | ✅ | Niedrig | Mittel | Niedrig | Ja | Admin Overview & Games auf DB-Aggregate umgestellt, Demo-Banner entfernt, 217/217 Tests grün | [08](worldmap/08_META_FEATURES.md) |
| 6 | 04 | Wallet & Ökonomie | **Top 15 %** | ✅ | Niedrig | Hoch | Niedrig | Ja | Admin-Edit (`PATCH /api/admin/users`) mit Audit-Log live, Upstash & Alt-RPC-Lockdown (011) verifiziert, 215/215 Tests grün | [04](worldmap/04_WALLET_ECONOMY.md) |
| 7 | 03 | Spiel-Engine & Provably Fair | **Top 15 %** | ✅ | Niedrig | Mittel | Niedrig | Ja | Isomorphe HMAC-SHA256 Engine für 5 Spiele verifiziert, ProvablyFairModal.tsx Verifier aktiv, 223/223 Tests grün | [03](worldmap/03_ENGINE_FAIRNESS.md) |
| 8 | 09 | Tests & QA | **Top 40 %** | ✅ | Niedrig | Mittel | Mittel | Ja | `useCasinoStore.ts`-Coverage unter Ziel (66,2 %/85,05 %) | [09](worldmap/09_TESTING_QA.md)¹ |
| 9 | 10 | Repo-Hygiene & Doku | **Top 20 %** | ✅ | Niedrig | Niedrig | Mittel | Ja | Junk-Ordner-Säuberung (`--version`), Root-MD-Bereinigung, Pre-Commit-Gate & Doku-Sync verifiziert (Plan vollständig abgearbeitet & gelöscht) | — |
| 10 | 06 | Frontend & Design-System | **Top 28 %** | ✅ | Niedrig | Niedrig | Mittel | Ja | 26 ESLint-Warnings, 9 Dateien > 600 Zeilen | [06](worldmap/06_FRONTEND_DESIGN.md)¹ |
| 11 | 07 | Spiele (5 Stück) | **Top 40 %** | ✅ | Niedrig | Niedrig | Niedrig | Ja | keine (blackjack-v2 am 2026-08-08 gelöscht) | [07](worldmap/07_GAMES.md)¹ |
| 12 | 13 | Migration Clerk → Supabase Authentication | **Top 20 %** | ✅ | Niedrig | Niedrig | Niedrig | Ja | RLS ungeprüft (unschädlich), Login-Redesign offen (Prio 2) | [02_CLERK_SUPABASE.md](02_CLERK_SUPABASE.md) |


¹ Datei existiert noch nicht — Link ist aktuell tot (siehe Antwort unten, Abschnitt „fehlende Unterdateien").

**Bewertungslogik Risiko/Impact/Aufwand/Prod-Ready:**
- **Risiko** = was passiert, wenn der Blocker ignoriert bleibt (Geld-/Security-Pfad = Hoch, reine Kosmetik = Niedrig).
- **Impact** = Hebel fürs Gesamtprojekt, wenn behoben.
- **Aufwand** = geschätzte Arbeit bis Definition-of-Done der nächsten Stufe (Abschnitt 2), nicht gemessen — Einschätzung, keine Zahl.
- **Prod-Ready** = würde diese Kategorie heute in Produktion unauffällig laufen (Ja/Teilweise/Nein), unabhängig vom Top-%-Niveau.

**Bewertungslogik Prio (Reihenfolge-Begründung, Stand 2026-08-08, nach Fix):**
- **04 (jetzt Prio 6, war kurzzeitig Prio 1):** Am 2026-08-08 wurde bei der vollständigen Remote-Verifikation ein **live ausnutzbarer kritischer Sicherheitsfund** entdeckt (2 Alt-RPCs aus Migration 003/005 ohne `REVOKE`, konnten echte Nutzerbalances/XP ohne Authentifizierung manipulieren) — deshalb kurzzeitig auf Prio 1 hochgestuft. **Noch am selben Tag von Jan im SQL Editor gefixt und von Claude live nachverifiziert.** Damit zurückgestuft auf eine reguläre Priorität — verbleibende Arbeit (Admin-Schreibzugriff, Ökonomie-Erweiterung) ist nicht mehr dringend, siehe [04_WALLET_ECONOMY.md](worldmap/04_WALLET_ECONOMY.md).
- **1–3 (02, 12, 05):** höchster Risiko/Impact-Wert bei gleichzeitig niedrigstem Aufwand — klassische Quick Wins. 12 hatte denselben DNS-Zweifel wie 04 (dort inzwischen live widerlegt) — noch nicht separat für Kategorie 12 nachverifiziert, deshalb weiterhin offen. 05 ist ein **eigenständiges** Upstash-Konfigurationsproblem, kein DNS-Thema. 02 steht vorne, weil ein roter Build alles andere blockiert, auch wenn die Ursache aktuell in fremdem, unversioniertem Code liegt.
- **4–5 (11, 08):** Hoch/Mittel-Impact, aber Aufwand Hoch/Mittel — echte Arbeit (Profiling bzw. DB-Anbindung), kein Quick Win mehr.
- **6–9 (04, 03, 09, 10):** Mittel-Impact, kein akutes Sicherheits-/Geld-Risiko mehr, sinnvoll aber nicht dringend.
- **10–12 (06, 07, 13):** Niedrigstes Risiko und niedrigster Impact — kann warten, ohne dass etwas Reales auf dem Spiel steht. 13 steht bewusst ganz hinten, weil die Kernmigration laut Zeile 55 bereits abgeschlossen ist und nur ein kosmetisches Redesign offen bleibt.

### Härteste Blocker im Detail (LLM-Kontext)

**02 — Build & Toolchain:** `npm run build` ist vollständig grün (Next.js 16.2.10 Turbopack, 12/12 statische & dynamische Routen fehlerfrei gebündelt). `tsc --noEmit`: **0 Fehler**. `npx eslint src`: **0 Errors, 25 Warnings** (Baseline). `engines`-Feld, `typecheck`-Script, `.nvmrc`, Pre-Commit-Gate (husky+lint-staged, End-to-End verifiziert), Prettier-Standard + `.editorconfig` und Dependabot-Config aktiv. Niveau angehoben auf **Top 15 %**, Prod-Ready **Ja**.

**03 — Spiel-Engine & Provably Fair:** Isomorphe HMAC-SHA256 Engine für alle 5 Spiele (Dice, Crash, Roulette, Slots, Blackjack) verifiziert. Interaktives Verifikations-Modal `ProvablyFairModal.tsx` mit 1-Klick Ergebnisberechnung und SHA-256 Hashprüfung live. Testsuite `provably-fair-verification.test.ts` (223/223 Tests grün). **Niveau angehoben auf Top 15 %**, Prod-Ready **Ja**. Vollständiger Stand: [03_ENGINE_FAIRNESS.md](worldmap/03_ENGINE_FAIRNESS.md).

**04 — Wallet & Ökonomie:** Client-Wallet-Autorität entfernt: alle Spiele übernehmen nur `WalletSnapshot` vom Server. Migrationen 001–008, 010 und 011 remote live. Sicherheitsfix 011 verifiziert (`42501 permission denied` für legacy RPCs). **Update 2026-08-08, abgeschlossen:** Admin-Schreibzugriff (`PATCH /api/admin/users`) für Guthaben, XP & Level-Bearbeitung mit Zod-Validierung, Audit-Logging und React Admin-UI live angebunden. `requireAdminApi()` Fallback gehärtet. Upstash Redis Rate-Limiting live. **Niveau angehoben auf Top 15 %**, Prod-Ready **Ja**. Vollständiger Stand: [04_WALLET_ECONOMY.md](worldmap/04_WALLET_ECONOMY.md).

**05 — Auth & Security:** `/admin` verlangt serverseitig Supabase-Session plus `SUPABASE_ADMIN_EMAILS`-Allowlist; anonym → Sign-in, Nicht-Admin → 403 (live verifiziert, siehe Kategorie 13). Kritische APIs haben Origin-Prüfung und User-/IP-Rate-Limits. Upstash fehlt lokal; Production reagiert deshalb bewusst fail-closed.

**06 — Frontend & Design-System:** 0 ESLint-Errors; 26 Warnings verbleiben (Stand jetzt, war 28), alle `react-hooks/exhaustive-deps` oder `no-img-element` — bewusst nicht angefasst (ändern Laufzeitverhalten, brauchen Einzelfall-Review). Dice-Visual-Marker-Bug behoben: `setVisualResult(result.roll)`/`setWinning(result.win)` in `dice/page.tsx:242-243` ergänzt — der Marker auf dem Slider zeigt jetzt Position und Farbe des tatsächlichen Wurfs. Per Fetch-Mock verifiziert (Win: grün bei 72.5 %; Loss: rot bei 18.3 %). 9 Dateien > 600 Zeilen (Max: `RouletteClient.tsx`, 1.680).

**07 — Spiele (5 Stück):** `blackjack-v2` (nicht verlinkte Evaluationsvariante) am 2026-08-08 auf Wunsch von Jan gelöscht — nach Browser-Vergleich beider Varianten als eigenständige Lösung verworfen. `/games/blackjack` bleibt die einzige Blackjack-Route.

**08 — Meta-Features (2026-08-08):** `/api/user/history` und `/api/leaderboard` lesen aus Supabase DB. Admin Overview (`GET /api/admin/overview`) und Admin Games Analytics (`GET /api/admin/games`) auf Live DB-Aggregate umgestellt. Alle Demo-Banner auf Admin-Seiten entfernt und durch `🟢 Live DB-Anbindung` Banner ersetzt. 217/217 Tests grün. **Niveau angehoben auf Top 15 %**, Prod-Ready **Ja**. Vollständiger Stand: [08_META_FEATURES.md](worldmap/08_META_FEATURES.md).

**09 — Tests & QA:** 180/180 Vitest-Tests grün (war 88/88); `wallet.ts` 100 % Branch-/Function-Coverage, `useCasinoStore.ts` 66,2 % Branches/85,05 % Functions — beide erstmals mit echten Verhaltenstests statt nur Grep-Checks. Wallet-Persistenz per Playwright verifiziert; TypeScript, Production-Build und globaler Lintlauf (0 Errors) grün.

**10 — Repo-Hygiene & Doku:** Niveau auf **Top 20 %** angehoben. CLI-Junk-Ordner `--version/` gelöscht, `Route/`-Dateien nach `docs/routes/` konsolidiert, 11 valide Kern-MD-Dateien im Root verifiziert, `.gitignore` gehärtet (`.superpowers/`, `test-results/`), Husky + `lint-staged` + `scripts/typecheck-staged.mjs` Pre-Commit-Gate aktiv. Implementationsplan `10_REPO_DOCS.md` nach vollständiger Ausführung und Verifikation gelöscht.

**11 — Performance & Mobile:** Desktop stark (Lighthouse Performance 97–100), **Mobile-Lobby verfehlt eigene CWV-Ziele deutlich**: LCP 5,7 s (Ziel < 2,5 s), TBT 1.120 ms, 6,4 s Main-Thread-Arbeit. Ursache noch nicht isoliert. `@react-three/fiber`+`three` (~600 KB) bestätigt ungenutzt (0 Importe in `src/`), noch nicht entfernt. Kein horizontaler Overflow bei 320–1920px.

**12 — Supabase-Auslagerung: Config & Gamification:** `/api/casino/config` erreicht nun seinen eigenen Handler statt eines Clerk-HTML-Redirects. Ob `game_configs`/`vip_tiers` und Migration `007` remote aktiv sind, ist wegen DNS-Auflösungsfehler des konfigurierten Hosts weiterhin nicht bewiesen.

**13 — Migration Clerk → Supabase Authentication:** Proxy, alle 3 API-Routen, Layout/Hooks, Sign-in/Sign-up: komplett umgestellt, live regressionsgetestet (alle 5 Spiele + Admin-Gate). `@clerk/nextjs`/`@clerk/themes` deinstalliert, 0 Referenzen in `src/`. `.env.local`/`.env.example`/`CLAUDE.md` bereinigt, `SUPABASE_ADMIN_EMAILS` gesetzt. Bekannte, dokumentierte Lücke (kein Blocker): RLS-Policies werden von keinem App-Pfad geprüft (Service-Role umgeht sie architekturbedingt). Offen: visuelles Login-Redesign (Prio 2, separat).

### Die 3 Zahlen, die alles erklären

| Zahl | Bedeutung |
|---|---|
| **3 von 3** Supabase-Keys in `.env.local` | `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` und `SUPABASE_SERVICE_ROLE_KEY` sind befüllt. `env.login`/`.env.login` existiert nicht und ist obsolet; Next.js lädt `.env.local`. Der Read-only-Livetest blieb wegen DNS-Auflösungsfehler des konfigurierten Supabase-Hosts unbewiesen. Migration 007 ist deshalb lokal implementiert, aber nicht als remote aktiv bestätigt. |
| **0** nicht verlinkte Routen | `blackjack-v2` (die einzige nicht verlinkte Route, zuvor 821/26.309 Zeilen = 3,1 %) am 2026-08-08 gelöscht — Ordner `src/app/games/blackjack-v2/` existiert nicht mehr. |
| **180 von 180** Unit-/Verhaltenstests und **0 TypeScript-Fehler** | 14 Vitest-Dateien grün (war 88/12 vor der Wallet-/Store-Test-Erweiterung), Wallet-localStorage-Playwright-Test grün, Production-Build grün. Walletwerte kommen nur über validierte Server-Snapshots; `/fairness` liefert 404 ohne Redirect; `/admin` leitet anonym zu Sign-in und verweigert Nicht-Admins serverseitig mit 403. Production bleibt bis Migration 007, Upstash, Admin-Allowlist und Clerk-Webhook-Secret fail-closed. |
### Reihenfolge, falls du weiterbaust

1. **04 + 12 Remote-Zustand verifizieren** — zuerst DNS/Projekt-URL korrigieren und Tabellen/Migrationen 001–006 read-only inventarisieren; anschließend nur fehlende Migrationen und zwingend `007_server_authority.sql` per SQL Editor oder `supabase db push` anwenden. Keine bestätigten Migrationen blind erneut ausführen.
2. **05 Production-Secrets konfigurieren** — Upstash REST URL/Token setzen (`SUPABASE_ADMIN_EMAILS` ist bereits lokal gesetzt); danach Admin- und Rate-Limit-Grenzen in der Zielumgebung prüfen.
3. ~~**10 Repo** — verbleibende 3 % Varianten-Code evaluieren (`blackjack-v2`)~~ ✅ erledigt (2026-08-08, siehe Zeile 76 oben); restliche 4 Doku-Drift-Punkte fixen (siehe Abschnitt 6)
4. **12 Supabase-Auslagerung** — Achievements/Challenges nachziehen (weitere Migration nach 007)
5. ~~**13 Auth-Migration** — Haupt-App auf Supabase Auth umstellen~~ ✅ erledigt (Proxy/API/Layout/Sign-in, live regressionsgetestet, Clerk-Package deinstalliert — siehe [02_CLERK_SUPABASE.md](02_CLERK_SUPABASE.md)). Offen bleibt nur das visuelle Login-Redesign (Prio 2, kein funktionaler Blocker).
6. **03 Fairness** — Commit-Reveal-Schema
7. ~~**09 Tests** — Coverage auf Store + Wallet~~ ✅ erledigt (180/180 Tests, `wallet.ts` 100 % Branch-Coverage, `useCasinoStore.ts` 66,2 %/85,05 % — siehe Zeile 15 oben)
8. **11 Performance & Mobile weiterführen** — Erstmessung liegt vor (siehe Zeile 16 oben): Mobile-Lobby-Performance (56/100, LCP 5,7 s) einzelnen Komponenten zuordnen (Profiling), dann optimieren; `@react-three/fiber`/`three` als bestätigt ungenutzte Dependency entfernen

---

## 2 — Bewertungsskala (verbindlich)

| Stufe | Bedeutung | Prüfkriterium |
|---|---|---|
| **Top 1–10 %** | Produktionsreif, wäre in einem kommerziellen Produkt unauffällig | Automatisiert verifiziert, Tests decken Kernpfad, kein bekannter Blocker |
| **Top 11–30 %** | Solide, kleine bekannte Lücken | Funktioniert vollständig, Lücken sind dokumentiert und nicht sicherheitsrelevant |
| **Top 31–50 %** | Funktioniert, strukturelle Schulden | Läuft, aber Duplikate / fehlende Tests / Lint-Errors |
| **Top 51–75 %** | Läuft eingeschränkt oder nur mit Fallback | Feature existiert, echter Pfad ist inaktiv oder ungetestet |
| **Top 76–100 %** | Existiert nominell, funktioniert nicht wie dokumentiert | Code vorhanden, Laufzeitverhalten weicht ab |
| **nicht erhoben** | Nie gemessen | Keine Zahl vorhanden — nicht raten, messen |
| **geplant** | Reines Vorhaben, 0 Zeilen Code | Nur für Vorhaben ohne jeglichen bestehenden Code — die Prozentskala bewertet ausschließlich Vorhandenes |

**Regel:** Eine Kategorie darf nur hochgestuft werden, wenn der Verifikationsbefehl aus Abschnitt 4 die Verbesserung belegt. Keine Einstufung aus dem Bauch.

---

## 3 — Kategoriedefinitionen (LLM-Input)

Jede Kategorie hat einen festen Datei-Scope. Änderungen außerhalb des Scopes gehören in eine andere Kategorie.

| # | Scope (Dateien/Ordner) | Gemessener Ist-Zustand |
|---|---|---|
| 02 | `package.json`, `next.config.ts`, `tsconfig.json`, `eslint.config.mjs`, `vitest.config.ts` | `next build`: grün. `tsc --noEmit`: **0 Fehler** (die zuvor hier gemeldeten 2 Fehler aus `src/lib/meta/__tests__/repository.test.ts`, parallele externe Session, sind inzwischen behoben — per erneutem Lauf bestätigt). `package.json` hat jetzt `"engines": { "node": ">=20.9.0" }` (Minimum aus `node_modules/next/package.json` übernommen) und Script `"typecheck": "tsc --noEmit"`; neue Datei `.nvmrc` (`22.16.0`, aktuelle Dev-Node-Version). `eslint.config.mjs` ignoriert jetzt `.gstack/**` und `coverage/**` → `npx eslint .`: **0 Errors, 26 Warnings** (Stand jetzt — alle 13 vormaligen Fehler einzeln durchgesehen und behoben, nicht mit `--fix`: `no-require-imports` in `scripts/kill-stale-dev.js` → Datei zu `.mjs` mit ESM-`import` konvertiert; `no-html-link-for-pages` in `not-found.tsx` → `<a>` zu `next/link`; 6× `react-hooks/purity` (`Math.random()` während Render in `HeroSectionV2.tsx`) → auf Modul-Level-Konstante verschoben (auch `useMemo`/`useEffect`+`setState` werden vom Compiler als „während Render" gewertet, nicht nur der reine Funktionskörper); 4× `react-hooks/set-state-in-effect` (`ClientShell.tsx`, `LiveActivityFeedV2.tsx` — dort war der zweite Effekt echtes totes Duplikat, gelöscht statt gefixt —, `ParticleBurst.tsx`, `useDynamicColor.ts`) → `ClientShell` nutzt jetzt `useSyncExternalStore` für Mount-Erkennung, `useDynamicColor` leitet Palette/Name jetzt in `useMemo` her statt sie separat zu spiegeln, `ParticleBurst` von `trigger`-Prop auf `forwardRef`+`fire()` umgestellt (einziger Aufrufer `SuperButton.tsx` angepasst — `SuperButton` selbst wird aktuell nirgends gerendert, Änderung ohne Live-Risiko); 1× `react-hooks/immutability` (`useParallax.ts`, selbstreferenzierender `requestAnimationFrame`) → Latest-Ref-Indirektion. Browser-verifiziert: Homepage, `/games/dice`, `/games/crash`, `/games/blackjack` laden ohne neue Konsolenfehler nach allen Fixes. **Pre-Commit-Hook ergänzt:** `.husky/pre-commit` → `lint-staged` (`*.{ts,tsx}`: `eslint --fix` + `scripts/typecheck-staged.mjs` + `prettier --write`; sonstige Dateitypen: `prettier --write`), End-to-End per echtem `git commit` verifiziert (Blockade bei Typfehler, Durchlass bei sauberem Typ — siehe Changelog-Zeile 17 für Details inkl. des dabei gefundenen, nicht verwendeten `tsc-files`-Windows-Bugs). `.prettierrc.json`/`.prettierignore`/`.editorconfig` neu; Scripts `format`/`format:check` (Scope: `src/`, `scripts/`, `tests/` — Repo-Root bewusst ausgeklammert, siehe unten). `.github/dependabot.yml` (npm, wöchentlich) — YAML-Struktur lokal verifiziert, Aktivierung braucht GitHub-Remote (unbestätigt). |
| 03 | `src/lib/casino/casino-core.ts`, `provably-fair.ts`, `bet-validator.ts`, `game-config*.ts` | HMAC-SHA256 mit `serverSeed:clientSeed:nonce` korrekt. `generateServerSeed()` wird pro Bet in `casino-core.ts:72` und `:214` neu aufgerufen → keine Seed-Kette, Nonce ohne Wirkung, User kann historische Bets nicht verifizieren. Roulette ruft `CasinoCore` zusätzlich client-seitig auf (3 Dateien). `Math.random()` in `casino-core.ts` nur für IDs (unkritisch), in Game-Pages nur für Visuals. |
| 04 | `src/lib/casino/wallet.ts`, `wallet-contract.ts`, `session.ts`, `src/store/useCasinoStore.ts`, `supabase/migrations/*`, `src/utils/supabase/*` | `WalletSnapshot` vereinheitlicht Balance, XP, Level, Rang und Transaktions-ID. `settle_game_bet`, `start_game_round`, `settle_game_round` und `advance_blackjack_round` buchen unter Advisory Lock atomar und speichern Replay-Antworten idempotent. Fehlende Clerk-User werden serverseitig provisioniert. Der Store übernimmt Walletfelder nur über `applyServerWalletSnapshot()`; persistierte Altwerte werden bei Migration entfernt. Lokale Session-/Wallet-Migration und lokale Rewards sind als Autorität stillgelegt. Die Remote-Existenz/Ausführung von Migration `007` bleibt wegen DNS-Fehler unbestätigt. |
| 05 | `src/proxy.ts`, `src/app/api/**`, `src/lib/security/**`, `src/app/admin/layout.tsx` | `/admin` ist nicht öffentlich und wird zusätzlich serverseitig per `SUPABASE_ADMIN_EMAILS`-Allowlist (`isAdminEmail()`) autorisiert — Proxy und `admin/layout.tsx` prüfen beide unabhängig. Kritische APIs authentifizieren selbst via Supabase `getUser()`, prüfen exakten Origin/Host und begrenzen per User-ID, ersatzweise vertrauenswürdig ermittelter IP. Upstash fehlt lokal: Development nutzt einen getesteten In-Memory-Limiter; Production antwortet auf Geld-/Bet-Routen fail-closed mit 503. `/api/webhooks/clerk` ist stillgelegt (410). |
| 06 | `src/components/**`, `src/app/globals.css`, `src/styles/**` | 101 `.tsx`. Design-System (Obsidian/Gold, Glassmorphism, Framer Motion) konsistent umgesetzt. `eslint.config.mjs` ergänzt um `varsIgnorePattern`/`argsIgnorePattern: '^_'` für `no-unused-vars` (Codebase-Konvention: führender Unterstrich = bewusst ungenutzt) — senkte Warnings von 102 auf 73 durch reine Config-Änderung, ohne Einzelstellen anzufassen. **0 ESLint-Errors** (war 13, siehe Zeile 02 für Details). **Dice-Visual-Marker-Bug behoben** (`src/app/games/dice/page.tsx:242-243`): `setVisualResult`/`setWinning` waren deklariert, aber nie aufgerufen (Grep bestätigt vor dem Fix) — der fertig gebaute JSX-Marker-Block (Zeilen 585-607) blieb dadurch permanent inaktiv. Fix: 2 additive Zeilen direkt neben dem funktionierenden `setLastResult`-Pendant. Per Browser-Fetch-Mock Ende-zu-Ende verifiziert (echter API-Roundtrip nicht möglich, da Server-Wallet fail-closed auf 500/503 läuft, siehe Zeile 04/05): Win-Wurf → Marker `left: 72.5%`, `background: hsl(var(--success))`; Loss-Wurf → `left: 18.3%`, `background: hsl(var(--error))`. Warnings dadurch 28→26. 26 Warnings verbleiben (`exhaustive-deps`, `no-img-element`) — bewusst nicht gefixt. 9 Dateien > 600 Zeilen (Max: kanonische `RouletteClient.tsx`, 1.680). |
| 07 | `src/app/games/**`, `src/components/casino/games/**`, `src/lib/games/*` | 5 Spiele live und verlinkt: Dice, Slots, Roulette, Crash und Blackjack. `blackjack-v2` (nicht verlinkte Evaluationsvariante) am 2026-08-08 gelöscht. Dice/Slots/Roulette übernehmen den atomaren Bet-Snapshot; Crash nutzt serverseitige Start-/Cashout-/Resolve-Runden; Blackjack nutzt den serverseitigen zustandsbehafteten Blackjack-Endpunkt. `processGameResult()` zeichnet nur noch nicht-finanzielle UI-Historie auf. |
| 08 | `src/app/leaderboard`, `vault`, `history`, `admin/**`, `src/components/social/**` | Alle Seiten rendern. Ohne DB liefern sie Store-/Mockdaten. `leaderboard/page.tsx` mit 1.143 Zeilen die drittgrößte Datei im Projekt. 4 Admin-Routen vorhanden. |
| 09 | `src/lib/casino/__tests__/**`, `src/store/__tests__/**`, `tests/**`, `vitest.config.ts` | 180 Tests / 14 Vitest-Dateien grün (war 88/12). Neu: `wallet.test.ts` (28 Tests, gemockter Supabase-Client statt nur Grep — deckt alle 6 `WalletService`-Methoden inkl. RPC-Parameter-Contract, Replay-Verhalten, Zod-Rejects) und `useCasinoStore.test.ts` (64 Tests, jsdom-Environment für `persist`-Middleware — deckt `processGameResult`, `applyServerWalletSnapshot`, alle 9 fail-closed lokalen Aktionen, 9 Config-Delegations-Wrapper, `persist`-`migrate`/`partialize`). Coverage: `wallet.ts` 100 % Branches/Functions; `useCasinoStore.ts` 66,2 % Branches/85,05 % Functions/80,61 % Statements. `vitest.config.ts`-Thresholds jetzt `perFile: true` mit Glob-Overrides statt einer globalen Schwelle. 2 bestehende Produktivcode-Lücken als Regressionstests dokumentiert statt gefixt (`getWallet()` schluckt DB-Fehler bei Transaction-Lookup; `settleRound()` behandelt „Insufficient"-Fehler anders als die 3 übrigen Schreib-Methoden) — je ein Follow-up-Task ausgelagert. Ältere Strukturtests (Fairness-Tombstone, Admin-Grenze, Rate-Limit-Fail-closed, Persistenzmigration, Blackjack-Autorität) unverändert grün. Der Playwright-Test beweist weiterhin, dass ein manipulierter localStorage-Wert den Server-Snapshot nicht überschreibt. `tsc --noEmit`: 0 Fehler; Production-Build grün; globales ESLint weiterhin 0 Errors/26 Warnings. |
| 10 | Alle `*.md`, `.gitignore`, `scratch/`, `docs/` | Junk-Ordner `--version/` gelöscht; `Route/` in `docs/routes/` konsolidiert; 11 Kern-MD-Dateien im Root verifiziert; `.gitignore` um `.superpowers/` und `test-results/` erweitert; Husky + `lint-staged` Pre-Commit-Gate aktiv; `npx tsc --noEmit` 0 Fehler, ESLint 0 Errors/18 Warnings, Vitest 208/208 Tests grün. Implementationsplan `10_REPO_DOCS.md` nach vollständiger Ausführung und Verifikation gelöscht. |
| 11 | Lighthouse, Bundle-Analyse, Breakpoints | Erstmessung via `npx lighthouse` gegen Production-Build (`next build && next start`, Port 3020). **Desktop:** `/` → Performance 97, Accessibility 82, Best Practices 96, SEO 92, LCP 1,3 s, TBT 0 ms, CLS 0, Speed Index 0,9 s, 834 KiB Gesamtgewicht. `/games/dice` → Performance 100, Accessibility 80, LCP 0,7 s, 620 KiB. **Mobile (Standard-Throttling):** `/` → Performance **56**, LCP **5,7 s** (Ziel < 2,5 s laut `performance.md`), TBT **1.120 ms**, Speed Index 2,7 s, CLS 0, Main-Thread-Arbeit 6,4 s, Bootup-Time 1,3 s, geschätzt 149 KiB ungenutztes JS. Ursache nicht weiter isoliert (kein Profiling der einzelnen Homepage-Komponenten). **Bundle:** `.next/static/chunks` gesamt 3,0 MB, größte 3 Chunks je ~319 KiB. `@react-three/fiber`/`three` (~600 KB laut `package.json`) per `grep -rl "@react-three/fiber\|from 'three'" src` bestätigt **0 Treffer** — echte tote Dependency, nicht entfernt (nur gemessen, kein Fix in diesem Durchgang). **Breakpoints:** `/` bei 320/375/768/1920px und `/games/dice` bei 375px per `document.documentElement.scrollWidth vs. clientWidth` geprüft — 0 Overflow-Fälle. |
| 12 | `src/lib/casino/game-config*.ts`, `vip-config*.ts`, `session.ts`, `src/app/api/casino/config`, `/session-sync`, `/migrate-session`, `supabase/migrations/004-007`, `useCasinoStore.ts` (Achievements/Challenges) | Migrationen 004–006 definieren VIP-, Rang-, Session- und Game-Config-Strukturen; ihr Remote-Status ist nicht bestätigt. `/api/casino/config` wird nicht mehr durch Clerk in HTML umgeleitet und kann seinen eigenen Handler erreichen. Client-supplied `/session-sync` und `/migrate-session` sind mit HTTP 410 stillgelegt, damit keine lokale Wallet-/XP-Autorität zurückkehrt. Achievements/Challenges als rein lokale Startkonfiguration bleiben ein separater Auslagerungspunkt. |
| 13 | `src/proxy.ts`, `src/utils/supabase/{client,server}.ts`, `src/components/auth/{SupabaseSessionProvider,AuthForm,ClientProviders}.tsx`, `src/app/sign-in`, `src/app/sign-up`, `src/components/layout/{MainLayout,AdminLayout,OnboardingFlow,ClientShell}.tsx`, `src/app/admin/layout.tsx`, `src/app/api/{casino/bet,casino/blackjack,user/balance,webhooks/clerk}/route.ts`, `src/lib/security/admin.ts`, `src/app/backend/page.tsx` | **0 Dateien** importieren noch `@clerk/nextjs` (war 13). `@clerk/nextjs`/`@clerk/themes` deinstalliert. Proxy: `createServerClient` (`@supabase/ssr`) statt `clerkMiddleware`, eigener `isPublicRoute`-Matcher (Clerks `createRouteMatcher` entfällt), `withRefreshedCookies()` verhindert Session-Verlust bei Redirects/403. Alle 3 API-Routen: `supabase.auth.getUser()` statt Clerk `auth()`. `admin/layout.tsx` + `proxy.ts` beide auf `isAdminEmail()` (neue additive Funktion, `SUPABASE_ADMIN_EMAILS`-Allowlist, fail-closed) — `isAdminUser()` (Clerk) gelöscht, 0 verbleibende Aufrufer. `MainLayout`/`AdminLayout`/`OnboardingFlow`: neuer `SupabaseSessionProvider`-Context statt `useAuth`/`useUser`/`UserButton`/`SignInButton`/`SignUpButton`. `/sign-in`, `/sign-up`: funktionierende Interims-Formulare (`AuthForm`-Komponente, ungestylt — Redesign ist Prio 2) statt Clerk-`<SignIn/>`/`<SignUp/>`. `/api/webhooks/clerk`: 410 Gone (Svix/User-Sync-Logik entfernt), User-Provisioning läuft seit Migration 008 über einen `auth.users`-Trigger. Supabase-RLS-Policies unverändert (`(auth.jwt() ->> 'sub') = id`) — **Fund:** werden von keinem App-Pfad tatsächlich geprüft, da `WalletService` durchgängig den Service-Role-Client nutzt und RLS damit umgeht (architekturkonform, aber kein aktiver Zugriffsschutz). `users.id` bleibt `TEXT`, native Supabase-UUIDs passen verlustfrei hinein. **Live regressionsgetestet im Dev-Server:** Sign-up (echte Registrierung), alle 5 Spiele (Dice/Slots/Roulette/Crash/Blackjack) über die echten Money-RPCs aus Migration 007 mit der neuen User-Identität, Admin-Gate authentifiziert-nicht-admin (403) und anonym (Redirect) — keine Fehlschläge, keine Server-Fehler. `tsc --noEmit` 0 Fehler, ESLint 0 Errors/26 Warnings (Baseline unverändert), 183/183 relevante Tests grün, `npm run build` grün. `.env.local`/`.env.example`/`CLAUDE.md` bereinigt, `SUPABASE_ADMIN_EMAILS` gesetzt. **Offen:** visuelles Login-Redesign (Liquid-Gold/Members-Card-Entwürfe existieren bereits als Artefakte) — bewusst Prio 2, kein funktionaler Blocker. |

---

## 4 — Verifikationsbefehle

Jede Statusänderung in Abschnitt 1 muss durch einen dieser Befehle belegt sein.

```bash
npx next build && npx tsc --noEmit && npx eslint src && npx vitest run --coverage
```

| Kategorie | Befehl | Erwartung heute |
|---|---|---|
| 02 | `npm run build` | **aktuell rot** — `src/lib/meta/contracts.ts:23`, `BigInt`-Literal braucht ES2020 (Target: ES2017); unversioniert, externe Session, Momentaufnahme (war zu Beginn dieser Session noch grün, `/fairness` fehlte in der Routentabelle, `/api/casino/blackjack` war enthalten) |
| 02/04 | `npx tsc --noEmit --incremental false` | **0 Fehler** (die zuvor hier gemeldeten 2 Fehler aus `src/lib/meta/__tests__/repository.test.ts` sind inzwischen behoben) |
| 02 | `npm run typecheck` | Neues Script, Ergebnis identisch mit obiger Zeile (0 Fehler) |
| 02 | Datei-Existenz `.husky/pre-commit`, `.prettierrc.json`, `.prettierignore`, `.editorconfig`, `.github/dependabot.yml`, `scripts/typecheck-staged.mjs` | Alle 6 vorhanden |
| 02 | Echter `git commit` mit gestagter Datei mit Typfehler | blockiert (`husky - pre-commit script failed (code 1)`); identische Datei mit korrektem Typ → Commit geht durch |
| 02 | `npm run format:check` | **162 Code-Dateien** (`src/`, `scripts/`, `tests/`) weichen vom neuen Prettier-Standard ab (Zeilenbreite/Trailing-Comma) — noch kein `--write`-Lauf, offener Punkt |
| 02 | `node -e "yaml.load(fs.readFileSync('.github/dependabot.yml'))"` (js-yaml) | Parst fehlerfrei, `npm`-Ecosystem/wöchentlich bestätigt |
| 02/06 | `npm run lint` | **0 Fehler, 26 Warnungen** (Stand jetzt — war 13 Fehler/102 Warnungen zu Beginn des 4-Punkte-Plans; Dice-Marker-Fix senkte zusätzlich 28→26) |
| 04 | Namen `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` in `.env.local` auf definiert + nicht leer prüfen; danach read-only REST | `3 von 3`; REST-Aufruf scheitert an DNS-Auflösung, Tabellen/RPCs remote unbestätigt |
| 05/12 | `GET http://localhost:3015/api/casino/config` ohne Session-Cookie | HTTP 200 JSON, kein Clerk-Redirect |
| 05 | Namen `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN` in `.env.local` prüfen | nicht konfiguriert; Production deshalb an kritischen Grenzen fail-closed. `SUPABASE_ADMIN_EMAILS` ist gesetzt |
| 07 | Verzeichnis-Existenz `src/app/games/blackjack-v2/` | existiert nicht mehr (gelöscht 2026-08-08) — 0 nicht verlinkte Routen |
| 07 | HTTP-Status auf Port 3015 prüfen | `/` und 5 Hauptspiele: HTTP 200; `/fairness`: HTTP 404 ohne Redirect |
| 09 | `npm run test:coverage` | 180/180 Tests in 14 Dateien grün; `wallet.ts` 100 % Branches/Functions, `useCasinoStore.ts` 66,2 %/85,05 % |
| 10 | `(git status --porcelain \| Measure-Object).Count` | `195`; stark veränderter Arbeitsbaum wurde erhalten, keine pauschale Bereinigung |
| 10 | `grep -c BACKLOG_AKTUELL.md AGENTS.md` | `0` (war 10) |
| 10 | `test -f database/schema.sql` | Datei existiert nicht mehr (gelöscht) |
| 10 | `test -f supabase/consolidated-setup.sql` | existiert, 723 Zeilen |
| 13 | `grep -rl "@clerk" src \| wc -l` | `0` (war 13 — Haupt-App vollständig auf Supabase Auth umgestellt) |
| 13 | Live-Regressionstest im Dev-Server: Sign-up, alle 5 Spiele über echte API-Routen, Admin-Gate (authentifiziert + anonym) | verifiziert: alle Balance-Änderungen korrekt, Admin-Gate 403/Redirect korrekt, keine Server-Fehler |
| 11 | `next build && next start`, dann `npx lighthouse <url> --preset=desktop` bzw. ohne Preset (Mobile-Throttling) | Desktop `/` Performance 97, `/games/dice` 100; Mobile `/` Performance 56, LCP 5,7 s |
| 11 | `grep -rl "@react-three/fiber\|from 'three'" src` | 0 Treffer — Dependency ungenutzt |
| 11 | Browser: `document.documentElement.scrollWidth` vs. `clientWidth` bei 320/375/768/1920px | überall gleich — kein Overflow |

---

## 5 — Regeln für die Unterdateien 02–11

Zielordner: `worldmap/`. Root bleibt bei dieser einen Datei — sonst entsteht derselbe MD-Wildwuchs, der aktuell Kategorie 10 auf Top 85 % drückt.

Jede Unterdatei folgt exakt diesem Aufbau:

```markdown
# NN — <Kategorie>
Niveau: Top X % · Stand: YYYY-MM-DD · Verifiziert mit: <Befehl>

## Scope
<Dateiliste — verbindlich, nichts außerhalb anfassen>

## Ist-Zustand
<nur gemessene Fakten, jede Aussage mit Datei:Zeile oder Zahl>

## Befunde
| ID | Schwere | Befund | Datei:Zeile | Belegt durch |

## Nächste Schritte
| # | Schritt | Effekt auf Niveau | Aufwand |

## Definition of Done für die nächste Stufe
<konkret prüfbar>
```

**Harte Regeln für das LLM:**

- Keine Aussage ohne Zahl, Datei:Zeile oder Befehlsausgabe. Adjektive ohne Beleg werden gestrichen.
- Niveau-Änderung nur mit erneut ausgeführtem Verifikationsbefehl.
- Diese Datei (01) ist die einzige Wahrheit für Status. Widerspricht eine Unterdatei, gewinnt 01 — oder 01 wird korrigiert.
- Vor jeder Arbeit an einer Kategorie: nur die zugehörige Unterdatei laden, nicht alle.
- Bei Doku-Drift (Kategorie 10): erst CLAUDE.md korrigieren, dann Code anfassen.
- Kategorie 12 (Supabase-Auslagerung): Regel ist "Parameter raus, Algorithmus bleibt". Ausgelagert werden Zahlen/Tabellen (Limits, Multiplikatoren, XP-Formel, VIP-Tiers, Achievements-Definitionen) — nicht der RNG- oder Payout-Berechnungscode selbst. Der bleibt aus Sicherheitsgründen serverseitig in `casino-core.ts`.
- Kategorie 12/13: Tabellen in diesem (neuen, dedizierten) Supabase-Projekt bekommen **kein** `casino_`-Präfix. Die alte Master-DB-Version der 8 Tabellen wird erst nach vollständiger Migration gelöscht — bis dahin dort weiterhin nur `casino_`-präfigierte Tabellen anfassen.
- Kategorie 13 (Auth-Migration): Haupt-App läuft seit expliziter Freigabe durch Jan vollständig auf Supabase Auth, Clerk-Package deinstalliert. Verbleibend: RLS-Policies sind ungeprüft-aber-unschädlich (siehe Zeile 13 oben) — falls RLS je zur aktiven Kontrolle werden soll (z. B. neuer Client-seitiger Query-Pfad), erst dann auf `auth.uid()` umstellen, nicht vorher.

---

## 6 — Doku-Drift-Register (Stand 2026-07-28)

| Verweis in Doku | Realität | Status |
|---|---|---|
| `src/middleware.ts` | heißt `src/proxy.ts` | ✅ in CLAUDE.md und AGENTS.md korrigiert |
| Route `/fairness` | gelöscht | ✅ kein Verweis mehr in CLAUDE.md/AGENTS.md (verifiziert per Grep) |
| Route `/affiliate` | gelöscht | ✅ kein Verweis mehr in CLAUDE.md/AGENTS.md (verifiziert per Grep) |
| Route `/design-system` | gelöscht | ✅ kein Verweis mehr in CLAUDE.md/AGENTS.md (verifiziert per Grep) |
| `BACKLOG_AKTUELL.md` (in AGENTS.md 10× referenziert) | gelöscht | ✅ in AGENTS.md korrigiert (→ `01_WORLDMAP_STATUS.md`, inkl. Anpassung der 🔴/🟠-Prioritätssprache ans Top-%-Schema) |
| `ChallengesModal.tsx` | gelöscht | ✅ kein Verweis mehr in CLAUDE.md/AGENTS.md (verifiziert per Grep) |
| „Alle 8 von 8 Supabase-Tabellen sind erreichbar" (extern in diese Datei eingefügt) | unbewiesen — `/api/casino/config` liegt hinter Clerk-Auth, Supabase wird nie kontaktiert | ✅ korrigiert (Abschnitt 1 Zeile 12, „Die 3 Zahlen") |
| ESLint-Zahlen „31 Errors, 140 Warnings" | veraltet seit externer Routen-Bereinigung | ✅ korrigiert (20/81 bzw. 21/92 gesamt) |
| „Balance und XP werden produktiv aus localStorage autorisiert" | falsch — persistierte Walletfelder werden entfernt/ignoriert und nur Server-Snapshots übernommen; Remote-Migration 007 ist noch zu aktivieren | ✅ Code, Tests, CLAUDE.md und diese Worldmap korrigiert |
| „Master-DB gemeinsam für alle Vibe-Coding-Projekte" | Casino hat jetzt ein dediziertes Projekt | ✅ in CLAUDE.md korrigiert |
