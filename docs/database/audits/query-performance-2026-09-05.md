# Query-Performance-Audit (2026-09-05)

- **Zeitstempel:** 2026-09-05T17:59:22.979Z
- **CLI:** 2.116.0
- **Modus:** `supabase inspect db --linked --output-format json` (read-only)
- **Bewertung:** ⚠️ **Befund** — siehe Bewertung unten

Quelle der Methodik & Schwellenwerte: [`docs/database/07_indexing_query_performance.md`](../07_indexing_query_performance.md) (§ Schwellenwerte).

Hinweis: Die CLI-Subcommands `seq-scans` und `unused-indexes` sind seit CLI 2.116.0 deprecated und in `index-stats` aufgegangen — beide Sichten werden aus dessen JSON abgeleitet.

---

## Bewertung gegen die Schwellenwerte

⚠️ Durchschnitt **28763.4 ms** pro Aufruf über 8 Aufrufe (> 50 ms): `SELECT pg_sleep($1)`
⚠️ Durchschnitt **497.7 ms** pro Aufruf über 173 Aufrufe (> 50 ms): `SELECT
  e.name,
  n.nspname AS schema,
  e.default_version,
  x.extversion AS installed_version,
  e.comment,
  ev.schema AS default_version_schema
FROM
  pg_a`
⚠️ Durchschnitt **246.5 ms** pro Aufruf über 151 Aufrufe (> 50 ms): `with f as (
      
-- CTE with sane arg_modes, arg_names, and arg_types.
-- All three are always of the same length.
-- All three include all args, including OU`
⚠️ Durchschnitt **296.9 ms** pro Aufruf über 13 Aufrufe (> 50 ms): `WITH dep AS (
      SELECT d.classid, d.objid, d.objsubid,
             d.refclassid, d.refobjid, d.refobjsubid
      FROM pg_depend d
      WHERE d.deptype IN `
⚠️ Durchschnitt **426.6 ms** pro Aufruf über 7 Aufrufe (> 50 ms): `with page as (
      select
        c.oid,
        c.relname,
        c.relrowsecurity,
        c.relforcerowsecurity,
        c.relreplident,
        nc.nspnam`
⚠️ Durchschnitt **520.9 ms** pro Aufruf über 4 Aufrufe (> 50 ms): `with tables as (
SELECT
  c.oid :: int8 AS id,
  nc.nspname AS schema,
  c.relname AS name,
  c.relrowsecurity AS rls_enabled,
  c.relforcerowsecurity AS rls_fo`

Unbenutzte Indizes (informativ, kein Handlungsdruck — erst nach ≥ 90 Tagen Beobachtungswindow entfernen):

- public.idx_wallet_transactions_actor (`public.wallet_transactions`, 88 kB)
- public.bet_network_fingerprints_pkey (`public.bet_network_fingerprints`, 40 kB)
- public.idx_user_identities_user_id (`public.user_identities`, 16 kB)
- public.idx_vip_tiers_sort (`public.vip_tiers`, 16 kB)
- public.promo_code_redemptions_pkey (`public.promo_code_redemptions`, 16 kB)
- public.idx_guide_documents_topic (`public.guide_documents`, 16 kB)
- public.background_job_runs_pkey (`public.background_job_runs`, 16 kB)
- public.ranks_pkey (`public.ranks`, 16 kB)
- public.idx_guide_documents_active (`public.guide_documents`, 16 kB)
- public.game_configs_pkey (`public.game_configs`, 16 kB)
- public.daily_race_winners_pkey (`public.daily_race_winners`, 16 kB)
- public.guide_feedback_rating_created_at_idx (`public.guide_feedback`, 16 kB)
- public.idx_ranks_sort (`public.ranks`, 16 kB)
- public.guide_telemetry_events_pkey (`public.guide_telemetry_events`, 16 kB)
- public.idx_promo_codes_active (`public.promo_codes`, 16 kB)
- public.user_identities_pkey (`public.user_identities`, 16 kB)
- public.idx_user_login_history_timeline (`public.user_login_history`, 16 kB)
- public.idx_crash_rounds_status (`public.crash_rounds`, 16 kB)
- public.guide_documents_slug_key (`public.guide_documents`, 16 kB)
- public.vip_tiers_pkey (`public.vip_tiers`, 16 kB)
- public.user_login_history_pkey (`public.user_login_history`, 16 kB)
- public.risk_events_pkey (`public.risk_events`, 16 kB)
- public.user_notifications_pkey (`public.user_notifications`, 16 kB)
- public.idx_game_configs_category (`public.game_configs`, 16 kB)
- public.guide_feedback_pkey (`public.guide_feedback`, 16 kB)
- public.idx_sessions_game (`public.game_sessions`, 8192 bytes)
- public.idx_anonymous_sessions_last_active (`public.anonymous_sessions`, 8192 bytes)
- public.game_sessions_pkey (`public.game_sessions`, 8192 bytes)
- public.wallet_invariant_events_user_id_fingerprint_key (`public.wallet_invariant_events`, 8192 bytes)
- public.idx_admin_roles_role_user (`public.admin_roles`, 8192 bytes)

## Rohdaten: calls

```json
{"rows":[{"query":"select set_config('search_path', $1, true), set_config('role', $2, true), set_config('request.jwt.claims', $3, true), set_config('request.method', $4, true), set_config('request.path', $5, true), set_config('request.headers', $6, true), set_config('request.cookies', $7, true)","total_exec_time":"00:00:08.540209","prop_exec_time":"0.8%","ncalls":"128,641","sync_io_time":"00:00:00"},{"query":"SELECT sessions.aal, sessions.created_at, sessions.factor_id, sessions.id, sessions.ip, sessions.not_after, sessions.oauth_client_id, sessions.refresh_token_counter, sessions.refresh_token_hmac_key, sessions.refreshed_at, sessions.scopes, sessions.tag, sessions.updated_at, sessions.user_agent, sessions.user_id FROM sessions AS sessions WHERE id = $1 LIMIT $2","total_exec_time":"00:00:03.72098","prop_exec_time":"0.4%","ncalls":"78,579","sync_io_time":"00:00:00"},{"query":"SELECT mfa_amr_claims.authentication_method, mfa_amr_claims.created_at, mfa_amr_claims.id, mfa_amr_claims.session_id, mfa_amr_claims.updated_at FROM mfa_amr_claims AS mfa_amr_claims WHERE session_id = $1","total_exec_time":"00:00:01.8678","prop_exec_time":"0.2%","ncalls":"78,541","sync_io_time":"00:00:00"},{"query":"SELECT mfa_factors.created_at, mfa_factors.factor_type, mfa_factors.friendly_name, mfa_factors.id, mfa_factors.last_challenged_at, mfa_factors.last_webauthn_challenge_data, mfa_factors.phone, mfa_factors.secret, mfa_factors.status, mfa_factors.updated_at, mfa_factors.user_id, mfa_factors.web_authn_aaguid, mfa_factors.web_authn_credential FROM mfa_factors AS mfa_factors WHERE user_id = $1","total_exec_time":"00:00:03.917556","prop_exec_time":"0.4%","ncalls":"78,029","sync_io_time":"00:00:00"},{"query":"SELECT identities.created_at, identities.email, identities.id, identities.identity_data, identities.last_sign_in_at, identities.provider, identities.provider_id, identities.updated_at, identities.user_id FROM identities AS identities WHERE user_id = $1","total_exec_time":"00:00:03.173702","prop_exec_time":"0.3%","ncalls":"78,029","sync_io_time":"00:00:00"},{"query":"SELECT users.aud, users.banned_until, users.confirmation_sent_at, users.confirmation_token, users.confirmed_at, users.created_at, users.deleted_at, users.email, users.email_change, users.email_change_confirm_status, users.email_change_sent_at, users.email_change_token_current, users.email_change_token_new, users.email_confirmed_at, users.encrypted_password, users.id, users.instance_id, users.invited_at, users.is_anonymous, users.is_sso_user, users.last_sign_in_at, users.phone, users.phone_change, users.phone_change_sent_at, users.phone_change_token, users.phone_confirmed_at, users.raw_app_meta_data, users.raw_user_meta_data, users.reauthentication_sent_at, users.reauthentication_token, users.recovery_sent_at, users.recovery_token, users.role, users.updated_at FROM users AS users WHERE instance_id = $1 and id = $2 LIMIT $3","total_exec_time":"00:00:08.805776","prop_exec_time":"0.8%","ncalls":"78,004","sync_io_time":"00:00:00"},{"query":"SET client_min_messages TO WARNING","total_exec_time":"00:00:00.075678","prop_exec_time":"0.0%","ncalls":"30,835","sync_io_time":"00:00:00"},{"query":"SET client_encoding = 'UTF8'","total_exec_time":"00:00:00.36354","prop_exec_time":"0.0%","ncalls":"30,835","sync_io_time":"00:00:00"},{"query":"COMMIT","total_exec_time":"00:00:00.045605","prop_exec_time":"0.0%","ncalls":"30,416","sync_io_time":"00:00:00"},{"query":"BEGIN ISOLATION LEVEL READ COMMITTED READ ONLY","total_exec_time":"00:00:00.342821","prop_exec_time":"0.0%","ncalls":"24,448","sync_io_time":"00:00:00"}],"message":"inspect db calls"}
```


## Rohdaten: outliers

```json
{"rows":[{"total_exec_time":"00:03:50.107165","prop_exec_time":"42.9%","ncalls":"8","sync_io_time":"00:00:00","query":"SELECT pg_sleep($1)"},{"total_exec_time":"00:01:26.104819","prop_exec_time":"16.1%","ncalls":"173","sync_io_time":"00:00:00","query":"SELECT\n  e.name,\n  n.nspname AS schema,\n  e.default_version,\n  x.extversion AS installed_version,\n  e.comment,\n  ev.schema AS default_version_schema\nFROM\n  pg_available_extensions e\n  LEFT JOIN pg_extension x ON e.name = x.extname\n  LEFT JOIN pg_namespace n ON x.extnamespace = n.oid\n  LEFT JOIN pg_available_extension_versions ev\n    ON ev.name = e.name AND ev.version = e.default_version"},{"total_exec_time":"00:01:23.636617","prop_exec_time":"15.6%","ncalls":"9,645","sync_io_time":"00:00:00","query":"SELECT public.retry_stale_big_win_events()"},{"total_exec_time":"00:01:06.137397","prop_exec_time":"12.3%","ncalls":"10,935","sync_io_time":"00:00:00","query":"SELECT public.retry_stale_wallet_events()"},{"total_exec_time":"00:00:37.218984","prop_exec_time":"6.9%","ncalls":"151","sync_io_time":"00:00:00","query":"with f as (\n      \n-- CTE with sane arg_modes, arg_names, and arg_types.\n-- All three are always of the same length.\n-- All three include all args, including OUT and TABLE args.\nwith functions as (\n  select\n    *,\n    -- proargmodes is null when all arg modes are IN\n    coalesce(\n      p.proargmodes,\n      array_fill($1::text, array[cardinality(coalesce(p.proallargtypes, p.proargtypes))])\n    ) as arg_modes,\n    -- proargnames is null when all args are unnamed\n    coalesce(\n      p.proargnames,\n      array_fill($2::text, array[cardinality(coalesce(p.proallargtypes, p.proargtypes))])\n    ) as arg_names,\n    -- proallargtypes is null when all arg modes are IN\n    coalesce(p.proallargtypes, p.proargtypes) as arg_types,\n    array_cat(\n      array_fill($3, array[pronargs - pronargdefaults]),\n      array_fill($4, array[pronargdefaults])) as arg_has_defaults\n  from\n    pg_proc as p\n  where\n    p.prokind in ($5, $6)\n)\nselect\n  f.oid as id,\n  n.nspname as schema,\n  f.proname as name,\n  l.lanname as language,\n  case f.prokind\n    when $7 then $8\n    when $9 then $10\n    when $11 then $12\n    when $13 then $14\n    else $15\n  end as type,\n  case\n    when l.lanname = $16 then $17\n    else f.prosrc\n  end as definition,\n  case\n    when l.lanname = $18 then f.prosrc\n    else pg_get_functiondef(f.oid)\n  end as complete_statement,\n  coalesce(f_args.args, $19) as args,\n  pg_get_function_arguments(f.oid) as argument_types,\n  pg_get_function_identity_arguments(f.oid) as identity_argument_types,\n  f.prorettype as return_type_id,\n  pg_get_function_result(f.oid) as return_type,\n  nullif(rt.typrelid, $20) as return_type_relation_id,\n  f.proretset as is_set_returning_function,\n  case\n    when f.provolatile = $21 then $22\n    when f.provolatile = $23 then $24\n    when f.provolatile = $25 then $26\n  end as behavior,\n  f.prosecdef as security_definer,\n  f_config.config_params as config_params\nfrom\n  functions f\n  left join pg_namespace n on f.pronamespace = n.oid\n  left join pg_language l on f.prolang = l.oid\n  left join pg_type rt on rt.oid = f.prorettype\n  left join (\n    select\n      oid,\n      jsonb_object_agg(param, value) filter (where param is not null) as config_params\n    from\n      (\n        select\n          oid,\n          (string_to_array(unnest(proconfig), $27))[$28] as param,\n          (string_to_array(unnest(proconfig), $29))[$30] as value\n        from\n          functions\n      ) as t\n    group by\n      oid\n  ) f_config on f_config.oid = f.oid\n  left join (\n    select\n      oid,\n      jsonb_agg(jsonb_build_object(\n        $31, t2.mode,\n        $32, name,\n        $33, type_id,\n        -- Cast null into false boolean\n        $34, COALESCE(has_default, $35)\n      )) as args\n    from\n      (\n        select\n          oid,\n          unnest(arg_modes) as mode,\n          unnest(arg_names) as name,\n          -- Coming from: coalesce(p.proallargtypes, p.proargtypes) postgres won't automatically assume\n          -- integer, we need to cast it to be properly parsed\n          unnest(arg_types)::int8 as type_id,\n          unnest(arg_has_defaults) as has_default\n        from\n          functions\n      ) as t1,\n      lateral (\n        select\n          case\n            when t1.mode = $36 then $37\n            when t1.mode = $38 then $39\n            when t1.mode = $40 then $41\n            when t1.mode = $42 then $43\n            else $44\n          end as mode\n      ) as t2\n    group by\n      t1.oid\n  ) f_args on f_args.oid = f.oid\n\n    )\n    select\n      f.*\n    from f\n   where schema NOT IN ($45,$46,$47)\n\n-- source: dashboard\n-- user: session:c96811ca-6454-4391-b085-973d30578cc3\n-- date: 2026-08-05T20:23:09.232Z"},{"total_exec_time":"00:00:03.86004","prop_exec_time":"0.7%","ncalls":"13","sync_io_time":"00:00:00","query":"WITH dep AS (\n      SELECT d.classid, d.objid, d.objsubid,\n             d.refclassid, d.refobjid, d.refobjsubid\n      FROM pg_depend d\n      WHERE d.deptype IN ($1, $2)\n        -- sequence OWNED BY is carried as payload + ALTER SEQUENCE … OWNED BY\n        -- (pg_dump's model); the auto edge would cycle with the column default\n        AND NOT (d.deptype = $3\n                 AND d.classid = $4::regclass\n                 AND d.refclassid = $5::regclass\n                 AND d.refobjsubid > $6\n                 AND EXISTS (SELECT $7 FROM pg_class sc\n                             WHERE sc.oid = d.objid AND sc.relkind = $8))\n    ),\n    endpoints AS (\n      SELECT classid, objid, objsubid FROM dep\n      UNION\n      SELECT refclassid, refobjid, refobjsubid FROM dep\n    ),\n    -- one derived table per resolver branch, each built once over its catalog\n    rel AS (\n      SELECT rc.oid, rc.relkind, json_build_object($9,\n               CASE rc.relkind\n                 WHEN $10 THEN $11 WHEN $12 THEN $13\n                 WHEN $14 THEN $15\n                 WHEN $16 THEN $17 WHEN $18 THEN $19\n                 WHEN $20 THEN $21 WHEN $22 THEN $23\n                 WHEN $24 THEN $25 END,\n               $26, rn.nspname, $27, rc.relname) AS id\n      FROM pg_class rc JOIN pg_namespace rn ON rn.oid = rc.relnamespace\n      WHERE rc.relkind IN ($28,$29,$30,$31,$32,$33,$34,$35)\n    ),\n    cbi AS (\n      -- a constraint-backed index resolves to its constraint, not the index\n      SELECT con.conindid AS oid,\n             json_build_object($36,$37,$38,cn.nspname,\n                               $39,cc.relname,$40,con.conname) AS id\n      FROM pg_constraint con\n      JOIN pg_class cc ON cc.oid = con.conrelid\n      JOIN pg_namespace cn ON cn.oid = cc.relnamespace\n      WHERE con.contype IN ($41,$42,$43) AND con.conindid <> $44\n    ),\n    col AS (\n      SELECT att.attrelid AS oid, att.attnum AS subid,\n             json_build_object($45,$46,$47,rn.nspname,\n                               $48,rc.relname,$49,att.attname) AS id\n      FROM pg_attribute att\n      JOIN pg_class rc ON rc.oid = att.attrelid AND rc.relkind IN ($50,$51,$52)\n      JOIN pg_namespace rn ON rn.oid = rc.relnamespace\n      WHERE att.attnum > $53 AND NOT att.attisdropped\n    ),\n    proc AS (\n      -- prokind 'w' (window functions) is extracted as a function fact\n      -- (src/extract/routines.ts: only 'p' -> procedure, everything else ->\n      -- function), so it MUST be resolvable here -- otherwise a pg_depend edge\n      -- into a user window function resolves to NULL and is silently dropped,\n      -- and a view/rule that uses it is never rebuilt against it (issue #333).\n      -- The kind CASE already maps 'w' to 'function' via ELSE.\n      SELECT pp.oid, json_build_object(\n               $54, CASE pp.prokind WHEN $55 THEN $56 WHEN $57 THEN $58 ELSE $59 END,\n               $60, pn.nspname, $61, pp.proname,\n               $62, ARRAY(SELECT format_type(t.t, $63)\n                             FROM unnest(pp.proargtypes) WITH ORDINALITY AS t(t, ord)\n                             ORDER BY t.ord)::text[]) AS id\n      FROM pg_proc pp JOIN pg_namespace pn ON pn.oid = pp.pronamespace\n      WHERE pp.prokind IN ($64,$65,$66,$67)\n    ),\n    tcon AS (\n      SELECT con.oid, json_build_object($68,$69,$70,cn.nspname,\n                               $71,cc.relname,$72,con.conname) AS id\n      FROM pg_constraint con\n      JOIN pg_class cc ON cc.oid = con.conrelid\n      JOIN pg_namespace cn ON cn.oid = cc.relnamespace\n      WHERE con.conrelid <> $73\n    ),\n    dcon AS (\n      SELECT con.oid, json_build_object($74,$75,$76,dn.nspname,\n                               $77,dt.typname,$78,con.conname) AS id\n      FROM pg_constraint con\n      JOIN pg_type dt ON dt.oid = con.contypid\n      JOIN pg_namespace dn ON dn.oid = dt.typnamespace\n      WHERE con.contypid <> $79\n    ),\n    typ AS (\n      -- Resolve array types (typcategory 'A') to their ELEMENT type: a column or\n      -- argument of an array type records its pg_depend edge against the implicit\n      -- array type (e.g. _user_defined_filter), but the managed fact is the\n      -- element type. Mapping the array oid to the element's stable id keeps the\n      -- depends-on-element ordering correct for array-of-composite/domain/enum\n      -- columns and arguments (regression: realtime.subscription has a\n      -- user_defined_filter array column; without this the table/function was\n      -- ordered before the type). An array of a base type (e.g. int4) resolves to\n      -- a pg_catalog element that the typtype filter drops below -- harmless, as\n      -- builtins are unmanaged.\n      SELECT tt.oid,\n        CASE\n          WHEN base.typrelid <> $80::oid AND rc.oid IS NOT NULL THEN\n            json_build_object(\n              $81, CASE rc.relkind\n                        WHEN $82 THEN $83\n                        WHEN $84 THEN $85\n                        WHEN $86 THEN $87\n                        ELSE $88\n                      END,\n              $89, rn.nspname,\n              $90, rc.relname)\n          ELSE json_build_object(\n            $91, CASE base.typtype WHEN $92 THEN $93 ELSE $94 END,\n            $95, tn.nspname,\n            $96, base.typname)\n        END AS id\n      FROM pg_type tt\n      JOIN pg_type base\n        ON base.oid = CASE\n             WHEN tt.typtype = $97 AND tt.typcategory = $98 AND tt.typelem <> $99\n             THEN tt.typelem ELSE tt.oid END\n      JOIN pg_namespace tn ON tn.oid = base.typnamespace\n      LEFT JOIN pg_class rc ON rc.oid = base.typrelid\n                           AND rc.relkind IN ($100, $101, $102, $103, $104)\n      LEFT JOIN pg_namespace rn ON rn.oid = rc.relnamespace\n      WHERE base.typtype IN ($105, $106, $107, $108)\n    ),\n    -- a composite type's pg_class (relkind c) endpoint resolves to the composite\n    -- TYPE fact. pg_depend records a composite attribute's type dependency as\n    -- (classid=pg_class, objid=composite pg_class, objsubid=attnum) -> pg_type,\n    -- but the col CTE excludes relkind c. Attributing the edge to the top-level\n    -- type (not the folded typeAttribute child) keeps the create/teardown\n    -- ordering of a composite-uses-domain/type chain correct and independent of\n    -- child-drop folding.\n    comptype AS (\n      SELECT cc.oid, json_build_object($109,$110,$111,cn.nspname,\n                               $112,cc.relname) AS id\n      FROM pg_class cc JOIN pg_namespace cn ON cn.oid = cc.relnamespace\n      WHERE cc.relkind = $113\n    ),\n    extm AS (\n      -- a reference INTO an extension-member object resolves to the extension,\n      -- not the member fact (4b Stage 3): the member is observed but projected\n      -- out by default, so a member-targeted edge would be pruned with it and\n      -- lose the dependent's ordering on the extension. One join keyed by\n      -- (classid, objid) replaces the old per-branch nested pg_depend lookups;\n      -- an object belongs to at most one extension, so (classid, objid) is\n      -- unique here and the join never multiplies dep rows (the old form's\n      -- LIMIT 1 was guarding the same single-membership invariant).\n      SELECT ed.classid, ed.objid,\n             json_build_object($114,$115,$116,ext.extname) AS id\n      FROM pg_depend ed JOIN pg_extension ext ON ext.oid = ed.refobjid\n      WHERE ed.refclassid = $117::regclass AND ed.deptype = $118\n    ),\n    coll AS (\n      SELECT cl.oid, json_build_object($119,$120,$121,cln.nspname,\n                               $122,cl.collname) AS id\n      FROM pg_collation cl JOIN pg_namespace cln ON cln.oid = cl.collnamespace\n    ),\n    pol AS (\n      SELECT po.oid, json_build_object($123,$124,$125,pn.nspname,\n                               $126,pc.relname,$127,po.polname) AS id\n      FROM pg_policy po\n      JOIN pg_class pc ON pc.oid = po.polrelid\n      JOIN pg_namespace pn ON pn.oid = pc.relnamespace\n    ),\n    evt AS (\n      SELECT et.oid, json_build_object($128,$129,$130,et.evtname) AS id\n      FROM pg_event_trigger et\n    ),\n    pub AS (\n      SELECT pb.oid, json_build_object($131,$132,$133,pb.pubname) AS id\n      FROM pg_publication pb\n    ),\n    pubrel AS (\n      SELECT pr.oid, json_build_object($134,$135,$136,pb.pubname) AS id\n      FROM pg_publication_rel pr JOIN pg_publication pb ON pb.oid = pr.prpubid\n    ),\n    fdw AS (\n      SELECT fd.oid, json_build_object($137,$138,$139,fd.fdwname) AS id\n      FROM pg_foreign_data_wrapper fd\n    ),\n    srv AS (\n      SELECT fs.oid, json_build_object($140,$141,$142,fs.srvname) AS id\n      FROM pg_foreign_server fs\n    ),\n    adef AS (\n      SELECT ad.oid, json_build_object($143,$144,$145,dn.nspname,\n                               $146,dc.relname,$147,da.attname) AS id\n      FROM pg_attrdef ad\n      JOIN pg_class dc ON dc.oid = ad.adrelid\n      JOIN pg_namespace dn ON dn.oid = dc.relnamespace\n      JOIN pg_attribute da ON da.attrelid = ad.adrelid AND da.attnum = ad.adnum\n    ),\n    rw AS (\n      -- A pg_rewrite endpoint resolves two different ways (issue #333):\n      --  - the '_RETURN' rule of a view/matview IS the relation's definition,\n      --    so its deps are attributed to the owning view/matview -- rebuilding\n      --    the relation recreates the rule. Only views/matviews carry a\n      --    '_RETURN' rule, so the relkind CASE only ever sees 'v'/'m' here.\n      --  - ANY other (user-created) rule is its OWN rule fact\n      --    (src/extract/relations.ts extracts every rulename <> '_RETURN' on any\n      --    relkind), so its deps must be attributed to the RULE fact, not the\n      --    owning relation. This is what lets a rule on a PLAIN TABLE (relkind\n      --    'r'/'p') be rebuilt against a function it references before that\n      --    function is dropped. CONSTRAINT: a user rule ON A VIEW (not\n      --    '_RETURN') likewise resolves to the rule fact, exactly like a rule on\n      --    a table -- never folded into the view's '_RETURN' definition.\n      SELECT r.oid,\n        CASE WHEN r.rulename = $148\n          THEN json_build_object(\n                 $149, CASE vc.relkind WHEN $150 THEN $151 ELSE $152 END,\n                 $153, vn.nspname, $154, vc.relname)\n          ELSE json_build_object(\n                 $155, $156, $157, vn.nspname,\n                 $158, vc.relname, $159, r.rulename)\n        END AS id\n      FROM pg_rewrite r\n      JOIN pg_class vc ON vc.oid = r.ev_class\n      JOIN pg_namespace vn ON vn.oid = vc.relnamespace\n    ),\n    trg AS (\n      SELECT tg.oid, json_build_object($160,$161,$162,tn.nspname,\n                               $163,tc.relname,$164,tg.tgname) AS id\n      FROM pg_trigger tg\n      JOIN pg_class tc ON tc.oid = tg.tgrelid\n      JOIN pg_namespace tn ON tn.oid = tc.relnamespace\n      WHERE NOT tg.tgisinternal\n    ),\n    nsp AS (\n      SELECT ns.oid, json_build_object($165,$166,$167,ns.nspname) AS id\n      FROM pg_namespace ns\n    ),\n    -- MATERIALIZED: resolved is referenced twice (rd + rr joins); force a single\n    -- evaluation over the distinct endpoint set.\n    resolved AS MATERIALIZED (\n      SELECT e.classid, e.objid, e.objsubid,\n        CASE\n          WHEN e.classid = $168::regclass AND e.objsubid = $169\n            THEN COALESCE(cbi.id, comptype.id, rel.id)\n          WHEN e.classid = $170::regclass AND e.objsubid > $171\n            -- a real column; a composite type's attribute resolves to its type;\n            -- else a view/matview column resolves to its relation\n            THEN COALESCE(col.id, comptype.id, CASE WHEN rel.relkind IN ($172,$173) THEN rel.id END)\n          WHEN e.classid = $174::regclass THEN COALESCE(extm.id, proc.id)\n          WHEN e.classid = $175::regclass THEN COALESCE(tcon.id, dcon.id)\n          WHEN e.classid = $176::regclass THEN COALESCE(extm.id, typ.id)\n          WHEN e.classid = $177::regclass THEN extm.id\n          WHEN e.classid = $178::regclass THEN extm.id\n          WHEN e.classid = $179::regclass THEN extm.id\n          WHEN e.classid = $180::regclass THEN coll.id\n          WHEN e.classid = $181::regclass THEN pol.id\n          WHEN e.classid = $182::regclass THEN evt.id\n          WHEN e.classid = $183::regclass THEN pub.id\n          WHEN e.classid = $184::regclass THEN pubrel.id\n          WHEN e.classid = $185::regclass\n            THEN COALESCE(extm.id, fdw.id)\n          WHEN e.classid = $186::regclass THEN srv.id\n          WHEN e.classid = $187::regclass THEN adef.id\n          WHEN e.classid = $188::regclass THEN rw.id\n          WHEN e.classid = $189::regclass THEN trg.id\n          WHEN e.classid = $190::regclass THEN nsp.id\n          ELSE $191\n        END AS id\n      FROM endpoints e\n      LEFT JOIN rel    ON rel.oid    = e.objid AND e.classid = $192::regclass\n      LEFT JOIN cbi    ON cbi.oid    = e.objid AND e.classid = $193::regclass AND e.objsubid = $194\n      LEFT JOIN col    ON col.oid    = e.objid AND col.subid = e.objsubid AND e.classid = $195::regclass\n      LEFT JOIN proc   ON proc.oid   = e.objid AND e.classid = $196::regclass\n      LEFT JOIN tcon   ON tcon.oid   = e.objid AND e.classid = $197::regclass\n      LEFT JOIN dcon   ON dcon.oid   = e.objid AND e.classid = $198::regclass\n      LEFT JOIN typ    ON typ.oid    = e.objid AND e.classid = $199::regclass\n      LEFT JOIN comptype ON comptype.oid = e.objid AND e.classid = $200::regclass\n      LEFT JOIN extm   ON extm.classid = e.classid AND extm.objid = e.objid\n      LEFT JOIN coll   ON coll.oid   = e.objid AND e.classid = $201::regclass\n      LEFT JOIN pol    ON pol.oid    = e.objid AND e.classid = $202::regclass\n      LEFT JOIN evt    ON evt.oid    = e.objid AND e.classid = $203::regclass\n      LEFT JOIN pub    ON pub.oid    = e.objid AND e.classid = $204::regclass\n      LEFT JOIN pubrel ON pubrel.oid = e.objid AND e.classid = $205::regclass\n      LEFT JOIN fdw    ON fdw.oid    = e.objid AND e.classid = $206::regclass\n      LEFT JOIN srv    ON srv.oid    = e.objid AND e.classid = $207::regclass\n      LEFT JOIN adef   ON adef.oid   = e.objid AND e.classid = $208::regclass\n      LEFT JOIN rw     ON rw.oid     = e.objid AND e.classid = $209::regclass\n      LEFT JOIN trg    ON trg.oid    = e.objid AND e.classid = $210::regclass\n      LEFT JOIN nsp    ON nsp.oid    = e.objid AND e.classid = $211::regclass\n    )\n    SELECT rd.id AS dependent, rr.id AS referenced\n    FROM dep\n    JOIN resolved rd ON rd.classid = dep.classid\n                    AND rd.objid = dep.objid\n                    AND rd.objsubid = dep.objsubid\n    JOIN resolved rr ON rr.classid = dep.refclassid\n                    AND rr.objid = dep.refobjid\n                    AND rr.objsubid = dep.refobjsubid"},{"total_exec_time":"00:00:03.665504","prop_exec_time":"0.7%","ncalls":"150","sync_io_time":"00:00:00","query":"SELECT\n    tbl.schemaname,\n    tbl.tablename,\n    tbl.quoted_name,\n    tbl.is_table,\n    json_agg(a) as columns\n  FROM\n    (\n      SELECT\n        n.nspname as schemaname,\n        c.relname as tablename,\n        (quote_ident(n.nspname) || $1 || quote_ident(c.relname)) as quoted_name,\n        $2 as is_table\n      FROM\n        pg_catalog.pg_class c\n        JOIN pg_catalog.pg_namespace n ON n.oid = c.relnamespace\n      WHERE\n        c.relkind = $3\n        AND n.nspname not in ($4, $5, $6)\n        AND n.nspname not like $7\n        AND n.nspname not like $8\n        AND has_schema_privilege(n.oid, $9) = $10\n        AND has_table_privilege(quote_ident(n.nspname) || $11 || quote_ident(c.relname), $12) = $13\n      union all\n      SELECT\n        n.nspname as schemaname,\n        c.relname as tablename,\n        (quote_ident(n.nspname) || $14 || quote_ident(c.relname)) as quoted_name,\n        $15 as is_table\n      FROM\n        pg_catalog.pg_class c\n        JOIN pg_catalog.pg_namespace n ON n.oid = c.relnamespace\n      WHERE\n        c.relkind in ($16, $17)\n        AND n.nspname not in ($18, $19, $20)\n        AND n.nspname not like $21\n        AND n.nspname not like $22\n        AND has_schema_privilege(n.oid, $23) = $24\n        AND has_table_privilege(quote_ident(n.nspname) || $25 || quote_ident(c.relname), $26) = $27\n    ) as tbl\n    LEFT JOIN (\n      SELECT\n        attrelid,\n        attname,\n        format_type(atttypid, atttypmod) as data_type,\n        attnum,\n        attisdropped\n      FROM\n        pg_attribute\n    ) as a ON (\n      a.attrelid = tbl.quoted_name::regclass\n      AND a.attnum > $28\n      AND NOT a.attisdropped\n      AND has_column_privilege(tbl.quoted_name, a.attname, $29)\n    )\n  \n  GROUP BY schemaname, tablename, quoted_name, is_table"},{"total_exec_time":"00:00:02.986202","prop_exec_time":"0.6%","ncalls":"7","sync_io_time":"00:00:00","query":"with page as (\n      select\n        c.oid,\n        c.relname,\n        c.relrowsecurity,\n        c.relforcerowsecurity,\n        c.relreplident,\n        nc.nspname as schema,\n        -- Computed once here so the final select can reference it for both the\n        -- raw byte count and pg_size_pretty without re-walking heap+toast+indexes.\n        pg_total_relation_size(c.oid) as bytes_raw\n      from pg_namespace nc\n      join pg_class c on nc.oid = c.relnamespace\n      where c.relkind in ($1, $2)\n        and not pg_is_other_temp_schema(nc.oid)\n        and c.oid > $3\n        and (\n          pg_has_role(c.relowner, $4)\n          or has_table_privilege(\n            c.oid,\n            $5\n          )\n          or has_any_column_privilege(c.oid, $6)\n        )\n        and nc.nspname IN ($7)\n        \n      order by c.oid\n      limit $8\n    ),\n    page_primary_keys as (\n      select\n        c.oid::int8 as table_id,\n        jsonb_agg(\n          jsonb_build_object(\n            $9, c.oid::int8,\n            $10, n.nspname,\n            $11, c.relname,\n            $12, a.attname\n          )\n          order by array_position(i.indkey, a.attnum)\n        ) as primary_keys\n      from pg_index i\n      join pg_class c on i.indrelid = c.oid\n      join pg_namespace n on c.relnamespace = n.oid\n      join pg_attribute a on a.attrelid = c.oid and a.attnum = any(i.indkey)\n      where i.indisprimary\n        and c.oid in (select oid from page)\n      group by c.oid\n    ),\n    -- Two-armed UNION ALL keyed by table_id so the downstream join is a plain\n    -- equi-join (see tables CTE below). The previous shape used an OR across\n    -- (source_oid, target_oid), which planners can't decompose into two index\n    -- probes. The target-side arm skips self-referential FKs so they aren't\n    -- emitted twice.\n    page_relationships as (\n      select\n        csa.oid::int8 as table_id,\n        c.oid::int8 as id,\n        c.conname as constraint_name,\n        nsa.nspname as source_schema,\n        csa.relname as source_table_name,\n        sa.attname as source_column_name,\n        nta.nspname as target_table_schema,\n        cta.relname as target_table_name,\n        ta.attname as target_column_name\n      from pg_constraint c\n      join pg_class csa on csa.oid = c.conrelid\n      join pg_namespace nsa on nsa.oid = csa.relnamespace\n      -- Pair conkey/confkey by ordinal so composite FKs don't fan out into a\n      -- cross-product of (source_col, target_col) rows.\n      join lateral unnest(c.conkey, c.confkey) as fk(src_attnum, tgt_attnum) on $13\n      join pg_attribute sa on sa.attrelid = c.conrelid and sa.attnum = fk.src_attnum\n      join pg_class cta on cta.oid = c.confrelid\n      join pg_namespace nta on nta.oid = cta.relnamespace\n      join pg_attribute ta on ta.attrelid = c.confrelid and ta.attnum = fk.tgt_attnum\n      where c.contype = $14\n        and csa.oid in (select oid from page)\n      union all\n      select\n        cta.oid::int8 as table_id,\n        c.oid::int8 as id,\n        c.conname as constraint_name,\n        nsa.nspname as source_schema,\n        csa.relname as source_table_name,\n        sa.attname as source_column_name,\n        nta.nspname as target_table_schema,\n        cta.relname as target_table_name,\n        ta.attname as target_column_name\n      from pg_constraint c\n      join pg_class csa on csa.oid = c.conrelid\n      join pg_namespace nsa on nsa.oid = csa.relnamespace\n      join lateral unnest(c.conkey, c.confkey) as fk(src_attnum, tgt_attnum) on $15\n      join pg_attribute sa on sa.attrelid = c.conrelid and sa.attnum = fk.src_attnum\n      join pg_class cta on cta.oid = c.confrelid\n      join pg_namespace nta on nta.oid = cta.relnamespace\n      join pg_attribute ta on ta.attrelid = c.confrelid and ta.attnum = fk.tgt_attnum\n      where c.contype = $16\n        and cta.oid in (select oid from page)\n        and cta.oid <> csa.oid\n    ),\n    tables as (\n      select\n        p.oid::int8 as id,\n        p.schema as schema,\n        p.relname as name,\n        p.relrowsecurity as rls_enabled,\n        p.relforcerowsecurity as rls_forced,\n        case\n          when p.relreplident = $17 then $18\n          when p.relreplident = $19 then $20\n          when p.relreplident = $21 then $22\n          else $23\n        end as replica_identity,\n        p.bytes_raw::int8 as bytes,\n        pg_size_pretty(p.bytes_raw) as size,\n        pg_stat_get_live_tuples(p.oid) as live_rows_estimate,\n        pg_stat_get_dead_tuples(p.oid) as dead_rows_estimate,\n        obj_description(p.oid) as comment,\n        coalesce(pk.primary_keys, $24::jsonb) as primary_keys,\n        coalesce(\n          jsonb_agg(\n            jsonb_build_object(\n              $25, r.id,\n              $26, r.constraint_name,\n              $27, r.source_schema,\n              $28, r.source_table_name,\n              $29, r.source_column_name,\n              $30, r.target_table_schema,\n              $31, r.target_table_name,\n              $32, r.target_column_name\n            )\n          ) filter (where r.id is not null),\n          $33::jsonb\n        ) as relationships\n      from page p\n      left join page_primary_keys pk on pk.table_id = p.oid\n      left join page_relationships r on r.table_id = p.oid\n      group by\n        p.oid,\n        p.schema,\n        p.relname,\n        p.relrowsecurity,\n        p.relforcerowsecurity,\n        p.relreplident,\n        p.bytes_raw,\n        pk.primary_keys\n    ), columns as (\n-- Adapted from information_schema.columns\n\nSELECT\n  c.oid :: int8 AS table_id,\n  nc.nspname AS schema,\n  c.relname AS table,\n  (c.oid || $34 || a.attnum) AS id,\n  a.attnum AS ordinal_position,\n  a.attname AS name,\n  CASE\n    WHEN a.atthasdef THEN pg_get_expr(ad.adbin, ad.adrelid)\n    ELSE $35\n  END AS default_value,\n  CASE\n    WHEN t.typtype = $36 THEN CASE\n      WHEN bt.typelem <> $37 :: oid\n      AND bt.typlen = $38 THEN $39\n      WHEN nbt.nspname = $40 THEN format_type(t.typbasetype, $41)\n      ELSE $42\n    END\n    ELSE CASE\n      WHEN t.typelem <> $43 :: oid\n      AND t.typlen = $44 THEN $45\n      WHEN nt.nspname = $46 THEN format_type(a.atttypid, $47)\n      ELSE $48\n    END\n  END AS data_type,\n  COALESCE(bt.typname, t.typname) AS format,\n  COALESCE(nbt.nspname, nt.nspname) AS format_schema,\n  a.attidentity IN ($49, $50) AS is_identity,\n  CASE\n    a.attidentity\n    WHEN $51 THEN $52\n    WHEN $53 THEN $54\n    ELSE $55\n  END AS identity_generation,\n  a.attgenerated IN ($56) AS is_generated,\n  NOT (\n    a.attnotnull\n    OR t.typtype = $57 AND t.typnotnull\n  ) AS is_nullable,\n  (\n    c.relkind IN ($58, $59)\n    OR c.relkind IN ($60, $61) AND pg_column_is_updatable(c.oid, a.attnum, $62)\n  ) AS is_updatable,\n  uniques.table_id IS NOT NULL AS is_unique,\n  check_constraints.definition AS \"check\",\n  array_to_json(\n    array(\n      SELECT\n        enumlabel\n      FROM\n        pg_catalog.pg_enum enums\n      WHERE\n        enums.enumtypid = coalesce(bt.oid, t.oid)\n        OR enums.enumtypid = coalesce(bt.typelem, t.typelem)\n      ORDER BY\n        enums.enumsortorder\n    )\n  ) AS enums,\n  col_description(c.oid, a.attnum) AS comment\nFROM\n  pg_attribute a\n  LEFT JOIN pg_attrdef ad ON a.attrelid = ad.adrelid\n  AND a.attnum = ad.adnum\n  JOIN (\n    pg_class c\n    JOIN pg_namespace nc ON c.relnamespace = nc.oid\n  ) ON a.attrelid = c.oid\n  JOIN (\n    pg_type t\n    JOIN pg_namespace nt ON t.typnamespace = nt.oid\n  ) ON a.atttypid = t.oid\n  LEFT JOIN (\n    pg_type bt\n    JOIN pg_namespace nbt ON bt.typnamespace = nbt.oid\n  ) ON t.typtype = $63\n  AND t.typbasetype = bt.oid\n  LEFT JOIN (\n    SELECT DISTINCT ON (table_id, ordinal_position)\n      conrelid AS table_id,\n      conkey[$64] AS ordinal_position\n    FROM pg_catalog.pg_constraint\n    WHERE contype = $65 AND cardinality(conkey) = $66\n  ) AS uniques ON uniques.table_id = c.oid AND uniques.ordinal_position = a.attnum\n  LEFT JOIN (\n    -- We only select the first column check\n    SELECT DISTINCT ON (table_id, ordinal_position)\n      conrelid AS table_id,\n      conkey[$67] AS ordinal_position,\n      substring(\n        pg_get_constraintdef(pg_constraint.oid, $68),\n        $69,\n        length(pg_get_constraintdef(pg_constraint.oid, $70)) - $71\n      ) AS \"definition\"\n    FROM pg_constraint\n    WHERE contype = $72 AND cardinality(conkey) = $73\n    ORDER BY table_id, ordinal_position, oid asc\n  ) AS check_constraints ON check_constraints.table_id = c.oid AND check_constraints.ordinal_position = a.attnum\nWHERE\n  NOT pg_is_other_temp_schema(nc.oid)\n  AND a.attnum > $74\n  AND NOT a.attisdropped\n  AND (c.relkind IN ($75, $76, $77, $78, $79))\n  AND (\n    pg_has_role(c.relowner, $80)\n    OR has_column_privilege(\n      c.oid,\n      a.attnum,\n      $81\n    )\n  )\n  AND c.oid in (select oid from page)\n)\n    select tables.*, \nCOALESCE(\n  (\n    SELECT\n      array_agg(row_to_json(columns)) FILTER (WHERE columns.table_id = tables.id)\n    FROM\n      columns\n  ),\n  $82\n) AS columns\n    from tables\n    order by tables.id\n  \n\n-- source: dashboard\n-- user: session:c96811ca-6454-4391-b085-973d30578cc3\n-- date: 2026-08-08T13:06:16.959Z"},{"total_exec_time":"00:00:02.328832","prop_exec_time":"0.4%","ncalls":"552","sync_io_time":"00:00:00","query":"SELECT public.retry_failed_background_jobs()"},{"total_exec_time":"00:00:02.083678","prop_exec_time":"0.4%","ncalls":"4","sync_io_time":"00:00:00","query":"with tables as (\nSELECT\n  c.oid :: int8 AS id,\n  nc.nspname AS schema,\n  c.relname AS name,\n  c.relrowsecurity AS rls_enabled,\n  c.relforcerowsecurity AS rls_forced,\n  CASE\n    WHEN c.relreplident = $1 THEN $2\n    WHEN c.relreplident = $3 THEN $4\n    WHEN c.relreplident = $5 THEN $6\n    ELSE $7\n  END AS replica_identity,\n  pg_total_relation_size(format($8, nc.nspname, c.relname)) :: int8 AS bytes,\n  pg_size_pretty(\n    pg_total_relation_size(format($9, nc.nspname, c.relname))\n  ) AS size,\n  pg_stat_get_live_tuples(c.oid) AS live_rows_estimate,\n  pg_stat_get_dead_tuples(c.oid) AS dead_rows_estimate,\n  obj_description(c.oid) AS comment,\n  coalesce(pk.primary_keys, $10) as primary_keys,\n  coalesce(\n    jsonb_agg(relationships) filter (where relationships is not null),\n    $11\n  ) as relationships\nFROM\n  pg_namespace nc\n  JOIN pg_class c ON nc.oid = c.relnamespace\n  left join (\n    select\n      c.oid::int8 as table_id,\n      jsonb_agg(\n        jsonb_build_object(\n          $12, c.oid::int8,\n          $13, n.nspname,\n          $14, c.relname,\n          $15, a.attname\n        )\n        order by array_position(i.indkey, a.attnum)\n      ) as primary_keys\n    from\n      pg_index i\n      join pg_class c on i.indrelid = c.oid\n      join pg_namespace n on c.relnamespace = n.oid\n      join pg_attribute a on a.attrelid = c.oid and a.attnum = any(i.indkey)\n    where\n      i.indisprimary\n    group by c.oid\n  ) as pk\n  on pk.table_id = c.oid\n  left join (\n    select\n      c.oid :: int8 as id,\n      c.conname as constraint_name,\n      nsa.nspname as source_schema,\n      csa.relname as source_table_name,\n      sa.attname as source_column_name,\n      nta.nspname as target_table_schema,\n      cta.relname as target_table_name,\n      ta.attname as target_column_name\n    from\n      pg_constraint c\n    join (\n      pg_attribute sa\n      join pg_class csa on sa.attrelid = csa.oid\n      join pg_namespace nsa on csa.relnamespace = nsa.oid\n    ) on sa.attrelid = c.conrelid and sa.attnum = any (c.conkey)\n    join (\n      pg_attribute ta\n      join pg_class cta on ta.attrelid = cta.oid\n      join pg_namespace nta on cta.relnamespace = nta.oid\n    ) on ta.attrelid = c.confrelid and ta.attnum = any (c.confkey)\n    where\n      c.contype = $16\n  ) as relationships\n  on (relationships.source_schema = nc.nspname and relationships.source_table_name = c.relname)\n  or (relationships.target_table_schema = nc.nspname and relationships.target_table_name = c.relname)\nWHERE\n  c.relkind IN ($17, $18)\n  AND NOT pg_is_other_temp_schema(nc.oid)\n  AND (\n    pg_has_role(c.relowner, $19)\n    OR has_table_privilege(\n      c.oid,\n      $20\n    )\n    OR has_any_column_privilege(c.oid, $21)\n  )\ngroup by\n  c.oid,\n  c.relname,\n  c.relrowsecurity,\n  c.relforcerowsecurity,\n  c.relreplident,\n  nc.nspname,\n  pk.primary_keys\n)\n  , columns as (\n-- Adapted from information_schema.columns\n\nSELECT\n  c.oid :: int8 AS table_id,\n  nc.nspname AS schema,\n  c.relname AS table,\n  (c.oid || $22 || a.attnum) AS id,\n  a.attnum AS ordinal_position,\n  a.attname AS name,\n  CASE\n    WHEN a.atthasdef THEN pg_get_expr(ad.adbin, ad.adrelid)\n    ELSE $23\n  END AS default_value,\n  CASE\n    WHEN t.typtype = $24 THEN CASE\n      WHEN bt.typelem <> $25 :: oid\n      AND bt.typlen = $26 THEN $27\n      WHEN nbt.nspname = $28 THEN format_type(t.typbasetype, $29)\n      ELSE $30\n    END\n    ELSE CASE\n      WHEN t.typelem <> $31 :: oid\n      AND t.typlen = $32 THEN $33\n      WHEN nt.nspname = $34 THEN format_type(a.atttypid, $35)\n      ELSE $36\n    END\n  END AS data_type,\n  COALESCE(bt.typname, t.typname) AS format,\n  COALESCE(nbt.nspname, nt.nspname) AS format_schema,\n  a.attidentity IN ($37, $38) AS is_identity,\n  CASE\n    a.attidentity\n    WHEN $39 THEN $40\n    WHEN $41 THEN $42\n    ELSE $43\n  END AS identity_generation,\n  a.attgenerated IN ($44) AS is_generated,\n  NOT (\n    a.attnotnull\n    OR t.typtype = $45 AND t.typnotnull\n  ) AS is_nullable,\n  (\n    c.relkind IN ($46, $47)\n    OR c.relkind IN ($48, $49) AND pg_column_is_updatable(c.oid, a.attnum, $50)\n  ) AS is_updatable,\n  uniques.table_id IS NOT NULL AS is_unique,\n  check_constraints.definition AS \"check\",\n  array_to_json(\n    array(\n      SELECT\n        enumlabel\n      FROM\n        pg_catalog.pg_enum enums\n      WHERE\n        enums.enumtypid = coalesce(bt.oid, t.oid)\n        OR enums.enumtypid = coalesce(bt.typelem, t.typelem)\n      ORDER BY\n        enums.enumsortorder\n    )\n  ) AS enums,\n  col_description(c.oid, a.attnum) AS comment\nFROM\n  pg_attribute a\n  LEFT JOIN pg_attrdef ad ON a.attrelid = ad.adrelid\n  AND a.attnum = ad.adnum\n  JOIN (\n    pg_class c\n    JOIN pg_namespace nc ON c.relnamespace = nc.oid\n  ) ON a.attrelid = c.oid\n  JOIN (\n    pg_type t\n    JOIN pg_namespace nt ON t.typnamespace = nt.oid\n  ) ON a.atttypid = t.oid\n  LEFT JOIN (\n    pg_type bt\n    JOIN pg_namespace nbt ON bt.typnamespace = nbt.oid\n  ) ON t.typtype = $51\n  AND t.typbasetype = bt.oid\n  LEFT JOIN (\n    SELECT DISTINCT ON (table_id, ordinal_position)\n      conrelid AS table_id,\n      conkey[$52] AS ordinal_position\n    FROM pg_catalog.pg_constraint\n    WHERE contype = $53 AND cardinality(conkey) = $54\n  ) AS uniques ON uniques.table_id = c.oid AND uniques.ordinal_position = a.attnum\n  LEFT JOIN (\n    -- We only select the first column check\n    SELECT DISTINCT ON (table_id, ordinal_position)\n      conrelid AS table_id,\n      conkey[$55] AS ordinal_position,\n      substring(\n        pg_get_constraintdef(pg_constraint.oid, $56),\n        $57,\n        length(pg_get_constraintdef(pg_constraint.oid, $58)) - $59\n      ) AS \"definition\"\n    FROM pg_constraint\n    WHERE contype = $60 AND cardinality(conkey) = $61\n    ORDER BY table_id, ordinal_position, oid asc\n  ) AS check_constraints ON check_constraints.table_id = c.oid AND check_constraints.ordinal_position = a.attnum\nWHERE\n  NOT pg_is_other_temp_schema(nc.oid)\n  AND a.attnum > $62\n  AND NOT a.attisdropped\n  AND (c.relkind IN ($63, $64, $65, $66, $67))\n  AND (\n    pg_has_role(c.relowner, $68)\n    OR has_column_privilege(\n      c.oid,\n      a.attnum,\n      $69\n    )\n  )\n  \n)\n  select\n    *\n    , \nCOALESCE(\n  (\n    SELECT\n      array_agg(row_to_json(columns)) FILTER (WHERE columns.table_id = tables.id)\n    FROM\n      columns\n  ),\n  $70\n) AS columns\n  from tables where schema NOT IN ($71,$72,$73)\n\n-- source: dashboard\n-- user: session:c96811ca-6454-4391-b085-973d30578cc3\n-- date: 2026-08-05T20:40:06.368Z"}],"message":"inspect db outliers"}
```


## Rohdaten: index-stats (Quelle für Seq-Scans & unbenutzte Indizes)

```json
{"rows":[{"name":"public.idx_bnf_ip_hash","table":"public.bet_network_fingerprints","columns":"ip_hash,last_seen_at,user_id","size":"392 kB","percent_used":"100%","index_scans":"1","seq_scans":"0","unused":false},{"name":"public.wallet_transactions_user_request_key","table":"public.wallet_transactions","columns":"user_id,request_id","size":"168 kB","percent_used":"100%","index_scans":"3924","seq_scans":"0","unused":false},{"name":"public.users_pkey","table":"public.users","columns":"id","size":"160 kB","percent_used":"100%","index_scans":"20409","seq_scans":"0","unused":false},{"name":"public.idx_wallet_transactions_history_cursor","table":"public.wallet_transactions","columns":"user_id,created_at,id","size":"160 kB","percent_used":"100%","index_scans":"620","seq_scans":"0","unused":false},{"name":"public.idx_users_id","table":"public.users","columns":"id","size":"160 kB","percent_used":"100%","index_scans":"14614","seq_scans":"0","unused":false},{"name":"public.seed_consumptions_pkey","table":"public.seed_consumptions","columns":"user_id,request_id","size":"120 kB","percent_used":"100%","index_scans":"1968","seq_scans":"0","unused":false},{"name":"public.bet_network_fingerprints_user_id_ip_hash_seen_date_key","table":"public.bet_network_fingerprints","columns":"user_id,ip_hash,seen_date","size":"104 kB","percent_used":"100%","index_scans":"1564","seq_scans":"0","unused":false},{"name":"public.idx_wallet_transactions_type_created","table":"public.wallet_transactions","columns":"type,created_at","size":"96 kB","percent_used":"100%","index_scans":"14","seq_scans":"0","unused":false},{"name":"public.wallet_events_user_id_request_id_event_type_key","table":"public.wallet_events","columns":"user_id,request_id,event_type","size":"88 kB","percent_used":"100%","index_scans":"539","seq_scans":"0","unused":false},{"name":"public.idx_wallet_transactions_actor","table":"public.wallet_transactions","columns":"actor_id,created_at","size":"88 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.wallet_transactions_pkey","table":"public.wallet_transactions","columns":"id","size":"88 kB","percent_used":"100%","index_scans":"1534","seq_scans":"0","unused":false},{"name":"public.idx_transactions_created","table":"public.wallet_transactions","columns":"created_at","size":"80 kB","percent_used":"100%","index_scans":"16124","seq_scans":"0","unused":false},{"name":"public.idx_transactions_user","table":"public.wallet_transactions","columns":"user_id","size":"72 kB","percent_used":"100%","index_scans":"16878","seq_scans":"0","unused":false},{"name":"public.seeds_pkey","table":"public.seeds","columns":"user_id","size":"56 kB","percent_used":"100%","index_scans":"8739","seq_scans":"0","unused":false},{"name":"public.wallet_events_pkey","table":"public.wallet_events","columns":"id","size":"48 kB","percent_used":"100%","index_scans":"1028","seq_scans":"0","unused":false},{"name":"public.idx_bnf_created_at","table":"public.bet_network_fingerprints","columns":"first_seen_at","size":"40 kB","percent_used":"100%","index_scans":"27","seq_scans":"0","unused":false},{"name":"public.bet_network_fingerprints_pkey","table":"public.bet_network_fingerprints","columns":"id","size":"40 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.idx_transactions_game","table":"public.wallet_transactions","columns":"game","size":"40 kB","percent_used":"100%","index_scans":"6","seq_scans":"0","unused":false},{"name":"public.idx_game_rounds_active","table":"public.game_rounds","columns":"user_id,game,status,created_at","size":"40 kB","percent_used":"100%","index_scans":"3765","seq_scans":"0","unused":false},{"name":"public.game_rounds_user_id_request_id_key","table":"public.game_rounds","columns":"user_id,request_id","size":"32 kB","percent_used":"100%","index_scans":"1872","seq_scans":"0","unused":false},{"name":"public.guide_telemetry_events_occurred_at_idx","table":"public.guide_telemetry_events","columns":"occurred_at","size":"16 kB","percent_used":"100%","index_scans":"69","seq_scans":"0","unused":false},{"name":"public.idx_user_identities_user_id","table":"public.user_identities","columns":"user_id","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.idx_vip_tiers_sort","table":"public.vip_tiers","columns":"sort_order","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.idx_achievement_configs_sort","table":"public.achievement_configs","columns":"sort_order","size":"16 kB","percent_used":"100%","index_scans":"433","seq_scans":"0","unused":false},{"name":"public.user_notifications_user_id_source_key_key","table":"public.user_notifications","columns":"user_id,source_key","size":"16 kB","percent_used":"100%","index_scans":"4","seq_scans":"0","unused":false},{"name":"public.background_job_runs_job_name_run_key_key","table":"public.background_job_runs","columns":"job_name,run_key","size":"16 kB","percent_used":"100%","index_scans":"12","seq_scans":"0","unused":false},{"name":"public.promo_code_redemptions_pkey","table":"public.promo_code_redemptions","columns":"id","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.idx_guide_documents_topic","table":"public.guide_documents","columns":"topic","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.crash_rounds_pkey","table":"public.crash_rounds","columns":"id","size":"16 kB","percent_used":"100%","index_scans":"437","seq_scans":"0","unused":false},{"name":"public.guide_documents_embedding_hnsw","table":"public.guide_documents","columns":"embedding","size":"16 kB","percent_used":"100%","index_scans":"4","seq_scans":"0","unused":false},{"name":"public.seed_history_pkey","table":"public.seed_history","columns":"id","size":"16 kB","percent_used":"100%","index_scans":"1","seq_scans":"0","unused":false},{"name":"public.background_job_runs_pkey","table":"public.background_job_runs","columns":"id","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.telegram_link_tokens_pkey","table":"public.telegram_link_tokens","columns":"token","size":"16 kB","percent_used":"100%","index_scans":"2","seq_scans":"0","unused":false},{"name":"public.game_rounds_pkey","table":"public.game_rounds","columns":"id","size":"16 kB","percent_used":"100%","index_scans":"280","seq_scans":"0","unused":false},{"name":"public.ranks_pkey","table":"public.ranks","columns":"id","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.game_configs_category_config_key_key","table":"public.game_configs","columns":"category,config_key","size":"16 kB","percent_used":"100%","index_scans":"6","seq_scans":"0","unused":false},{"name":"public.telegram_links_chat_id_key","table":"public.telegram_links","columns":"chat_id","size":"16 kB","percent_used":"100%","index_scans":"1","seq_scans":"0","unused":false},{"name":"public.idx_wallet_events_unprocessed","table":"public.wallet_events","columns":"created_at","size":"16 kB","percent_used":"100%","index_scans":"41163","seq_scans":"0","unused":false},{"name":"public.idx_chat_messages_created","table":"public.chat_messages","columns":"created_at","size":"16 kB","percent_used":"100%","index_scans":"6897","seq_scans":"0","unused":false},{"name":"public.idx_guide_documents_active","table":"public.guide_documents","columns":"is_active","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.promo_codes_pkey","table":"public.promo_codes","columns":"code","size":"16 kB","percent_used":"100%","index_scans":"11","seq_scans":"0","unused":false},{"name":"public.guide_telemetry_events_actor_hash_actor_hash_version_outcom_key","table":"public.guide_telemetry_events","columns":"actor_hash,actor_hash_version,outcome,rate_limit_window_started_at","size":"16 kB","percent_used":"100%","index_scans":"5","seq_scans":"0","unused":false},{"name":"public.promo_code_redemptions_user_id_code_key","table":"public.promo_code_redemptions","columns":"user_id,code","size":"16 kB","percent_used":"100%","index_scans":"7","seq_scans":"0","unused":false},{"name":"public.idx_user_notifications_inbox","table":"public.user_notifications","columns":"user_id,created_at","size":"16 kB","percent_used":"100%","index_scans":"150","seq_scans":"0","unused":false},{"name":"public.game_configs_pkey","table":"public.game_configs","columns":"id","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.daily_race_winners_pkey","table":"public.daily_race_winners","columns":"race_date,rank","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.daily_races_pkey","table":"public.daily_races","columns":"race_date","size":"16 kB","percent_used":"100%","index_scans":"42","seq_scans":"0","unused":false},{"name":"public.user_identities_user_id_provider_key","table":"public.user_identities","columns":"user_id,provider","size":"16 kB","percent_used":"100%","index_scans":"35","seq_scans":"0","unused":false},{"name":"public.telegram_links_pkey","table":"public.telegram_links","columns":"user_id","size":"16 kB","percent_used":"100%","index_scans":"50","seq_scans":"0","unused":false},{"name":"public.idx_seed_history_user","table":"public.seed_history","columns":"user_id,rotated_at","size":"16 kB","percent_used":"100%","index_scans":"3","seq_scans":"0","unused":false},{"name":"public.guide_feedback_rating_created_at_idx","table":"public.guide_feedback","columns":"rating,created_at","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.idx_ranks_sort","table":"public.ranks","columns":"sort_order","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.user_identities_provider_provider_user_id_key","table":"public.user_identities","columns":"provider,provider_user_id","size":"16 kB","percent_used":"100%","index_scans":"17485","seq_scans":"0","unused":false},{"name":"public.chat_messages_pkey","table":"public.chat_messages","columns":"id","size":"16 kB","percent_used":"100%","index_scans":"2","seq_scans":"0","unused":false},{"name":"public.ranks_name_key","table":"public.ranks","columns":"name","size":"16 kB","percent_used":"100%","index_scans":"5","seq_scans":"0","unused":false},{"name":"public.guide_telemetry_events_pkey","table":"public.guide_telemetry_events","columns":"id","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.risk_events_subject_user_id_fingerprint_key","table":"public.risk_events","columns":"subject_user_id,fingerprint","size":"16 kB","percent_used":"100%","index_scans":"1","seq_scans":"0","unused":false},{"name":"public.idx_promo_codes_active","table":"public.promo_codes","columns":"active","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.idx_user_notifications_unread","table":"public.user_notifications","columns":"user_id,created_at","size":"16 kB","percent_used":"100%","index_scans":"300","seq_scans":"0","unused":false},{"name":"public.jackpot_pool_pkey","table":"public.jackpot_pool","columns":"id","size":"16 kB","percent_used":"100%","index_scans":"1946","seq_scans":"0","unused":false},{"name":"public.user_identities_pkey","table":"public.user_identities","columns":"id","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.idx_user_login_history_timeline","table":"public.user_login_history","columns":"user_id,created_at","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.promo_code_redemptions_user_id_request_id_key","table":"public.promo_code_redemptions","columns":"user_id,request_id","size":"16 kB","percent_used":"100%","index_scans":"8","seq_scans":"0","unused":false},{"name":"public.idx_crash_rounds_status","table":"public.crash_rounds","columns":"status,created_at","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.guide_documents_slug_key","table":"public.guide_documents","columns":"slug","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.vip_tiers_pkey","table":"public.vip_tiers","columns":"id","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.idx_game_rounds_crash_round","table":"public.game_rounds","columns":"crash_round_id","size":"16 kB","percent_used":"100%","index_scans":"6","seq_scans":"0","unused":false},{"name":"public.telegram_link_tokens_user_id_idx","table":"public.telegram_link_tokens","columns":"user_id","size":"16 kB","percent_used":"100%","index_scans":"2","seq_scans":"0","unused":false},{"name":"public.idx_game_rounds_settled_updated","table":"public.game_rounds","columns":"status,updated_at","size":"16 kB","percent_used":"100%","index_scans":"1300","seq_scans":"0","unused":false},{"name":"public.guide_telemetry_events_outcome_occurred_at_idx","table":"public.guide_telemetry_events","columns":"outcome,occurred_at","size":"16 kB","percent_used":"100%","index_scans":"1","seq_scans":"0","unused":false},{"name":"public.guide_feedback_created_at_idx","table":"public.guide_feedback","columns":"created_at","size":"16 kB","percent_used":"100%","index_scans":"12","seq_scans":"0","unused":false},{"name":"public.user_login_history_pkey","table":"public.user_login_history","columns":"id","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.wallet_ledger_baselines_pkey","table":"public.wallet_ledger_baselines","columns":"user_id","size":"16 kB","percent_used":"100%","index_scans":"29","seq_scans":"0","unused":false},{"name":"public.risk_events_pkey","table":"public.risk_events","columns":"id","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.user_achievements_pkey","table":"public.user_achievements","columns":"user_id,achievement_id","size":"16 kB","percent_used":"100%","index_scans":"350","seq_scans":"0","unused":false},{"name":"public.user_notifications_pkey","table":"public.user_notifications","columns":"id","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.fraud_scan_lock_pkey","table":"public.fraud_scan_lock","columns":"id","size":"16 kB","percent_used":"100%","index_scans":"5","seq_scans":"0","unused":false},{"name":"public.guide_documents_pkey","table":"public.guide_documents","columns":"id","size":"16 kB","percent_used":"100%","index_scans":"20","seq_scans":"0","unused":false},{"name":"public.vip_tiers_name_key","table":"public.vip_tiers","columns":"name","size":"16 kB","percent_used":"100%","index_scans":"5","seq_scans":"0","unused":false},{"name":"public.idx_game_configs_category","table":"public.game_configs","columns":"category","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.admin_analytics_snapshots_pkey","table":"public.admin_analytics_snapshots","columns":"id","size":"16 kB","percent_used":"100%","index_scans":"14","seq_scans":"0","unused":false},{"name":"public.achievement_configs_pkey","table":"public.achievement_configs","columns":"id","size":"16 kB","percent_used":"100%","index_scans":"10","seq_scans":"0","unused":false},{"name":"public.idx_risk_events_review_queue","table":"public.risk_events","columns":"status,severity,last_seen_at","size":"16 kB","percent_used":"100%","index_scans":"9","seq_scans":"0","unused":false},{"name":"public.guide_feedback_pkey","table":"public.guide_feedback","columns":"id","size":"16 kB","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.idx_user_achievements_user","table":"public.user_achievements","columns":"user_id","size":"16 kB","percent_used":"100%","index_scans":"1075","seq_scans":"0","unused":false},{"name":"public.idx_identity_link_quarantine_unresolved","table":"public.identity_link_quarantine","columns":"detected_at","size":"8192 bytes","percent_used":"100%","index_scans":"3","seq_scans":"0","unused":false},{"name":"public.idx_anonymous_sessions_migrated","table":"public.anonymous_sessions","columns":"migrated_to_user_id","size":"8192 bytes","percent_used":"100%","index_scans":"1","seq_scans":"0","unused":false},{"name":"public.idx_sessions_game","table":"public.game_sessions","columns":"game","size":"8192 bytes","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.anonymous_sessions_pkey","table":"public.anonymous_sessions","columns":"id","size":"8192 bytes","percent_used":"100%","index_scans":"1","seq_scans":"0","unused":false},{"name":"public.idx_anonymous_sessions_last_active","table":"public.anonymous_sessions","columns":"last_active_at","size":"8192 bytes","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.admin_roles_pkey","table":"public.admin_roles","columns":"user_id,role","size":"8192 bytes","percent_used":"100%","index_scans":"1","seq_scans":"0","unused":false},{"name":"public.wallet_invariant_events_pkey","table":"public.wallet_invariant_events","columns":"id","size":"8192 bytes","percent_used":"100%","index_scans":"1","seq_scans":"0","unused":false},{"name":"public.identity_link_quarantine_pkey","table":"public.identity_link_quarantine","columns":"provider,provider_user_id","size":"8192 bytes","percent_used":"100%","index_scans":"17466","seq_scans":"0","unused":false},{"name":"public.game_sessions_pkey","table":"public.game_sessions","columns":"id","size":"8192 bytes","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.wallet_invariant_events_user_id_fingerprint_key","table":"public.wallet_invariant_events","columns":"user_id,fingerprint","size":"8192 bytes","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.idx_admin_roles_role_user","table":"public.admin_roles","columns":"role,user_id","size":"8192 bytes","percent_used":"0%","index_scans":"0","seq_scans":"0","unused":true},{"name":"public.idx_sessions_user","table":"public.game_sessions","columns":"user_id","size":"8192 bytes","percent_used":"100%","index_scans":"1","seq_scans":"0","unused":false}],"message":"inspect db index-stats"}
```
