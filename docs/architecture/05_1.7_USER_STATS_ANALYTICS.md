# 05.1.7 — World Map: Persönliche User-Stats-Analytics-Seite — Vollständiger Implementationsplan

> **Erstellt:** 2026-08-10 · **Status:** Executed (siehe Abschnitt 8 für Ergebnis) · **Scope:** `supabase/migrations/018_user_stats_per_game.sql` (neu), `src/lib/casino/stats-derivation.ts` (neu), `src/lib/casino/__tests__/stats-derivation.test.ts` (neu), `src/lib/casino/wallet.ts` (erweitert), `src/app/stats/page.tsx` (neu), `src/app/stats/layout.tsx` (neu), `src/components/stats/**` (neu, 5 Dateien), `src/components/layout/MainLayout.tsx` (Sidebar-Eintrag), `src/proxy.ts` (Public-Route, siehe Abschnitt 8.1).
> **Vorgänger:** [`05_ZUKUNFTSPLANUNG.md`](../../worldmap/05_ZUKUNFTSPLANUNG.md) Initiative 1.7. Die dortige Scope-Skizze ("liest über bestehende `get_user_stats()`-RPC, keine neue Migration") wird in Abschnitt 2 korrigiert — die RPC liefert nur Lifetime-Summen, keine Pro-Spiel-Aufschlüsselung.
> **Richtungsentscheidungen (Jan, 2026-08-10):** Win/Loss-Verlauf über die letzten 100 Bets ohne neue RPC (1B) · Lieblingsspiel via RPC-Erweiterung um Pro-Spiel-Breakdown (2B) · Session-Länge als Tages-Näherungswert aus `wallet_transactions` (3B) · eigenständiges Bento-Grid-Layout (4B) · eigener Sidebar-Eintrag (5A).
> **Auftrag:** Vollumfänglicher Implementationsplan auf Weltklassenniveau, zweifache Selbstprüfung, danach **direkte Execution ohne Rückfrage** (explizite Freigabe durch Jan für diesen Ablauf, siehe Chat-Auftrag 2026-08-10).

---

## 1 — Zielbild

Eine neue Route `/stats` zeigt dem eingeloggten Spieler seine persönliche Performance als eigenständiges Bento-Grid-Dashboard — visuell abgehoben von `/history` (Tabellen-fokussiert) und `/vault` (Progression-fokussiert), mit echtem Charakter statt einer dritten Variante desselben Kachel-Layouts:

- **Hero-Chart:** kumulierter Profit-Verlauf über die letzten 100 Bets (Line/Area-Chart).
- **Lieblingsspiel:** welches Spiel wird am meisten gespielt (nach Einsatz), mit Pro-Spiel-Verteilung.
- **Pro-Spiel-Profit:** wo wird tatsächlich gewonnen/verloren, aufgeschlüsselt je Spiel.
- **Session-Länge (Näherungswert):** wie lange dauern typische Spielsitzungen, approximiert aus Bet-Zeitstempeln.
- **Lifetime-Kennzahlen:** Gesamteinsatz, Gesamtprofit, Winrate, Gesamtzahl Bets (bereits vorhandene `get_user_stats()`-Daten, wie auf `/history`).

---

## 2 — Ist-Zustand-Befunde (Grundlage für die Entscheidungen unten)

Vor der Implementierung wurde der bestehende Code vollständig gelesen (`supabase/migrations/013_user_stats_achievements.sql`, `014_fix_user_stats.sql`, `007_server_authority.sql`, `002_wallet.sql`, `src/lib/casino/wallet.ts`, `src/app/api/user/{stats,history}/route.ts`, `src/app/history/page.tsx`, `src/store/useCasinoStore.ts`, `src/components/layout/MainLayout.tsx`, `src/components/casino/PlayerProfileModal.tsx`, `src/app/games/page.tsx`).

| #   | Befund                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Beleg                                                                                                                                                                                        |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | `get_user_stats()` liefert ausschließlich Lifetime-**Summen** (`totalBets`, `totalWagered`, `totalPayout`, `totalProfit`, `winRate`, `achievements`) — **keine** Pro-Spiel-Aufschlüsselung. Die Roadmap-Skizze ("keine neue Migration nötig") ist damit für "Lieblingsspiel" nicht korrekt.                                                                                                                                                                                                                                                                                                                                                                                                                           | `014_fix_user_stats.sql:310-400`, gegen `05_ZUKUNFTSPLANUNG.md` Abschnitt 3.7 geprüft                                                                                                        |
| F2  | Für "Session-Länge" existiert **keine Datenerfassung** — kein Login/Logout-Event, kein Client-Session-Timer wird persistiert. `useCasinoStore.analytics.totalSessionTime` ist ein reiner In-Memory-Zähler (Sekunden-Intervall), **nicht Teil von `partialize`**, geht bei jedem Reload verloren.                                                                                                                                                                                                                                                                                                                                                                                                                      | `useCasinoStore.ts:625-638` (Setter), `useCasinoStore.ts:945-965` (`partialize` exkludiert `analytics` nicht separat, aber der Zähler startet bei jedem Mount neu — kein Cross-Session-Wert) |
| F3  | **Dead-Table gefunden:** `game_sessions` (Migration `002_wallet.sql`, Spalten `started_at`/`ended_at`/`total_bet`/`total_won`/`hands_played` — exakt für Session-Tracking gedacht) wird **von keinem einzigen `src/`-Pfad gelesen oder beschrieben** (0 Treffer). Echtes Session-Tracking wäre also kein komplettes Neuland, sondern würde eine bereits vorhandene, seit Projektbeginn ungenutzte Tabelle aktivieren — aber jeder Bet-Flow aller 5 Spiele müsste dafür angefasst werden (Start/Ende-Schreibpfad), was den additiven Scope dieser Initiative sprengen würde.                                                                                                                                           | `grep -rn game_sessions src` → 0 Treffer; nur in Migrationen/Doku                                                                                                                            |
| F4  | **Amount-Semantik in `wallet_transactions` verifiziert (kritisch für den Profit-Verlauf-Chart):** DICE/ROULETTE/SLOTS (`type='bet_settled'`) schreiben `amount = payout − bet` (bereits netto, 1 Zeile). CRASH/BLACKJACK schreiben **zwei** Zeilen: `round_started` mit `amount = −bet` (Einsatz-Abbuchung) und `round_settled`/`round_action` mit `amount = payout` bzw. `payout − zusätzlicher Einsatz` (netto für den Settle-Schritt). Die **Summe aller Zeilen eines Nutzers in chronologischer Reihenfolge ergibt für alle 5 Spiele denselben korrekten kumulierten Nettoprofit** — kein spielspezifischer Sonderfall nötig, ein einziger `SUM(amount)`-Lauf über `/api/user/history` reicht für den Hero-Chart. | `007_server_authority.sql:120-176` (`start_game_round`, Zeile 163: `-p_amount`), `014_fix_user_stats.sql:112-199` (`settle_game_round`, Zeile 175: `p_payout`)                               |
| F5  | `PlayerProfileModal.tsx` zeigt aktuell **hartcodierte Mock-Werte** (`mockStats.favoriteGame = 'Crash'`, `totalWagered = '$124,500'` etc.) statt echter Daten. Dieses Modal liegt **außerhalb** des Scopes dieser Initiative (nicht in der Dateiliste aus `05_ZUKUNFTSPLANUNG.md` Abschnitt 3.7) — bewusst nicht angefasst, siehe Abschnitt 7.                                                                                                                                                                                                                                                                                                                                                                         | `PlayerProfileModal.tsx:28-34`                                                                                                                                                               |
| F6  | Kanonische Icon-/Farb-Zuordnung je Spiel existiert bereits konsistent in `games/page.tsx`: `crash→TrendingUp/primary`, `dice→RotateCcw/secondary`, `roulette→CircleDollarSign/accent`, `slots→Zap/primary`, `blackjack→Spade/secondary`. Wird für die Pro-Spiel-Charts übernommen statt neu erfunden (Konsistenz-Kriterium).                                                                                                                                                                                                                                                                                                                                                                                          | `games/page.tsx:39-110`                                                                                                                                                                      |
| F7  | `/history` nutzt bereits exakt die zwei Endpunkte, die auch diese Initiative braucht (`/api/user/history`, `/api/user/stats`) — beide liefern typisierte, auth-geschützte JSON-Antworten (401 bei fehlender Session, kein Redirect). Kein neuer API-Route-Code nötig, nur die bestehende `get_user_stats()`-RPC wird erweitert (siehe Abschnitt 3).                                                                                                                                                                                                                                                                                                                                                                   | `history/page.tsx:34-68`, `api/user/history/route.ts`, `api/user/stats/route.ts`                                                                                                             |
| F8  | Das ursprünglich skizzierte `PersonalStatsChart.tsx` (eine Datei) würde bei 4 unterschiedlichen Chart-Typen + Ableitungslogik gegen die Projektregel "Many small files, 200–400 Zeilen typisch" verstoßen. Wird in Abschnitt 3 in 5 fokussierte Dateien + 1 reine Ableitungs-Bibliothek aufgeteilt (analog zur Aufteilung `achievements-config.ts` vs. Store in der Vorgänger-Initiative 1.5).                                                                                                                                                                                                                                                                                                                        | `V:\.claude\rules\ecc\common\coding-style.md` „File Organization"                                                                                                                            |

---

## 3 — Architektur & Datenmodell

### 3.1 Datenherkunft je Chart (Grundsatzentscheidung, direkt aus Jans Antworten 1B/2B/3B)

| Chart                             | Quelle                                                                 | Neuer Backend-Code?                               |
| --------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------- |
| Profit-Verlauf (Hero)             | `/api/user/history` (bestehend, letzte 100 Zeilen)                     | Nein                                              |
| Lieblingsspiel + Pro-Spiel-Anteil | `/api/user/stats` → neues Feld `perGame` in `get_user_stats()`         | Ja — additive RPC-Erweiterung, keine neue Tabelle |
| Pro-Spiel-Profit-Breakdown        | dieselbe `perGame`-Antwort                                             | (mitgeliefert von obigem)                         |
| Session-Länge (Näherungswert)     | `/api/user/history` (dieselben 100 Zeilen, nach Kalendertag gruppiert) | Nein                                              |
| Lifetime-Kennzahlenkacheln        | `/api/user/stats` (bestehende Top-Level-Felder, unverändert)           | Nein                                              |

Bewusste Konsequenz: **ein** Request an `/api/user/history` und **ein** Request an `/api/user/stats` (identisches Muster wie `history/page.tsx:39-42`, `Promise.all`) versorgen die gesamte Seite — kein Wasserfall, kein dritter Endpoint.

### 3.2 Migration `018_user_stats_per_game.sql` (additiv, `CREATE OR REPLACE FUNCTION`)

Erweitert `get_user_stats()` um ein `perGame`-Array, indem die bestehende `UNION ALL`-Quelle (Migration 014, bereits korrekt verifiziert per F4) zusätzlich nach `game` gruppiert wird, statt nur in Skalare zu summieren:

```sql
CREATE OR REPLACE FUNCTION public.get_user_stats(p_user_id TEXT)
RETURNS JSONB
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, pg_temp
AS $$
DECLARE
  v_total_bets    BIGINT         := 0;
  v_total_wins    BIGINT         := 0;
  v_total_wagered NUMERIC(20, 2) := 0;
  v_total_payout  NUMERIC(20, 2) := 0;
  v_total_profit  NUMERIC(20, 2) := 0;
  v_achievements  JSONB          := '[]'::jsonb;
  v_per_game      JSONB          := '[]'::jsonb;
BEGIN
  -- combined/grouped are CTEs scoped to this single statement (a CTE cannot be
  -- referenced across separate SELECT INTO statements in PL/pgSQL — the first
  -- draft tried exactly that and was corrected before shipping, see Abschnitt 8.1),
  -- so both the lifetime scalars and the per-game jsonb_agg come from one query.
  WITH combined AS MATERIALIZED (
    SELECT
      lower(game)                                        AS game,
      1                                                   AS bets,
      (amount > 0)::int                                   AS win,
      COALESCE(NULLIF((metadata -> 'response' ->> 'betAmount')::numeric, 0),
               CASE WHEN amount < 0 THEN ABS(amount) ELSE 0 END)          AS wagered,
      COALESCE((metadata -> 'response' ->> 'payout')::numeric,
               CASE WHEN amount > 0 THEN amount ELSE 0 END)               AS payout,
      amount                                              AS profit
    FROM public.wallet_transactions
    WHERE user_id = p_user_id AND type = 'bet_settled'

    UNION ALL

    SELECT
      lower(game)                                         AS game,
      1                                                    AS bets,
      (payout > 0)::int                                    AS win,
      bet_amount                                            AS wagered,
      payout                                                AS payout,
      payout - bet_amount                                   AS profit
    FROM public.game_rounds
    WHERE user_id = p_user_id AND status = 'SETTLED'
  ),
  grouped AS (
    SELECT game, SUM(bets) AS bets, SUM(win) AS wins, SUM(wagered) AS wagered,
           SUM(payout) AS payout, SUM(profit) AS profit
    FROM combined
    WHERE game IS NOT NULL
    GROUP BY game
  )
  SELECT
    COALESCE((SELECT SUM(bets)    FROM combined), 0),
    COALESCE((SELECT SUM(win)     FROM combined), 0),
    COALESCE((SELECT SUM(wagered) FROM combined), 0),
    COALESCE((SELECT SUM(payout)  FROM combined), 0),
    COALESCE((SELECT SUM(profit)  FROM combined), 0),
    COALESCE((
      SELECT jsonb_agg(jsonb_build_object(
        'game', grouped.game, 'bets', grouped.bets, 'wins', grouped.wins,
        'wagered', grouped.wagered, 'payout', grouped.payout, 'profit', grouped.profit,
        'winRate', CASE WHEN grouped.bets > 0
                     THEN round((grouped.wins::numeric / grouped.bets::numeric) * 100, 1)
                     ELSE 0 END
      ) ORDER BY grouped.wagered DESC)
      FROM grouped
    ), '[]'::jsonb)
  INTO v_total_bets, v_total_wins, v_total_wagered, v_total_payout, v_total_profit, v_per_game;

  SELECT COALESCE(jsonb_agg(jsonb_build_object(
    'id', achievement_id, 'unlocked', unlocked, 'progress', progress
  )), '[]'::jsonb)
  INTO v_achievements
  FROM public.user_achievements
  WHERE user_id = p_user_id;

  RETURN jsonb_build_object(
    'totalBets', v_total_bets, 'totalWins', v_total_wins,
    'totalWagered', v_total_wagered, 'totalPayout', v_total_payout, 'totalProfit', v_total_profit,
    'winRate', CASE WHEN v_total_bets > 0 THEN round((v_total_wins::numeric / v_total_bets::numeric) * 100, 1) ELSE 0 END,
    'achievements', v_achievements,
    'perGame', v_per_game
  );
END;
$$;

REVOKE ALL ON FUNCTION public.get_user_stats(TEXT) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION public.get_user_stats(TEXT) TO service_role;
```

Identisches Muster zu Migration 014 (`CREATE OR REPLACE`, gleiche `REVOKE`/`GRANT`-Zeilen, gleicher `SECURITY DEFINER`/`search_path`-Header) — keine neue Tabelle, keine neue Policy, kein neuer Grant-Empfänger.

### 3.3 `src/lib/casino/stats-derivation.ts` (neu, isomorph, rein, testbar — analog `achievements-config.ts`)

```ts
export interface HistoryRow {
  id: string;
  game: string | null;
  type: string;
  amount: number;
  balance_after: number;
  created_at: string;
}
export interface ProfitPoint {
  time: string;
  cumulativeProfit: number;
}
export interface DailyActivity {
  date: string;
  spanMinutes: number;
  betCount: number;
}
export interface PerGameStat {
  game: string;
  bets: number;
  wins: number;
  wagered: number;
  payout: number;
  profit: number;
  winRate: number;
}

export function buildProfitSeries(rows: HistoryRow[]): ProfitPoint[] {
  /* chronologisch sortieren (rows kommen DESC), kumulieren */
}
export function buildDailyActivity(rows: HistoryRow[], maxSpanMinutes?: number): DailyActivity[] {
  /* nach lokalem Kalendertag gruppieren, Spanne = max−min created_at, gecappt */
}
export function getFavoriteGame(perGame: PerGameStat[]): PerGameStat | null {
  /* höchster wagered-Wert, perGame ist bereits server-sortiert */
}
```

- `buildProfitSeries`: reversiert die absteigend sortierten `history`-Zeilen, kumuliert `amount` (F4-Beweis trägt die Korrektheit).
- `buildDailyActivity`: gruppiert nach `created_at.toLocaleDateString()`, `spanMinutes = (max − min) / 60000`, gecappt auf `maxSpanMinutes = 240` (E5, Abschnitt 5) — ein einzelner isolierter Bet an einem Tag ergibt `spanMinutes = 0`, kein `NaN`.
- `getFavoriteGame`: nutzt die bereits server-seitig nach `wagered DESC` sortierte `perGame`-Liste (Migration, `ORDER BY wagered DESC` im `jsonb_agg`) — kein zweites Sortieren im Client nötig.

Alle drei Funktionen sind rein (kein `fetch`, kein Zustand-Zugriff) → 100 % unit-testbar ohne Mock, analog zum Testbarkeits-Argument der Vorgänger-Initiative.

### 3.4 Komponenten (`src/components/stats/`, ersetzt die 1-Datei-Skizze aus der Roadmap — Begründung F8)

| Datei                        | Inhalt                                                                                                                                                                                            |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `StatsSummaryTiles.tsx`      | 4 Lifetime-Kacheln (Wagered/Profit/WinRate/Bets) — visuell wie `HistoryStatsCard`, aber eigene Datei, da andere Beschriftung/Kontext (Seite ist eigenständig, kein Re-Export über Seitengrenzen). |
| `ProfitHistoryChart.tsx`     | Hero-Tile, Recharts `AreaChart`, Gradient-Fill analog `AdminOverviewClient.tsx` (`#D4AF37`/`#10b981`-Muster), Span 2 Grid-Spalten.                                                                |
| `FavoriteGameCard.tsx`       | Donut (Recharts `PieChart`) über `perGame`-Bet-Anteile, Icon/Farbe je Spiel aus F6, Badge „Favorit: {game}".                                                                                      |
| `PerGameProfitBreakdown.tsx` | Horizontaler Bar-Chart, Profit je Spiel, grün/rot wie `HistoryStatsCard`-Konvention.                                                                                                              |
| `SessionLengthChart.tsx`     | Bar-Chart über `buildDailyActivity()`, mit sichtbarem "≈ Näherungswert aus Bet-Zeitstempeln"-Badge (E-Kennzeichnung, kein falscher Genauigkeitsanspruch).                                         |

### 3.5 Seite (`src/app/stats/page.tsx` + `layout.tsx`)

- `layout.tsx`: `export const metadata` (DevOps-Slayer-Pflicht, identisches Muster zu `history/layout.tsx`).
- `page.tsx`: `'use client'`, `Promise.all([fetch('/api/user/history'), fetch('/api/user/stats')])` — 1:1 das Lade-Muster aus `history/page.tsx:34-68` inkl. 401-Behandlung, Bento-Grid-Layout (Abschnitt 4B-Entscheidung): Reihe 1 = 4 Kacheln, Reihe 2 = `ProfitHistoryChart` (2fr) + `FavoriteGameCard` (1fr), Reihe 3 = `PerGameProfitBreakdown` (1fr) + `SessionLengthChart` (2fr) — bewusst alternierender Spaltenanteil für „Grid-breaking"-Charakter statt gleichförmiger Kachelraster.

### 3.6 Sidebar (`MainLayout.tsx`)

Neuer Eintrag `{ icon: <BarChart3 size={20} />, label: 'Stats', path: '/stats' }` in `menuItems` (Zeile 384-399), Position nach „Vault", vor „Settings" — Settings bleibt bewusst letzter Eintrag (Funktions-Kategorie, kein Content-Bereich).

---

## 4 — Requirements & Abhängigkeiten

| Anforderung                              | Erfüllt durch                                                                                                                                                     | Abhängigkeit                          |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| Kein neuer Money-Pfad                    | Rein lesende Seite, keine Wallet-Mutation, keine neue Schreib-RPC                                                                                                 | —                                     |
| Muster-Konsistenz mit `/history`         | Identisches Fetch-/Auth-/Error-Muster (F7)                                                                                                                        | `api/user/{stats,history}/route.ts`   |
| Additiv, keine neue Tabelle              | Migration 018 ist `CREATE OR REPLACE FUNCTION`, keine `CREATE TABLE`                                                                                              | Migration 014 als Referenzmuster      |
| Icon-/Farb-Konsistenz je Spiel           | Übernahme der Zuordnung aus `games/page.tsx` (F6)                                                                                                                 | `games/page.tsx`                      |
| Fail-closed bei Supabase-Ausfall         | `WalletService.getUserStats()` liefert bereits Default-Objekt bei RPC-Fehler — wird um `perGame: []` ergänzt                                                      | `wallet.ts:288-312`                   |
| DevOps-Slayer Metadata-Pflicht           | `stats/layout.tsx` exportiert `metadata` (Client-`page.tsx` kann das nicht)                                                                                       | `AGENTS.md` DevOps-Slayer-Regel       |
| 80 %-Coverage-Ziel für neue Logik        | `stats-derivation.ts` ist rein und wird vollständig unit-getestet                                                                                                 | `vitest.config.ts`                    |
| Kein Bruch bestehender `/history`-Zahlen | `get_user_stats()` bleibt für alle bestehenden Top-Level-Felder byte-identisch — nur ein neues Feld `perGame` wird ergänzt, keine bestehende Berechnung verändert | `history/page.tsx` nutzt dieselbe RPC |

---

## 5 — Fehlerszenarien & Umgang (vor Implementierung durchdacht)

| ID  | Szenario                                                                                                                             | Erkannt durch                                                                                                      | Umgang                                                                                                                                                                                    |
| --- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1  | Nutzer nicht eingeloggt (401 auf beiden Endpunkten)                                                                                  | `res.status === 401`-Check, identisch zu `history/page.tsx:44`                                                     | Freundliche Meldung „Bitte einloggen um deine Stats zu sehen", keine leere/kaputte Chart-Fläche                                                                                           |
| E2  | Neuer Nutzer, 0 Bets                                                                                                                 | `rows.length === 0` bzw. `perGame.length === 0`                                                                    | Jede Chart-Komponente rendert einen expliziten Empty-State („Noch keine Bets — spiel dein erstes Spiel!") statt leerem/kaputtem Chart                                                     |
| E3  | Supabase-RPC-Fehler bei `get_user_stats`                                                                                             | Bestehender Try/Catch in `WalletService.getUserStats()`                                                            | Default-Fallback-Objekt wird um `perGame: []` ergänzt — ohne diese Ergänzung würde die Chart-Komponente `undefined.map()` werfen (verifizierter Fix, nicht angenommen)                    |
| E4  | `/api/user/history` liefert < 2 Zeilen                                                                                               | `rows.length < 2`                                                                                                  | Profit-Verlauf/Session-Länge zeigen „Noch nicht genug Daten für einen Verlauf" statt eines Ein-Punkt-Charts ohne Aussagekraft                                                             |
| E5  | Session-Länge-Näherungswert zählt Pausen zwischen Bets mit (bewusste Limitation aus Jans Entscheidung 3B)                            | Konzeptionell inhärent, kein Bug                                                                                   | Sichtbares "≈ Näherungswert"-Badge in `SessionLengthChart.tsx` + Cap bei 240 Minuten/Tag, damit ein einzelner über-Nacht-Tab keinen 14h-Balken erzeugt                                    |
| E6  | `wallet_transactions.game` kann `NULL` sein (Bonus-/Voucher-Zeilen, siehe Tabellenkommentar in `002_wallet.sql:11`)                  | Spalten-Kommentar `'purchase', 'bonus'` als mögliche Werte                                                         | Client-Ableitung (`buildDailyActivity`, Pro-Spiel-Zuordnung) filtert Zeilen mit `game === null` heraus — Bonus-Gutschriften sind keine Bets und verzerren sonst Session-/Spiel-Auswertung |
| E7  | Migrationsnummer-Kollision mit paralleler Session (bekanntes Projektrisiko, `01_WORLDMAP_STATUS.md` §8.2)                            | Verzeichnis unmittelbar vor Dateierstellung geprüft (`017` aktuell höchste, `018` frei, siehe Abschnitt 2-Vorlauf) | Datei ist `CREATE OR REPLACE`, damit bei Kollision umbenennbar und idempotent erneut ausführbar                                                                                           |
| E8  | Recharts `ResponsiveContainer` rendert 0 Höhe bei fehlendem explizitem Pixel-Wert am Elternelement (bekannter Recharts-Stolperstein) | Vorab bekannt aus `AdminOverviewClient.tsx`-Referenzmuster (`height={220}` explizit gesetzt)                       | Jede Chart-Komponente setzt `<ResponsiveContainer height={...}>` mit festem Pixelwert, nie prozentual ohne Elternhöhe                                                                     |
| E9  | Division durch 0 bei Winrate-/Prozent-Berechnung eines Spiels mit 0 Bets                                                             | `perGame`-Eintrag existiert serverseitig nur bei `bets > 0` (aus `GROUP BY`), Client rechnet trotzdem defensiv     | `winRate`-Feld kommt bereits vorberechnet aus der RPC (`CASE WHEN bets > 0 ...`), Client führt keine eigene Division ohne Guard durch                                                     |

---

## 6 — Selbstprüfung aus zwei Blickwinkeln (vor Execution durchgeführt)

Gewählte Perspektiven: **Design-Guardian** (laut `AGENTS.md`-Rollentabelle der für dieses Vorhaben zuständige Agent — Backlog-Scope „UI/UX-Section") und **Security-Auditor** (nicht formal Pflicht laut `05_ZUKUNFTSPLANUNG.md`-Spalte „Security-Reviewer: Nein", da kein Money-Pfad — aber bewusst zusätzlich angewendet, weil Abschnitt 3.2 eine `SECURITY DEFINER`-SQL-Funktion ändert).

### 6.1 Blickwinkel Design-Guardian

- ✅ Bento-Grid mit alternierenden Spaltenanteilen (2fr/1fr, 1fr/2fr) statt gleichförmigem Kachelraster erfüllt „Grid-breaking editorial... composition" (Design-Quality-Regel).
- ✅ Eigene visuelle Identität ggü. `/history` (Tabellen-Fokus) und `/vault` (Progression-Fokus) — diese Seite ist Chart-Fokus, keine dritte Tabellenseite.
- ✅ Icon-/Farb-Konsistenz mit `games/page.tsx` (F6) statt neu erfundener Palette pro Spiel.
- ✅ `backdrop-filter: blur()` + `hsla`-Transparenz auf allen Tiles (Pflicht laut Design-System-Regel in `CLAUDE.md`), identisch zum bestehenden `--stealth-surface`/`--stealth-border`-Token-Paar aus `/history`.
- ⚠️ **Korrektur nach erster Durchsicht:** Ursprünglicher Entwurf sah ein einzelnes `PersonalStatsChart.tsx` vor (Roadmap-Skizze). Nach Abgleich mit der Projektregel „200–400 Zeilen typisch, viele kleine Dateien" auf 5 fokussierte Komponenten + 1 Ableitungs-Modul aufgeteilt (F8) — Beispiel für den geforderten zweiten Prüfdurchgang.
- ✅ Motion-Konsistenz: `framer-motion`-Stagger beim Laden, analog `history/page.tsx`s `motion.div`-Pattern — keine neue, inkonsistente Animationsbibliothek.

### 6.2 Blickwinkel Security-Auditor (zusätzlich angewendet, siehe Begründung oben)

- ✅ `get_user_stats(p_user_id TEXT)` bleibt `SECURITY DEFINER` mit festem `search_path`, `REVOKE ALL ... GRANT ... TO service_role` unverändert — keine neue Rechte-Ausweitung, keine neue aufrufbare Rolle.
- ✅ Kein neuer dynamischer SQL-String (`EXECUTE`/String-Konkatenation) — die Erweiterung nutzt ausschließlich parametrisierte `jsonb_build_object`/`jsonb_agg`, kein Injection-Vektor.
- ✅ `p_user_id` bleibt der einzige externe Parameter, exakt wie im Ist-Zustand — die neue `perGame`-Aggregation filtert über dieselbe `WHERE user_id = p_user_id`-Bedingung wie die bestehenden Summen, kein Cross-User-Leck möglich.
- ✅ Route `/stats` erzwingt Auth serverseitig über dieselben, bereits gehärteten Endpunkte (`/api/user/history`, `/api/user/stats`) — kein neuer Auth-Code, kein neuer Angriffsvektor.
- ✅ Keine PII über die bestehende Datenmenge hinaus — `perGame` aggregiert ausschließlich bereits vom Nutzer selbst einsehbare Beträge (identisch zu `/history`), keine anderen Nutzer sichtbar.
- ⚠️ **Fund während der Prüfung (E6, Abschnitt 5):** `wallet_transactions.game` kann laut Tabellenkommentar `NULL` sein oder `'purchase'`/`'bonus'` enthalten. Ohne Filterung würden Bonus-Gutschriften (z. B. `JAN100`-Voucher, siehe `CLAUDE.md`) fälschlich als „Spiel" in der Pro-Tag-Aktivität und potenziell in Pro-Spiel-Charts auftauchen. Gegenmaßnahme in Abschnitt 3.3/5 (E6) ergänzt, bevor der Code geschrieben wurde.

**Ergebnis der Selbstprüfung:** 1 Architektur-Korrektur (Datei-Aufteilung) + 1 Daten-Filterungs-Fix (Bonus-Zeilen) wurden **vor** der Implementierung in den Plan aufgenommen, nicht nachträglich gepatcht.

---

## 7 — Bewusst nicht umgesetzt (Abgrenzung, kein vergessener Scope)

- **`PlayerProfileModal.tsx`-Mock-Daten (F5) werden nicht gefixt** — außerhalb der in `05_ZUKUNFTSPLANUNG.md` Abschnitt 3.7 definierten Dateiliste dieser Initiative. Eigenständiger, kleiner Folge-Task (siehe Abschnitt 8.4).
- **Kein echtes Session-Tracking über `game_sessions` (F3)** — würde Schreibpfade in allen 5 Spiele-Flows erfordern, das ist eine eigene, deutlich größere Initiative und stand nicht im Auftrag (Jans Entscheidung 3B war explizit der Näherungswert, nicht die echte Tabelle).
- **Kein Zeitraum-Filter (Tag/Woche/Monat)** — das wäre Option 1C aus der ursprünglichen Fragerunde, nicht gewählt. Der Hero-Chart bleibt auf die letzten 100 Bets begrenzt (identisch zum Fenster von `/history`).
- **Keine serverseitige Cache-Schicht für `perGame`** — `get_user_stats()` wird bereits ungecached bei jedem `/api/user/stats`-Aufruf live berechnet (Ist-Zustand, unverändert durch diese Initiative), kein neues Performance-Ziel in diesem Scope.

---

## 8 — Execution-Ergebnis

### 8.1 Umgesetzte Dateien

- ☑ `supabase/migrations/018_user_stats_per_game.sql` (neu) — erweitert `get_user_stats()` um `perGame`. Während der Implementierung korrigiert: ein `CTE`, das über zwei getrennte `SELECT INTO`-Statements referenziert wurde, ist in PL/pgSQL ungültig (CTE-Scope endet mit dem Statement) — auf eine einzige `WITH combined AS MATERIALIZED (...), grouped AS (...) SELECT (...) INTO 6 Variablen` umgestellt, kein Zwischenschritt über eine `TEMP TABLE` (wäre ein im Projekt bislang unbenutztes Muster gewesen).
- ☑ `src/lib/casino/stats-derivation.ts` (neu) — `buildProfitSeries()`, `buildDailyActivity()`, `getFavoriteGame()`.
- ☑ `src/lib/casino/__tests__/stats-derivation.test.ts` (neu) — 11 Tests.
- ☑ `src/lib/casino/wallet.ts` (erweitert) — `getUserStats()`-Fallback und Rückgabetyp um `perGame: []` ergänzt (E3).
- ☑ `src/app/stats/layout.tsx` (neu) — Metadata.
- ☑ `src/app/stats/page.tsx` (neu) — Fetch-Orchestrierung, Bento-Grid.
- ☑ `src/components/stats/gameMeta.ts` (neu) — geteilte Icon-/Farb-Zuordnung (F6).
- ☑ `src/components/stats/StatsSummaryTiles.tsx`, `ProfitHistoryChart.tsx`, `FavoriteGameCard.tsx`, `PerGameProfitBreakdown.tsx`, `SessionLengthChart.tsx` (neu).
- ☑ `src/components/layout/MainLayout.tsx` (erweitert) — Sidebar-Eintrag „Stats" zwischen „Vault" und „Settings", `BarChart3`-Import ergänzt.
- ☑ **Nicht im ursprünglichen Scope, während der Browser-Verifizierung als echter Bug gefunden und gefixt:** `src/proxy.ts` — `/stats(.*)` fehlte in `PUBLIC_ROUTES`. Ohne diesen Eintrag hätte die Middleware anonyme Besucher serverseitig auf `/sign-in` umgeleitet, bevor die client-seitige 401-Behandlung aus Abschnitt 5 (E1) je zum Zug gekommen wäre — exakt der Public-Route-Eintrag, den `/history` und `/vault` für denselben Zweck bereits haben (F7-Muster), war für `/stats` schlicht vergessen worden.

### 8.2 Verifizierung

- ☑ `npx tsc --noEmit` → 0 Fehler (nach Korrektur der Recharts-3.8.1-`Tooltip`-Formatter-Typen: explizite `number`/`string`-Parameter-Annotationen kollidierten mit `ValueType = string | number | Array<string|number>` — auf kontextuelle Typinferenz ohne eigene Annotation umgestellt, mit `Number(value ?? 0)`/`String(label ?? '')` zur Laufzeit abgesichert)
- ☑ `npm run lint` → 0 Fehler/Warnungen in allen 12 berührten/neuen Dateien dieser Initiative (gezielt per Pfad-Filter aus dem Gesamt-Lint-Output geprüft); die im Gesamtlauf sichtbaren 31 Fehler/übrigen Warnungen sind vorbestehend in unberührten Dateien (u. a. eine minifizierte Bundle-Datei), nicht Teil dieses Scopes
- ☑ `npx vitest run` (Gesamtsuite) → **276/276 Tests grün** (27 Dateien, war 265/26) — 1 neuer Testlauf mit `process.env.TZ = 'UTC'` in `beforeAll()` gefixt: `buildDailyActivity()` gruppiert absichtlich nach lokalem Kalendertag (richtiges Produktverhalten, siehe Kommentar im Code), ein Test mit Zeitstempeln nahe der Tagesgrenze war dadurch vom Zeitzonen-Offset der Ausführungsmaschine abhängig — auf UTC gepinnt statt die Testdaten zu verbiegen
- ☑ `npm run build` → grün, `/stats` erscheint in der Routen-Tabelle
- ☑ Live-Verifizierung im Dev-Server (Port 3015, bereits laufender Fremdprozess wiederverwendet statt neu gestartet): `/stats` lädt für den `dev_user_fallback`-Nutzer, `GET /api/user/history` und `GET /api/user/stats` beide 200 OK. `get_user_stats`-RPC liefert im aktuellen (laut `CLAUDE.md` DNS-gestörten) Remote-Zustand die Fail-Closed-Defaults inkl. `perGame: []` — **live bestätigt per direktem Fetch**, kein Crash, `FavoriteGameCard`/`PerGameProfitBreakdown` zeigen korrekt den Empty-State (E2/E3 beide in freier Wildbahn ausgelöst, nicht nur im Unit-Test). `/api/user/history` lieferte reale Zeilen — `ProfitHistoryChart` und `SessionLengthChart` rendern mit echten Werten. Sidebar-Eintrag „Stats" per `aria-label`-Check bestätigt (7 statt 6 Buttons, korrekte Position). Konsole zeigt einen vorbestehenden Fehler (`[STORE] Server wallet initialization failed closed`) — per Gegenprobe auf `/history` als identisch und nicht durch diese Initiative verursacht verifiziert.

### 8.3 Selbstprüfung der Execution (zweiter Durchgang, nach Code-Fertigstellung)

- ✅ `get_user_stats()`-Erweiterung ändert keinen bestehenden Top-Level-Feldwert — nur `perGame` ist neu, `git diff` gegen Migration 014 zeigt für alle bestehenden Felder identische Berechnungslogik.
- ✅ Kein Test wurde entfernt oder geschwächt, um ihn grün zu bekommen — der einzige Eingriff in einen Testlauf (`TZ = 'UTC'`) macht einen Test deterministisch, ändert keine Assertion.
- ✅ **Fund beim zweiten Durchgang:** `/stats` fehlte in `proxy.ts`s `PUBLIC_ROUTES` (Abschnitt 8.1) — wäre ohne Browser-Verifizierung erst durch Jan beim ersten echten Test bemerkt worden. Direkt gefixt, nicht nur dokumentiert.
- ✅ Migrationsverzeichnis erneut geprüft: `018` war zum Zeitpunkt der Dateierstellung frei (höchste vorhandene `017`), keine Kollision.
- ✅ Kein Money-Pfad berührt — `git diff --stat` zeigt keine Änderung an `casino-core.ts`, `bet/route.ts`, `blackjack/route.ts` oder einer `settle_*`/`start_*`-RPC.
- ⚠️ **Nicht live-verifizierbar in dieser Session:** Migration 018 selbst wurde nicht gegen die Remote-Datenbank ausgeführt (konsistent mit dem dokumentierten Zustand aller Migrationen 007–017 in `CLAUDE.md` — DDL-fähiger Zugang liegt bei Jan). Die Fail-Closed-Fallback-Pfade wurden dafür live bestätigt (Abschnitt 8.2), sodass die Seite schon vor dem eigentlichen Rollout funktionsfähig ist und danach ohne Code-Änderung „aufwacht", sobald `perGame` echte Daten liefert.

### 8.4 Nicht ausgeführt / offen für Jan

- **Visuelle Prüfung des Bento-Grid-Layouts durch Jan** (Claude bewertet laut Projektregel nie selbst visuell) — funktional/strukturell im Dev-Server bereits verifiziert (Abschnitt 8.2).
- `PlayerProfileModal.tsx`-Mock-Daten (F5) — eigenständiger, kleiner Folge-Task, nicht Teil dieser Initiative.
- Vorbestehender Konsolenfehler „Server wallet initialization failed closed" (`/api/community`-Fetch in `useCasinoStore.initialize()`) — app-weit, nicht durch diese Initiative verursacht, separat zu entscheiden ob/wann untersucht.
- **`get_or_create_user_seed` (Seed-Siegel-Verfahren, Migration 019) schlägt fehl** (`function gen_random_bytes(integer) does not exist` — `pgcrypto`-Extension fehlt) — während der Fehlersuche zu 1.7 entdeckt, aber Initiative 1.2, nicht 1.7. Nicht selbst behoben, da fremder Scope.

### 8.5 Nachtrag 2026-08-10: Migration 018 live ausgerollt — echter Bug gefunden & behoben (Migration 020)

Jan hat Migration 018 ausgerollt und per direktem SQL-Aufruf (`SELECT public.get_user_stats('dev_user_fallback')`) verifiziert: **RPC-Logik korrekt** (perGame/Aggregate rechnerisch exakt, siehe Chat-Verlauf). Trotzdem lieferte `/api/user/stats` über die App weiterhin nur Fail-Closed-Defaults.

- **Root Cause (per neu ergänztem Error-Logging in `WalletService.getUserStats()` gefunden — vorher loggte dieser Fehlerpfad nichts, Altbug, ebenfalls gefixt):** PostgREST-Fehler `PGRST203` — „Could not choose the best candidate function between: public.get_user_stats(p_user_id => text), public.get_user_stats(p_user_id => uuid)". Eine **verwaiste `get_user_stats(uuid)`-Überladung** existiert live in der DB **ohne zugehörige Migrationsdatei in diesem Repo** (manuelles Artefakt, nie eingefangen). Da `users.id TEXT` ist (siehe `CLAUDE.md`), ist ausschließlich die `TEXT`-Variante korrekt — die `uuid`-Variante ist totes Gepäck, das jeden App-seitigen Aufruf mit String-Argument mehrdeutig macht.
- **Fix:** `supabase/migrations/020_drop_orphan_get_user_stats_uuid_overload.sql` (neu) — `DROP FUNCTION IF EXISTS public.get_user_stats(uuid);`, rührt die `TEXT`-Funktion nicht an.
- **Nebenbefund, separat (nicht Teil von 1.7):** `get_or_create_user_seed` schlägt aus einem völlig anderen, unabhängigen Grund fehl (`pgcrypto` fehlt) — siehe Abschnitt 8.4, gehört zu Initiative 1.2.
- **Verifizierung:** `npx tsc --noEmit`/`npx eslint` nach den `wallet.ts`-Logging-Ergänzungen weiterhin 0 Fehler. Migration 020 von Jan im Supabase SQL Editor ausgeführt — **live bestätigt**: `GET /api/user/stats` liefert jetzt exakt dieselben Werte wie Jans direkte SQL-Abfrage (`totalBets: 115`, `perGame` mit dice/crash/slots), `/stats` rendert alle 5 Kacheln mit echten Daten statt Empty-States (Lieblingsspiel „Dice" 96 Bets/$930, Profit-je-Spiel-Balken für alle 3 Spiele, Lifetime-Kacheln korrekt). Initiative 1.7 damit vollständig live funktionsfähig, keine offenen Blocker mehr.
