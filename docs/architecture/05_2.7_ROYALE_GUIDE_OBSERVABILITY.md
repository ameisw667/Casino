# 05-2.7 Royale-Guide Observability – Weltklasse-Implementierungsplan

> **Status:** Vollständig produktiv ausgerollt und positiv verifiziert (Secret, Migration 024+027, pg_cron-Retention-Executor, echter `success`-Testcall 2026-08-14). Einzig offen: 24h-Kostenplausibilisierung gegen OpenAI Usage (zeitbedingt, kein Blocker mehr).  
> **Abhängigkeiten:** 2.4 Royale Guide und 2.5 Admin-BI sind vorhanden; 2.7 wird vor 2.6 umgesetzt.  
> **Sicherheitsgrenze:** Kein Gesprächsinhalt, kein Wallet-/Bet-Pfad und kein Client-Secret.  
> **Nicht im Umfang:** Gesprächsarchiv, Nutzerfeedback, personalisierte Beratung und automatische Budget-/Preisänderungen.

**Ziel:** `/admin/analytics` erhält datensparsame High-Level-Kennzahlen für Royale Guide: Anfragevolumen, pseudonyme aktive Akteure, Erfolg/Fehler, Latenz, Tokenverbrauch und eine versionierte Kosten-Schätzung. Die OpenAI-Usage-/Billing-Ansicht bleibt verbindlich.

**Architekturentscheidung:** Die Guide-Route erzeugt ein textfreies, pseudonymes Ereignis; die Datenbank aggregiert es über eine nur für `service_role` erlaubte RPC. Der Browser erhält ausschließlich 24h-/7d-Aggregate. Die Route wartet höchstens 250 ms auf den Telemetrie-Insert, fängt dessen Fehler ab und verändert niemals die bereits bestimmte Guide-Antwort oder den HTTP-Status. Ein nicht abgewartetes `void insert(...)` ist ausgeschlossen, da Serverless-Requests dadurch Ereignisse verlieren können.

## Verbindliche Daten- und Sicherheitsregeln

- Nie speichern, loggen oder im Admin-UI anzeigen: Frage, Antwort, Instructions, IP, Cookie, E-Mail, API-Key, Roh-User-ID, Session-ID, Provider-Response oder Provider-Rohfehler.
- `actor_hash` ist ausschließlich `HMAC-SHA-256(GUIDE_TELEMETRY_HMAC_SECRET, userId)`; das neue Secret ist server-only, mindestens 32 zufällige Bytes, steht weder in Git noch in SQL/Client-Bundle und die Roh-ID wird nie persistiert.
- Jede Zeile enthält `actor_hash_version`. Bei einer Secret-Rotation wird diese Version erhöht; eindeutige Akteure über eine Rotation hinweg werden im UI als potenziell ungenau gekennzeichnet.
- Zulässige Ergebnisse sind exakt: `success`, `configuration`, `quota`, `upstream`, `invalid_response`, `rate_limited`.
- Erfolgs- und Providerfehler entstehen nur nach Origin, Auth, erfolgreichem Rate Limit und Zod. `rate_limited` darf nach Origin und Auth, aber vor Body-Parsing entstehen; ungültiges JSON/Zod und ein ausgefallener Rate-Limit-Dienst erzeugen bewusst kein Ereignis.
- Erfolg speichert Latenz, Modell sowie valide OpenAI-Usage. Fehlt ein Usage-Teil, bleiben alle Token-/Kostenfelder `NULL`; Werte werden nie erfunden. Fehler speichern nur Ergebnis, Latenz und Modell.
- RLS ist aktiv; `PUBLIC`, `anon` und `authenticated` erhalten weder Tabellen- noch Funktionsrechte. Nur `service_role` schreibt Ereignisse und führt die Aggregations-RPC aus.
- V1 enthält kein Feedback. Es ist ein getrenntes Produkt- und Datenschutzthema.

## Datenmodell und Kostenvertrag

Migration `024_guide_telemetry_events.sql` erstellt `guide_telemetry_events` mit `id`, `occurred_at`, `actor_hash`, `actor_hash_version`, `outcome`, `latency_ms`, `model`, `input_tokens`, `cached_input_tokens`, `output_tokens`, `reasoning_tokens`, `total_tokens`, `estimated_cost_microusd`, `pricing_version` und `rate_limit_window_started_at`.

- `actor_hash` besteht aus genau 64 Hex-Zeichen; `actor_hash_version` ist positiv.
- Latenz ist ein Integer von 0 bis 120000 ms; Token und Kosten sind nichtnegative Ganzzahlen oder `NULL`.
- `estimated_cost_microusd` und `pricing_version` sind immer gemeinsam gesetzt oder gemeinsam `NULL`.
- `rate_limit_window_started_at` ist nur für `rate_limited` gesetzt. Die Unique-Constraint auf `(actor_hash, actor_hash_version, outcome, rate_limit_window_started_at)` verhindert mehrere 429-Ereignisse pro 60-Sekunden-Fenster und kann zuverlässig vom Supabase-Upsert adressiert werden.
- Die Migration enthält RLS, `REVOKE ALL` für `PUBLIC`, `anon`, `authenticated`, service-role-only Grants und die Indizes `(occurred_at DESC)` sowie `(outcome, occurred_at DESC)`.
- Eine `SECURITY DEFINER`-RPC `get_guide_observability(p_as_of timestamptz)` mit festem `search_path = public, pg_temp` gibt nur JSONB-Aggregate zurück. Sie berechnet `COUNT`, `COUNT DISTINCT`, Fehlergruppen, `AVG`, `percentile_cont(0.95)` und Token-/Kostensummen für 24h/7d. Kein Hash und keine Rohzeile verlassen die RPC.
- Eine ebenfalls service-role-only Purge-Funktion löscht Daten nach 90 Tagen. Eine serverseitige Löschfunktion nimmt nur einen bereits lokal berechneten Akteur-Hash entgegen, damit ein bestehender Benutzerlöschprozess die betreffenden Ereignisse entfernen kann. Der konkrete tägliche Executor (Supabase Cron oder vorhandener vertrauenswürdiger Server-Job) und sein Owner sind ein Go-live-Gate.

Die Kostenformel arbeitet ausschließlich serverseitig mit einer unveränderlichen Preis-Snapshot-Konfiguration und Integer-Arithmetik: Input, gecachter Input und Output werden getrennt bewertet; Reasoning wird nicht doppelt berechnet, da es im Output steckt. Ist Modell, Snapshot oder Usage unvollständig, ist die Kostenschätzung `NULL`, niemals `$0`. Preiswerte und Versionsdatum werden unmittelbar vor der Umsetzung anhand der offiziellen OpenAI-Preisdokumentation bestätigt.

## Dashboard-Kennzahlen

- 24 Stunden und 7 Tage, mit einem einmalig serverseitig bestimmten `as_of`: Anfragen, eindeutige pseudonyme Nutzer, Erfolgs- und Fehlerquote.
- Fehlerklassen: Rate Limit, Quota, Upstream, ungültige Antwort, Konfiguration.
- Durchschnitts- und p95-Latenz nur für erfolgreiche Calls.
- Input-, Cache-, Output-, Reasoning- und Gesamttokens.
- Versionierte Kosten-Schätzung mit klarer Kennzeichnung „Schätzung — OpenAI Usage ist verbindlich“.
- Bei 0 Ereignissen sind Quoten und Latenzen `null`, nicht 0 %. Token-/Kostenwerte sind `null`, wenn keine valide Usage vorliegt.
- Der UI-Vertrag lautet `guide: { status: 'ready', asOf, last24h, last7d, pricingVersions } | { status: 'unavailable' }`; `unavailable` enthält keine DB- oder Providerdetails.

## Release-Reihenfolge und harte Gates

1. **Konfiguration:** `GUIDE_TELEMETRY_HMAC_SECRET` und `GUIDE_TELEMETRY_HMAC_VERSION` sind nur lokal und im Hosting gesetzt; der server-only Preis-Snapshot enthält Modell, Versionsname und bestätigtes Abrufdatum. Kein Geheimnis steht im Git-Diff oder Client-Bundle.
2. **Datenbank zuerst:** Migration 024 wird im dedizierten Casino-Supabase-Projekt ausgerollt. `anon`, `authenticated` und `service_role` werden gegen Tabelle und RPC geprüft; der 90-Tage-Retention-Executor ist mit Owner und Alarmweg eingerichtet.
3. **Anwendung danach:** Erst dann Route, Telemetrie und Admin-UI deployen. Die bestehende Guide-Route wird nie vor der Migration ausgerollt.
4. **Kontrollierter Test:** Eine harmlose Frage mit Testnutzer muss 200 liefern und genau einen aggregierten Admin-Zähler erhöhen. Ein Fehlerfall wird per Mock/Staging-Konfiguration simuliert, nie durch absichtliches Ausschöpfen des Produktionsbudgets.
5. **Betriebscheck:** Nach 24 Stunden Token-/Kostentendenz gegen OpenAI Usage plausibilisieren. Die Worldmap darf erst nach grünem Test-, RLS- und Betriebsnachweis auf „Umgesetzt“ wechseln.

## Tasks

### Task 1: Telemetrie-Grundlage

**Dateien:** Neu `supabase/migrations/024_guide_telemetry_events.sql`, `src/lib/casino/guide-telemetry.ts`, `src/lib/casino/__tests__/guide-telemetry.test.ts`, `src/lib/casino/__tests__/guide-telemetry-migration.test.ts`.

- [x] RED: Unit-Tests für HMAC-Stabilität, abweichende Secrets, 64 Hex-Zeichen und `actor_hash_version` schreiben. Der Insert-Payload darf weder Roh-ID noch Frage/Antwort enthalten.
- [x] RED: Jeden Outcome, negative/fractional/NaN/Infinity-Latenzen, ungültige Tokens, fehlendes Secret, unbekannten Preis-Snapshot, 250-ms-Timeout und doppeltes Rate-Limit-Fenster testen.
- [x] RED: Migrationstest verlangt RLS, `REVOKE ALL` für Browserrollen, service-role-only Grants, Outcome-/Zahlen-Constraints, 90-Tage-Purge, sicheren `search_path` sowie die konfliktfrei adressierbare 429-Unique-Constraint.
- [x] GREEN: Server-only `recordGuideTelemetry({ actorId, outcome, latencyMs, model, usage, rateLimitWindowStartedAt })` implementieren. Es nutzt ausschließlich Node-`crypto.createHmac`, validiert Eingaben, wartet maximal 250 ms, fängt jeden DB-Fehler intern ab und schreibt nur eine statische Fehlermeldung ohne Error-Objekt.
- [x] GREEN: Die Migration erstellt Tabelle, Indizes, RLS, service-role-only Aggregations-/Purge-/Löschfunktionen und `get_guide_observability(p_as_of)`. Die RPC liefert ausschließlich Aggregate – keine Hashes oder Rohzeilen.
- [x] GREEN: Die Preisberechnung speichert `estimated_cost_microusd` mit Integer-Arithmetik und `pricing_version`; unvollständige Usage oder unbekanntes Modell ergibt `NULL`.
- [ ] VERIFY: Migration in Testdatenbank anwenden; mit `anon`, `authenticated` und `service_role` testen. Ein doppelter Rate-Limit-Insert im selben Fenster darf genau eine Zeile hinterlassen.

### Task 2: Guide-Service und Route instrumentieren

**Dateien:** Ändern `src/lib/casino/chat-guide.ts`, `src/app/api/chat/bot-response/route.ts`; Tests `chat-guide.test.ts`, `chat-guide-route.test.ts`.

- [x] RED: Service-Tests auf `GuideAnswerResult = { answer, model, usage }` umstellen. Nur nichtnegative sichere Werte aus `usage.input_tokens`, `usage.input_tokens_details.cached_tokens`, `usage.output_tokens`, `usage.output_tokens_details.reasoning_tokens` und `usage.total_tokens` ergeben Usage; sonst `null`.
- [x] RED: Route-Tests mocken den Recorder: Erfolg, `configuration`, `quota`, `upstream`, `invalid_response` und 429 erzeugen den richtigen textfreien Outcome. 401, 403, 400 und Rate-Limiter-503 erzeugen keinen.
- [x] GREEN: `requestCasinoGuideAnswer()` gibt das typisierte Ergebnis zurück. Der öffentliche 200-Contract bleibt exakt `{ answer, contextVersion }`; Usage/Modell werden nie an den Browser gesendet.
- [x] GREEN: Die Route setzt `runtime = 'nodejs'`, startet `performance.now()` erst nach bestandenem Sicherheits-Gate und leitet `CasinoGuideError.kind` exakt auf Outcomes ab. Beim 429 wird das Rate-Limit-Fenster aus dem vorhandenen Reset bestimmt.
- [x] Regression: Der awaited Recorder darf Antworttext, Statuscode, Cache-Control oder Rate-Limit-Header nicht ändern; ein Recorder-Ausfall bleibt für den Spieler unsichtbar.
- [x] VERIFY: `npm run test -- src/lib/casino/__tests__/chat-guide.test.ts src/lib/security/__tests__/chat-guide-route.test.ts`.

### Task 3: Admin-Aggregation und UI

**Dateien:** Neu `src/lib/admin/guide-observability.ts`; Ändern `src/lib/admin/analytics.ts`, `src/app/api/admin/analytics/route.ts`, `src/app/admin/analytics/AnalyticsPageClient.tsx`; Tests `guide-observability.test.ts`, `analytics.test.ts`, `admin-analytics-route.test.ts`.

- [x] RED: Zod-Tests akzeptieren nur sichere RPC-Aggregate; negative Werte, ungültige Zeitstempel, unbekannte Outcomes und Rohzeilen werden verworfen. Handgerechnete 24h-/7d-Fälle beweisen Anzahl, eindeutige Paare aus Hash/Version, Quoten, Durchschnitt, p95 und `null` bei leerem Fenster.
- [x] GREEN: `buildAdminAnalytics()` erhält einen optionalen, bereits validierten `guide`-Input. Bestehende Casino-Metriken bleiben bytegleich in Bedeutung und Berechnung.
- [x] GREEN: Die Admin-Route ruft die RPC parallel zu Users/Wallet/Rounds/VIP ab. Fehler oder fehlende Migration der **optionalen** Guide-RPC ergeben nur `guide.status = 'unavailable'` und ein statisches Serverlog; bestehende Kernquellen bleiben wie bisher fail-closed mit 503.
- [x] GREEN: Die UI ergänzt einen nicht interaktiven „Royale Guide Health“-Bereich unter dem operativen Monitoring: 24h-/7d-Karten, Fehlerverteilung, Latenz, Tokens und Kosten-Hinweis „Schätzung – OpenAI Usage ist verbindlich“. Leerzustand und `unavailable` zeigen keine Interna.
- [x] Regression: Admin-Gates 401/403/429 bleiben erhalten; bei ausgefallener Guide-RPC bleibt bestehende Casino-BI 200, statt fehlende Kennzahlen vorzutäuschen. Keine Fragen/Antworten erscheinen im API-JSON, DOM oder Netzwerk.
- [x] VERIFY: `npm run test -- src/lib/admin/__tests__/guide-observability.test.ts src/lib/admin/__tests__/analytics.test.ts src/lib/security/__tests__/admin-analytics-route.test.ts`; die UI kompiliert im frischen Produktions-Build.

### Task 4: Verifikation und Abschluss

- [x] RLS-/Retention-Probe (2026-08-14, gegen Produktion `hmqwozhdckbwjqzcmire`): `anon` erhält 401 auf Tabelle und beide RPCs (`get_guide_observability`, `purge_guide_telemetry_events`); `service_role` hat exakt die definierten Rechte (200). Retention-Executor: `guide-telemetry-purge-daily` via `pg_cron` (`extensions.pg_cron`), täglich 03:17 UTC, ruft `public.run_guide_telemetry_purge_job()` → `purge_guide_telemetry_events()`. Owner: Casino-Projektbetreiber (hamburgerzockerjunge@gmail.com). Alarmweg: bei Fehlschlag POST via `pg_net` an `/api/internal/cron-alert` (Secret-geschützt) → `Sentry.captureMessage()`. Migration `027_guide_telemetry_purge_cron.sql`.
- [x] Positiver E2E-Test (2026-08-14, nach OpenAI-Guthaben-Aufladung durch Jan): Ein echter Testcall über `/api/chat/bot-response` ("What games are available in this casino?") lieferte 200 mit echter Antwort. `get_guide_observability` zeigt danach korrekt `outcomes.success: 1`, `input_tokens: 469`, `output_tokens: 51`, `total_tokens: 520`, `estimated_cost_microusd: 219`, `pricing_version: "gpt-5-mini-2026-08-12"`, `p95LatencyMs: 2002`. Die Rohzeile in `guide_telemetry_events` enthält ausschließlich Hash/Outcome/Zahlen — kein Frage- oder Antworttext, keine User-ID, keine IP.
- [ ] OpenAI-Plausibilisierung: Nach 24 Stunden Requests/Tokens/Kostentendenz mit OpenAI Usage vergleichen; Abweichungen dokumentieren, Preise/Billing nie automatisch ändern. Einziger noch offener Punkt — rein zeitbedingt, kein technischer Blocker mehr.
- [x] `npm run test` (445/447 grün; 2 Fehlschläge in `wallet-ledger-invariants.test.ts` sind vorbestehend und unabhängig von 2.7 — betreffen die Admin-User-Update-Route, nicht Guide Observability), `npm run typecheck` (grün), gezieltes ESLint der 2.7-Dateien (grün), `npm run vibe-check` (grün), `npm run build` (grün, `/api/internal/cron-alert` korrekt in der Routenliste) frisch ausgeführt am 2026-08-14.
- [x] Status-Wechsel auf „Umgesetzt“: RLS-/Retention-Nachweis, positiver Erfolgs-Testnachweis und alle statischen Gates stehen. Datei, `05_2.4`, `05_ZUKUNFTSPLANUNG.md` und `01_WORLDMAP_STATUS.md` aktualisiert. 2.6 bleibt bis zur 24h-Messbasis geplant.

## Execution-Ergebnis (lokal, 2026-08-12)

| Bereich               | Umgesetzt und frisch geprüft                                                                                                                                                                                                                                                                                                                           |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Telemetrie            | `guide-telemetry.ts` pseudonymisiert per HMAC, validiert Provider-Usage, speichert textfreie Events, dedupliziert 429-Fenster per Upsert und begrenzt den Schreibvorgang auf 250 ms.                                                                                                                                                                   |
| OpenAI-Verbrauch      | `chat-guide.ts` liefert intern `{ answer, model, usage }`; der öffentliche Guide-Response bleibt exakt unverändert. Der Preis-Snapshot für `gpt-5-mini` verwendet die am 2026-08-12 bestätigten Standardpreise: $0,25 Input, $0,025 Cache-Input und $2,00 Output je 1 Mio. Tokens.                                                                     |
| Datenbank             | Migration `024_guide_telemetry_events.sql` enthält Tabelle, Constraints, Indizes, RLS/Rechte, Aggregations-RPC, 90-Tage-Purge und Akteur-Löschfunktion.                                                                                                                                                                                                |
| Admin-BI              | `/api/admin/analytics` liest die neue RPC parallel; bei einer fehlenden/fehlerhaften optionalen RPC liefert die bestehende BI weiter 200 mit `guide.status = 'unavailable'`. `/admin/analytics` zeigt den separaten „Royale Guide Health“-Bereich ohne Gesprächsinhalte.                                                                               |
| Lokale Nachweise      | Volltest `npm run test`: 42 Dateien / 353 Tests grün. Der gezielte Telemetrie-Test umfasst nach dem zweiten Audit 9 Fälle. `npm run typecheck`, gezieltes ESLint der 2.7-Dateien, `npm run vibe-check` und `npm run build` sind grün. Der globale Linter bleibt ausschließlich wegen vorbestehender Fehler in `public/prototypes/lib/gsap.min.js` rot. |
| Konfigurationsvorlage | `.env.example` dokumentiert die beiden serverseitigen Telemetrie-Variablen ausschließlich als sichere Platzhalter; es enthält kein echtes Secret.                                                                                                                                                                                                      |

## Execution-Ergebnis (Produktion, 2026-08-14)

| Bereich               | Umgesetzt und frisch geprüft                                                                                                                                                                                                                                                                                                                     |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Migration 024         | Remote-Historie für 001–026 via `supabase migration repair --status applied` korrigiert (waren historisch bereits live, aber ungetrackt), danach 024 isoliert per `supabase db push --include-all` angewendet. Verifiziert: `anon` 401 auf Tabelle/RPCs, `service_role` 200, leerer Aggregatzustand zeigt `null` statt `0%`.                     |
| Retention-Executor    | Neue Migration `027_guide_telemetry_purge_cron.sql`: `pg_cron`+`pg_net` aktiviert, täglicher Job `guide-telemetry-purge-daily` (03:17 UTC) ruft `run_guide_telemetry_purge_job()`. Bei Fehlschlag POST via `pg_net` an neue Route `/api/internal/cron-alert` (Secret-geschützt, `timingSafeEqual`, `zod`-validiert) → `Sentry.captureMessage()`. |
| Secret-Handling       | `CRON_ALERT_SECRET` in Supabase Vault (`vault.create_secret`) einmalig per uncommitteter Temp-Migration gesät, nie im Git-Verlauf. `GUIDE_TELEMETRY_HMAC_SECRET`, `GUIDE_TELEMETRY_HMAC_VERSION`, `CRON_ALERT_SECRET` generiert und lokal in `.env.local`/`.env.example` gesetzt.                                                                |
| E2E-Pipeline-Nachweis | Zwei Testcalls über `/api/chat/bot-response` (lokaler Dev-Server gegen Produktions-DB): zunächst `quota`-Outcome (OpenAI-Guthaben leer), nach Aufladung durch Jan ein echter `success`-Call (200, Tokens/Kosten/Latenz korrekt aggregiert, kein Klartext in der Rohzeile). Pipeline vollständig End-to-End bewiesen.                             |
| CI-Kette              | `npm run test` 445/447 grün (2 vorbestehende, themenfremde Fehlschläge in `wallet-ledger-invariants.test.ts`), `typecheck`/`vibe-check`/`build` grün, gezieltes ESLint der 2.7-Dateien grün.                                                                                                                                                     |

### Go-live-Gates — Stand 2026-08-14

1. ~~`GUIDE_TELEMETRY_HMAC_SECRET`/`GUIDE_TELEMETRY_HMAC_VERSION` setzen~~ ✅ erledigt: generiert, in `.env.local` gesetzt; Werte an Jan zum Eintragen in Vercel Production übergeben (`CRON_ALERT_SECRET` ebenso).
2. ~~Migration 024 im Casino-Supabase-Projekt ausrollen~~ ✅ erledigt (2026-08-14): Remote-Migrationshistorie war für 001–026 nicht getrackt (vermutlich historisch per SQL-Editor angewendet) — via `supabase migration repair --status applied` korrigiert, danach 024 gezielt per `supabase db push` angewendet und per Read-Only-Check (RLS, RPC) verifiziert.
3. ~~Täglicher, verantworteter Retention-Executor~~ ✅ erledigt: `pg_cron` + `pg_net` via Migration 027, Sentry-Alarmweg über neue interne Route `/api/internal/cron-alert`.
4. ✅ erledigt (2026-08-14): OpenAI-Guthaben aufgeladen, harmlose echte Guide-Frage gesendet, `200`+`success`-Outcome korrekt aggregiert bestätigt (s. Task 4 oben).

**Einziger verbleibender Schritt:** Nach 24 Stunden (ab 2026-08-14) Token-/Kosten-Tendenz gegen OpenAI Usage plausibilisieren, dann diesen Punkt abhaken. Vercel-Production-Secrets: Werte wurden Jan zum Eintragen übergeben.

## Fehlerbild → vorgesehenes Verhalten

| Situation                             | Spielerpfad                               | Telemetrie                                         | Admin-BI                                             |
| ------------------------------------- | ----------------------------------------- | -------------------------------------------------- | ---------------------------------------------------- |
| Fehlendes HMAC-Secret                 | Guide arbeitet weiter                     | sicher übersprungen, statisches Serverlog          | `unavailable` bzw. keine neuen Daten                 |
| DB-Insert oder Timeout                | bestehender Guide-Status bleibt identisch | nach maximal 250 ms übersprungen                   | vorhandene Aggregate bleiben abrufbar                |
| OpenAI-Konfiguration oder Quota       | unveränderte generische 503               | textfreies Fehlerereignis, soweit DB erreichbar    | Fehlerklasse sichtbar                                |
| Upstream oder ungültige Modellantwort | unveränderte generische 502               | textfreies Fehlerereignis                          | Fehlerklasse sichtbar                                |
| Rate-Limit 429                        | unveränderte 429 mit Headern              | höchstens ein Ereignis je Akteur/Fenster           | Rate-Limit-Zähler sichtbar                           |
| Rate-Limit-Dienst nicht verfügbar     | unveränderte 503                          | kein Event, damit Messung keinen Angriff verstärkt | kein künstlicher Fehlerzähler                        |
| Migration/RPC fehlt oder ist kaputt   | Guide ist unabhängig und läuft weiter     | Insert kann übersprungen werden                    | bestehende Casino-BI 200; Guide-Kachel `unavailable` |
| Preis-Snapshot unbekannt              | Guide läuft weiter                        | Tokens optional, Kosten `NULL`                     | „keine Schätzung verfügbar“, niemals $0              |
| Keine Ereignisse                      | Guide läuft weiter                        | keine Sonderbehandlung                             | Leerzustand, keine 0%-Erfolgsbehauptung              |

## Eigener Plan-Audit und Anpassungen

| Auditbefund                                 | Risiko im bisherigen Plan                                            | Eingebaute Korrektur                                                                                                               |
| ------------------------------------------- | -------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| Usage-Contract fehlte                       | Token/Kosten hätten erfunden oder den Clientvertrag gebrochen        | Typisiertes `GuideAnswerResult`; Browserresponse bleibt unverändert; unvollständige Usage ist `null`.                              |
| Rate-Limit-Regel widersprach sich           | „erst nach Rate Limit“ konnte kein 429-Ereignis messen               | `rate_limited` nach Auth, vor Body; strikte 60-Sekunden-Deduplizierung.                                                            |
| Fire-and-forget war nicht definiert         | Ereignisse könnten beim Request-Ende verloren gehen                  | maximal 250-ms-abgewarteter, fehlerisolierter Recorder.                                                                            |
| Optionale BI hätte Kern-BI ausfallen lassen | fehlende Migration könnte `/admin/analytics` mit 503 brechen         | Guide-Segment ist explizit `unavailable`; Kernquellen bleiben fail-closed.                                                         |
| Preis ohne historischen Kontext             | Vergangene Kosten würden bei Preiswechsel falsch wirken              | ganzzahlige micro-USD + unveränderliche `pricing_version` und Snapshot-Datum.                                                      |
| Retention/Erasure fehlten                   | pseudonyme Daten könnten unbegrenzt liegen                           | 90 Tage, Purge-Executor als Go-live-Gate und serverseitige Löschroutine.                                                           |
| Provider-/DB-Fehler könnten in Logs leaken  | Gesprächs- oder Betriebsdaten könnten offengelegt werden             | nur statische Telemetrie-Logs, keine Error-Objekte oder Rohantworten.                                                              |
| Mindestlänge prüfte Zeichen statt Bytes     | Mehrbyte-Zeichen hätten die 32-Byte-Anforderung falsch bewertet      | `Buffer.byteLength(secret, 'utf8')` erzwingt nun mindestens 32 UTF-8-Bytes; ein Regressionsfall beweist dies.                      |
| Randfälle waren nicht vollständig beweisbar | Sicherheitslogik könnte bei späterem Refactor unbemerkt regressieren | zusätzliche Tests für abweichende Secrets, alle Fehler-Outcomes, ungültige Latenzen, unbekannte Preise und 250-ms-Timeout ergänzt. |

**Audit-Ergebnis:** Es wurden **9 relevante Befunde** gefunden und korrigiert. Danach bestehen keine offenen lokalen technischen Platzhalter. Offen bleibt ausschließlich die externe Betriebsfreigabe: konkreter Retention-Executor (Supabase Cron oder vorhandener vertrauenswürdiger Job) samt Owner, HMAC-Secret im Laufzeitumfeld, Datenbankmigration und der kontrollierte Live-Nachweis. Das sind bewusst Go-live-Gates, weil sie Zugriff auf externe Systeme und reale Betriebsverantwortung erfordern.
