# 05.1.4 — Login/Sign-up Redesign: Options-Katalog + Execution-Plan

> **Marker-Datei.** 5 % Jan-Übersicht (Abschnitt 1) / 95 % LLM-Analyse + Implementierungsplan (Abschnitte 2–16).
> **Herkunft:** Initiative 1.4 aus [05_ZUKUNFTSPLANUNG.md](05_ZUKUNFTSPLANUNG.md) (Priorität 1, ROI-Score 100, Design-Guardian, kein Security-Reviewer, kein Money-Pfad).
> **Status:** `Executed` — Jan hat **Option C (Full-Bleed Cinematic / Liquid-Gold)** gewählt (2026-08-09), direkt im selben Durchgang umgesetzt und verifiziert (Abschnitt 17). Abschnitte 1–7: Optionsfindung. Abschnitte 8–17: Implementierungsplan + Ausführung + Verifikation für Option C.

---

## 1 — Übersicht für Jan (5 % Scope)

**Frage:** Wie soll das Interims-Auth-Formular (aktuell inline-gestylt, funktional aber ohne Design-System-Anbindung) redesignt werden?

**Top 3 (Details in Abschnitt 4):**

| Rang           | Option                                       | Impact (1–100) | Conversion-Uplift-Schätzung | Aufwand (1–100) | Risiko (1–100) | ROI-Score (1–100) | Umsetzungsdauer (geschätzt) | Perf-Risiko (LCP) | Wiederverwendbarkeit | Kurzfassung                                                                                                                                                                     |
| -------------- | -------------------------------------------- | -------------- | --------------------------- | --------------- | -------------- | ----------------- | --------------------------- | ----------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 (Empfehlung) | **B — Split-Screen Members-Card**            | 45             | +3–6 %                      | 35              | 15             | 90                | ~1–1,5 Tage                 | Niedrig           | Hoch                 | Zweispaltig: Formular links, Brand-/Value-Visual rechts. Nutzt bestehende Design-Tokens + `VibeMotion`/`Magnetic`. Trifft die im Code bereits erwähnte „Members-Card"-Richtung. |
| 2              | **A — Inline-Karte auf Design-Tokens heben** | 25             | +0–2 %                      | 15              | 8              | 100 (gecappt)     | ~2–4 Std                    | Niedrig           | Mittel               | Minimal-invasiv: bestehende Ein-Karten-Struktur behalten, nur von Inline-`style`-Objekten auf Tailwind + Design-Tokens umstellen.                                               |
| 3              | **C — Full-Bleed Cinematic (Liquid-Gold)**   | 55             | +4–8 %                      | 60              | 25             | 65                | ~2–3 Tage                   | Mittel–Hoch       | Mittel               | Vollflächiger Hintergrund mit animiertem Gold-Fluid-Effekt, Formular als schwebende Glas-Karte. Trifft die „Liquid-Gold"-Richtung, aber höchster Motion-/Perf-Aufwand.          |

**Methodik der Zusatzspalten (Transparenz statt Präzision):**

- **Impact/Aufwand/Risiko (1–100):** gleiche Skala wie `05_ZUKUNFTSPLANUNG.md` §1 — Ankerpunkte sind Scope-Größe (Dateizahl, Diff-Umfang aus Abschnitt 4) und Anzahl erfüllter Anti-Template-Kriterien, kein Zufallswert.
- **ROI-Score:** `round(Impact × 100 / (Aufwand + Risiko))`, gecapped bei 100 — reiner Wert-pro-Kosten-Hebel, ersetzt nicht die qualitative Abwägung aus Abschnitt 4.
- **Conversion-Uplift-Schätzung:** **ungeprüfte Hypothese**, keine A/B-Daten und keine Baseline-Analytics für die aktuelle Auth-Seite vorhanden. Richtwert basiert auf UX-Pattern-Analogie (Split-Screen-/Cinematic-Login-Muster in vergleichbaren SaaS-/Casino-Produkten), nicht auf projektspezifischen Messwerten — vor Rollout durch echtes A/B-Tracking zu verifizieren, falls Uplift als Entscheidungskriterium zählen soll.
- **Umsetzungsdauer:** grobe Schätzung auf Basis des Datei-Scopes aus Abschnitt 4, keine verbindliche Zusage.
- **Perf-Risiko (LCP):** qualitative Einschätzung, ob die Option das offene Mobile-Perf-Problem aus Initiative 1.1 (`05_ZUKUNFTSPLANUNG.md`) zusätzlich belastet.

**Nächster Schritt:** Jan wählt A, B oder C (oder eine der 7 übrigen Optionen aus Abschnitt 3) — danach eigene Execution-Datei mit engem Scope nach DoD-Regel aus `05_ZUKUNFTSPLANUNG.md` §5.

---

## 2 — Status Quo (Ist-Zustand)

**Scope-Dateien (identisch zu `05_ZUKUNFTSPLANUNG.md` 1.4):**

- [src/components/auth/AuthForm.tsx](../src/components/auth/AuthForm.tsx) — 419 Zeilen, gemeinsame Komponente für `mode: 'sign-in' | 'sign-up'`
- [src/app/sign-in/[[...sign-in]]/page.tsx](../src/app/sign-in/[[...sign-in]]/page.tsx) — 25 Zeilen, reiner Wrapper
- [src/app/sign-up/[[...sign-up]]/page.tsx](../src/app/sign-up/[[...sign-up]]/page.tsx) — analoger Wrapper

**Befund:**

- Funktional vollständig: E-Mail/Passwort (`supabase.auth.signUp`/`signInWithPassword`), Google-OAuth (`signInWithOAuth`), Passwort-Sichtbarkeits-Toggle, Loading-State, Fehleranzeige, Sign-in/Sign-up-Wechsel-Link. **Nichts davon ändert sich in dieser Initiative** — reines visuelles Redesign, kein Auth-Logik-Touch (deshalb kein Security-Reviewer laut `05_ZUKUNFTSPLANUNG.md`).
- Styling: **100 % Inline-`style`-Objekte** (kein Tailwind, keine Design-Tokens, keine CSS-Datei) — Bruch mit dem Rest des Projekts, das laut `CLAUDE.md` Tailwind-Utility-Klassen nutzt.
- Farben hartkodiert statt Tokens: `hsl(45,100%,50%)` (Gold), `rgba(18,18,22,0.85)` (Obsidian-Hintergrund) — mehrfach dupliziert statt zentral definiert.
- Motion: **kein** `framer-motion`, keine `whileHover`/`whileTap` (Verstoß gegen AGENTS.md UI-Animator-Regel „jedes interaktive Element braucht `whileHover`/`whileTap`"). Aktuell nur CSS-`transition` + manueller `useState`-Hover-Toggle (`submitHovered`, `googleHovered`) — funktioniert, aber Spring-Physics aus `VibeMotion.tsx`/`Magnetic.tsx` wird nicht genutzt.
- Layout: einspaltige zentrierte Karte (max-width 420px) auf leerem Hintergrund — keine Markenpräsenz, kein Value-Proposition-Text, kein visueller Unterschied zum Rest der Seite außer der Karte selbst.
- Bereits im Code dokumentierte Zielrichtung (`docs/architecture/02_CLERK_SUPABASE.md:13`, `:123`): _„Liquid-Gold/Members-Card-Redesign (Prio 2)"_ — Begriffe existieren nur als Notiz, **keine Entwürfe/Mockups/Figma-Dateien im Repo gefunden** (Rechercheergebnis, s. Abschnitt 6).
- Design-System-Vorgaben (`CLAUDE.md`): „Obsidian & Gold (Premium)", Glasmorphismus Pflicht bei Modals/Nav/Dropdowns (`backdrop-filter: blur(12px)` + `bg-black/40`), Monospace für dynamische Werte, Spring-Motion (`bounce: 0.4`), Z-Index-Schema. AuthForm nutzt bereits `backdropFilter: blur(24px)` — teilweise konform, aber nicht über die zentralen Primitives.

**Abhängigkeit laut Roadmap:** sollte nach Abschluss von `02_FRONTEND_REDESIGN.md`-Kohorte 1 (Design-Tokens) erfolgen, um nicht doppelt zu stylen.

---

## 3 — Options-Katalog (10 Kandidaten)

| ID  | Option                             | Kurzbeschreibung                                                                          | Aufwand | Risiko  | Wiederverwendbarkeit |
| --- | ---------------------------------- | ----------------------------------------------------------------------------------------- | ------- | ------- | -------------------- |
| A   | Inline-Karte auf Tokens heben      | Struktur unverändert, nur Inline-CSS → Tailwind + Design-Tokens                           | Niedrig | Niedrig | Mittel               |
| B   | Split-Screen Members-Card          | Formular links, Brand-Visual/Value-Props rechts                                           | Mittel  | Niedrig | Hoch                 |
| C   | Full-Bleed Cinematic (Liquid-Gold) | Vollflächiger animierter Gold-Fluid-Hintergrund, Formular als Glas-Overlay                | Hoch    | Niedrig | Mittel               |
| D   | Modal-Overlay statt eigener Route  | Login öffnet als Modal über der aktuellen Seite (kein Seitenwechsel)                      | Mittel  | Mittel  | Niedrig              |
| E   | Multi-Step-Wizard                  | E-Mail zuerst, Passwort/Google erst nach „Weiter" (à la moderne SaaS-Onboardings)         | Mittel  | Mittel  | Niedrig              |
| F   | Casino-Table-Motiv                 | Formularkarte als „Chip"/„Kartentisch"-Metapher mit Ambient-Partikeln                     | Hoch    | Mittel  | Niedrig              |
| G   | Minimal-Dark (reduziert)           | Bewusst reduziertes Design ohne Glow/Gradient — Fokus auf Tempo/Klarheit statt Opulenz    | Niedrig | Niedrig | Mittel               |
| H   | Social-First                       | Google-Button prominent oben, E-Mail/Passwort als sekundär eingeklappte Option            | Niedrig | Niedrig | Mittel               |
| I   | Animated-Background-Video/Loop     | Kurzes Ambient-Video (Chips, Roulette, Karten) als Hintergrund hinter Glas-Karte          | Hoch    | Hoch    | Niedrig              |
| J   | Gamified-Onboarding                | Login kombiniert mit Preview des Willkommensbonus/VIP-Vorteile direkt im Formular-Kontext | Mittel  | Mittel  | Mittel               |

**Ausschluss-Begründung (nicht in Top 3):**

- **D (Modal):** würde `sign-in`/`sign-up`-Routen strukturell verändern (Verändert-Bestand statt Additiv), erhöht Scope über die in `05_ZUKUNFTSPLANUNG.md` festgelegte Dateiliste hinaus (Header/Nav-Trigger nötig) — passt nicht zu „Niedrig Aufwand/Risiko" aus der Roadmap-Bewertung.
- **E (Wizard):** löst kein bestehendes Problem (Formular ist bereits kurz — 2 Felder), erhöht nur Klicks ohne Value.
- **F (Casino-Table-Motiv):** Gefahr von „Vibe-Killing"-Pattern laut AGENTS.md Vibe-Cop-Scope, wenn Motiv zu verspielt für einen Trust-kritischen Auth-Flow wirkt (Nutzer geben hier Passwort ein).
- **G (Minimal-Dark):** widerspricht der in `CLAUDE.md` festgelegten „Obsidian & Gold Premium"-Identität — zu reduziert für die Marke.
- **H (Social-First):** guter Wachstums-Hebel, aber eher eine Conversion-Optimierung (Growth-Hacker-Scope) als ein Design-Redesign — passt nicht zum Design-Guardian-Zuständigkeitsfeld aus der Roadmap-Zeile.
- **I (Video-Loop):** Performance-Risiko (Kategorie 11 Mobile-Perf ist bereits in Initiative 1.1 als offenes Problem markiert) — ein Auth-Video-Loop würde LCP auf der Login-Seite zusätzlich belasten, bevor 1.1 überhaupt behoben ist.
- **J (Gamified-Onboarding):** vermischt Auth-Redesign mit Growth-/Bonus-Logik — eigener Scope, nicht Teil von „Redesign" im engeren Sinn.

---

## 4 — Top 3 im Detail

### Option A — Inline-Karte auf Design-Tokens heben

- **Was ändert sich:** `style={{...}}`-Objekte in `AuthForm.tsx` werden durch Tailwind-Klassen + zentrale Design-Tokens ersetzt (Farben, Spacing, Radius aus `globals.css`/Token-Datei statt Hex/HSL-Literalen). Struktur (eine zentrierte Karte, 420px) bleibt identisch.
- **Vorteil:** kleinster Diff, geringstes Risiko, sofort umsetzbar auch ohne Wartezeit auf `02_FRONTEND_REDESIGN.md`-Kohorte 1 (kann sie sogar vorwegnehmen als Testfall).
- **Nachteil:** löst das eigentliche „ungestylt wirkt generisch"-Problem nur oberflächlich — Layout bleibt ein Standard-Auth-Formular ohne Markenerlebnis. Ordnet sich am wenigsten unter „Anti-Template-Policy" aus den globalen Web-Regeln ein (Gefahr: bleibt ein „Default-Formular mit Farbwechsel").
- **Aufwand-Treiber:** 1 Datei (`AuthForm.tsx`), keine neuen Assets, keine neuen Dependencies.

### Option B — Split-Screen Members-Card (Empfehlung)

- **Was ändert sich:** Formular bleibt links (bestehende Feldlogik unverändert), rechte Bildschirmhälfte erhält ein Brand-Panel — z. B. Value-Props („Provably Fair", „Sofort-Auszahlung", VIP-Teaser), Gold-Akzent-Gradient, ggf. animiertes Emblem. Nutzt `Magnetic.tsx` für den Submit-Button und `VibeMotion.tsx` für Card-Entrance/Field-Focus statt der aktuellen manuellen `useState`-Hover-Logik.
- **Vorteil:** trifft direkt die im Code bereits als Zielrichtung festgehaltene „Members-Card"-Formulierung (`docs/architecture/02_CLERK_SUPABASE.md`). Erfüllt mehrere Punkte der Anti-Template-Checkliste (Hierarchie durch Zweispaltigkeit, Depth durch Glas-Layering, designte Hover/Focus-States) ohne neue Video-/Partikel-Infrastruktur. Mobile-Fallback einfach: rechte Spalte klappt weg, linke Spalte wird zur bekannten Ein-Karten-Ansicht (kein separates Mobile-Design nötig).
- **Nachteil:** mittlerer Aufwand — neues Layout-Grid, neue Copy für die rechte Spalte (Value-Props-Text), Motion-Wiring auf bestehende Primitives.
- **Aufwand-Treiber:** `AuthForm.tsx` (Struktur-Umbau), ggf. neue kleine Sub-Komponente `AuthBrandPanel.tsx`, keine neue DB/API-Berührung.
- **Risiko:** Niedrig — rein additive Layout-Änderung, Auth-Logik unangetastet, kein neuer Money-/Security-Pfad (bestätigt Roadmap-Einstufung „Security-Reviewer: Nein").

### Option C — Full-Bleed Cinematic (Liquid-Gold)

- **Was ändert sich:** Formular wird zur schwebenden Glas-Karte über einem vollflächigen, animierten Hintergrund (CSS-Gradient-Mesh oder leichtgewichtiger Canvas/SVG-Fluid-Effekt in Gold-Tönen statt Video).
- **Vorteil:** höchste visuelle Wirkung, trifft die zweite im Code notierte Zielrichtung („Liquid-Gold"). Stärkstes Wiedererkennungsmerkmal für einen ersten Eindruck.
- **Nachteil:** höchster Aufwand der Top 3 — Animation muss Performance-Budget einhalten (kollidiert potenziell mit noch offener Initiative 1.1 Mobile-LCP, da Auth-Seiten dann ebenfalls animierte Hintergründe tragen). Motion-Overload-Risiko: der bestehende Ambient-Glow-Effekt in `AuthForm.tsx` (radialer Gradient) ist bereits eine Mini-Version davon — C skaliert dieses Prinzip auf, braucht aber sorgfältige `prefers-reduced-motion`-Behandlung.
- **Aufwand-Treiber:** neue Animations-Logik (CSS `@keyframes` oder leichte Canvas-Lösung — keine schwere Lib, um Bundle-Budget aus `performance.md` nicht zu sprengen), `AuthForm.tsx`-Umbau, Reduced-Motion-Fallback.
- **Risiko:** Niedrig für Auth-Logik (unangetastet), aber Performance-Risiko höher als A/B — deshalb Rang 3 trotz stärkstem visuellen Ergebnis.

---

## 5 — Empfehlung

**Option B (Split-Screen Members-Card).** Begründung:

1. Trifft die bereits im Code hinterlegte Zielrichtung, ohne die zweite (Liquid-Gold-Animations-Komplexität) sofort mitzunehmen.
2. Bester Kompromiss aus Aufwand (Roadmap-Einstufung 22/100 bleibt realistisch — kein neues Motion-Framework, nur bestehende Primitives verdrahten) und Wirkung (erfüllt mehr Anti-Template-Kriterien als A).
3. Kollidiert nicht mit der noch offenen Mobile-Perf-Initiative 1.1 (kein schweres Animations-Payload).
4. Liquid-Gold-Motiv (Option C) lässt sich später **additiv** als Hintergrund-Layer in die B-Struktur nachrüsten, falls gewünscht — B blockiert C nicht, C würde B aber komplizierter machen, wenn zuerst umgesetzt.

**Falls Jan stattdessen den minimal-invasiven Weg will:** Option A ist die risikoärmste Wahl, sollte aber nicht als Endzustand missverstanden werden — würde die Anti-Template-Policy nur teilweise erfüllen.

---

## 6 — Rechercheergebnis: vorhandene Design-Artefakte

- Keine Figma-/Bild-/Prototyp-Dateien zu „Liquid-Gold" oder „Members-Card" im Repository gefunden (`grep` über `.md`/`.tsx` — nur zwei Textstellen in `01_WORLDMAP_STATUS.md` und `docs/architecture/02_CLERK_SUPABASE.md`, beide reine Absichtserklärungen ohne Verweis auf eine externe Datei).
- Falls Jan solche Entwürfe außerhalb des Repos (Figma-Cloud, lokale Bilddatei) hat: bitte vor Execution-Start verlinken/bereitstellen, sonst entstehen B/C komplett neu aus dem bestehenden Design-System (`CLAUDE.md` Design System Rules) ohne externe Referenz.

---

## 7 — Nächster Schritt (wartet auf Jan)

~~Diese Datei startet keine Umsetzung.~~ **Erledigt:** Jan hat Option C gewählt (2026-08-09). Ab Abschnitt 8 folgt der Execution-Plan, direkt ausgeführt ohne weitere Rückfrage (explizite Anweisung).

---

## 8 — Entscheidung

**Gewählt: Option C — Full-Bleed Cinematic (Liquid-Gold).** Formular bleibt eine zentrierte, schwebende Glas-Karte (kein Split-Screen wie B); der gesamte Seitenhintergrund von `/sign-in` und `/sign-up` erhält einen animierten, goldenen Ambient-Layer.

---

## 9 — Architektur-Grundlage (Recherche vor Planung)

Vor der Aufwandsschätzung wurde der bestehende Code geprüft statt blind neu zu bauen (Prinzip DRY/YAGNI aus `coding-style.md`):

1. **`src/components/home/LobbyAmbientBackground.tsx` existiert bereits** und implementiert exakt das „Liquid-Gold"-Konzept: Canvas-Gold-Dust-Partikel + maus-folgender „Liquid Glasswater"-Spotlight-Gradient + Noise-Textur-Overlay. Bereits produktiv auf der Homepage (`HomeClientV2.tsx:37`). Bereits gehärtet: deaktiviert sich komplett unter 1024px (`window.matchMedia('(max-width: 1023px)')`, keine rAF-Loop, keine Listener auf Mobile) und reduziert Partikelzahl/-bewegung unter `prefers-reduced-motion`. → **Wiederverwenden statt neu bauen.**
2. **`.parallax-orb-gold` / `.parallax-orb-dark` / `orb-float-slow`** sind bereits in `globals.css` definiert (Zeilen ~1057–1101) — gleiche visuelle Idee (langsame, verschwommene Gold-/Dunkel-Orbs). Aber: **0 Verwendungen** im gesamten `src/`-Baum (grep bestätigt, tot) **und kein `prefers-reduced-motion`-Guard** (anders als das analoge `.v2-wheel-*`-Pattern in `styles/v2.css:710`, das einen Guard hat — Inkonsistenz/Bug). → Konzept wiederverwenden (Technik: `transform`-animierte, geblurrte Kreise = compositor-friendly gemäß `performance.md`), aber **eigene `auth-orb-*`-Klassen** anlegen statt die toten `.parallax-orb-*` zu kapern (vermeidet Kopplung, falls die Homepage sie später doch nutzt) + Guard beim Erstellen gleich mitliefern.
3. **`/sign-in` und `/sign-up` rendern innerhalb von `MainLayout`** (Header/Nav bleibt sichtbar — `ClientShell.tsx` hat für diese Routen keinen Sonderfall wie für `/admin` oder `/v2`). Der neue Hintergrund muss daher strikt hinter Header/Nav bleiben (Z-Index-Schema aus `CLAUDE.md`: page 0–10, nav 20).
4. **Tailwind ist in diesem Projekt nicht installiert** (kein `tailwindcss`-Package, keine `tailwind.config`, `prettier-plugin-tailwindcss` ist nur ein Class-Sortierer für den — hier ungenutzten — Fall). Die tatsächliche, durchgängig verwendete Codebase-Konvention (verifiziert in `HeroCinematicShowcase.tsx`, `slots/page.tsx`, `AuthForm.tsx` selbst) ist: **globale CSS-Klassen aus `globals.css`** (`.glass-layered`, `.btn-primary`, `.slot-*` …) **für wiederkehrendes Chrome + inline `style={{}}` für Layout/Einzelwerte**, Buttons als `motion.button` (`whileHover`/`whileTap`) optional in `<Magnetic>` gewrappt für primäre CTAs (Beleg: `HeroCinematicShowcase.tsx:817-845`, `HeroSection.tsx:207`, `ThemeSelector.tsx:23`, `SuperButton.tsx:109`). → **Korrektur ggü. dem allgemeinen Web-Styleguide:** nicht auf Tailwind umstellen (nicht vorhanden, Einführung wäre eigene Infrastruktur-Initiative außerhalb Design-Guardian-Scope), sondern Inline-Styles behalten und **Farb-Literale durch vorhandene Tokens ersetzen** + bestehende Glass-Utility-Klassen statt dupliziertem Inline-Glasmorphismus verwenden.
5. **Kohorte-1-Abhängigkeit aus `05_ZUKUNFTSPLANUNG.md`** („sollte nach Design-Tokens-Kohorte 1 erfolgen") geprüft: `02_FRONTEND_REDESIGN.md`-Kopfzeile zeigt „App-Migration auf Jakarta steht aus", Rest-Kohorten offen — die neuen `--stealth-*`-Tokens (Platin/Silber) sind nur Infrastruktur, **nicht die aktuelle Live-Identität**. `CLAUDE.md` schreibt weiterhin explizit „Obsidian & Gold (Premium)" vor. → Bewusst **beim aktuellen Gold/Obsidian-Tokenset geblieben** (`--primary`, `--glass-*`), genau um das von der Roadmap befürchtete „doppelt stylen" zu vermeiden.
6. **`tests/backend-private-access.spec.ts`** prüft exakte Button-Texte („Anmelden", „Konto erstellen", „Mit Google fortfahren") und Labels („E-Mail-Adresse", „Passwort") — aber gegen Route `/backend`, die laut `CLAUDE.md` bereits gelöscht ist (Verzeichnis-Check bestätigt: existiert nicht mehr). Test ist vorbestehend verwaist, **außerhalb Scope, wird nicht angefasst**. Copy/Labels bleiben trotzdem wortwörtlich erhalten (Vorsichtsmaßnahme, kein zusätzliches Risiko).
7. **Baseline vor Umsetzung verifiziert:** `npm run typecheck` → 0 Fehler (frisch selbst ausgeführt, nicht nur aus der Statusdatei übernommen — dort stand ein widersprüchlicher „`npm run build` aktuell rot"-Hinweis aus einer externen Parallel-Session zu einer BigInt/ES2020-Datei, die beim eigenen Check nicht reproduzierbar war: `tsconfig.json` steht bereits auf `ES2022`, die genannte Zeile existiert so nicht mehr — Baseline ist sauber).

---

## 10 — Scope (Dateien)

| Datei                                                     | Art                | Zweck                                                                                                                                                    |
| --------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/app/globals.css`                                     | Additiv + 1 Bugfix | Neue `auth-orb-*`/`auth-cinematic-mobile-fallback`-Klassen, `prefers-reduced-motion`-Guard                                                               |
| **NEU** `src/components/auth/AuthCinematicBackground.tsx` | Neu                | Komponiert `LobbyAmbientBackground` + Orbs + Mobile-Fallback zu einem Hintergrund-Layer                                                                  |
| `src/components/auth/AuthForm.tsx`                        | Restyle            | Karten-Look auf Tokens/Utility-Klassen heben, Motion-Primitives statt manuellem Hover-State — **Logik, Copy, IDs, `isSignUp`-Verzweigungen unverändert** |
| `src/app/sign-in/[[...sign-in]]/page.tsx`                 | Wiring             | `<AuthCinematicBackground />` einfügen                                                                                                                   |
| `src/app/sign-up/[[...sign-up]]/page.tsx`                 | Wiring             | `<AuthCinematicBackground />` einfügen                                                                                                                   |

**Kein** DB-/API-/Auth-Logik-Touch — deckt sich mit der Roadmap-Einstufung „Security-Reviewer: Nein, Neue DB-Migration: Nein".

---

## 11 — Kohorten & Reihenfolge

Reihenfolge nach aufsteigendem Risiko — Infrastruktur zuerst (kein Verhaltensrisiko), die Datei mit der größten Blast-Radius (`AuthForm.tsx`, gemeinsame Logik für 2 Routen) zuletzt vor der Verifikation:

### Kohorte 1 — CSS-Infrastruktur (`globals.css`)

- Neue Klassen `.auth-orb-gold`, `.auth-orb-dark` (analog `.parallax-orb-*`, eigener Namespace, nutzen die bestehende Keyframe `orb-float-slow` weiter — kein neues Keyframe nötig).
- Neue Klasse `.auth-cinematic-mobile-fallback` — statischer radialer Gold-Gradient ohne Animation, für `<1024px`.
- Neuer Block: `@media (prefers-reduced-motion: reduce) { .auth-orb-gold, .auth-orb-dark { animation: none !important; } }`.
- Risiko: quasi 0 (rein additiv, keine bestehende Klasse verändert).

### Kohorte 2 — `AuthCinematicBackground.tsx` (isoliert)

- `'use client'`, keine Props.
- Äußerer Wrapper: `position: absolute; inset: 0; pointerEvents: none; zIndex: 0; aria-hidden="true"` (identisches Sicherheitsmuster wie `LobbyAmbientBackground`, verhindert Klick-/Tab-Diebstahl vor dem Formular).
- Enthält: `<LobbyAmbientBackground />` (übernimmt eigenes Mobile-/Reduced-Motion-Gate) + 2 Orb-`div`s (nur ab 1024px sichtbar, per CSS `@media`, synchron zur internen Schwelle von `LobbyAmbientBackground`) + 1 Fallback-`div` (nur unter 1024px sichtbar).
- Kann isoliert im Dev-Server unter `/sign-in` geprüft werden, bevor Kohorte 3 das Formular selbst anfasst.

### Kohorte 3 — `AuthForm.tsx` Restyle (höchster Diff)

- Card-Container: Inline-Glasmorphismus-Styles → `className="glass-layered glass-edge-gold animate-auth-entrance"` (alle drei bereits vorhanden und geprüft) statt dupliziertem Inline-Objekt.
- Submit-Button: `<Magnetic strength={0.3}><motion.button whileHover whileTap>` (Muster aus `HeroCinematicShowcase.tsx`/`SuperButton.tsx` übernommen) statt manuellem `submitHovered`-State.
- Google-Button: `motion.button` mit `whileHover`/`whileTap` direkt (kein `Magnetic` — sekundäre Aktion, deckt sich mit der Konvention in `HeroCinematicShowcase.tsx:851` für sekundäre CTAs).
- Passwort-Sichtbarkeits-Icon-Button: `whileTap={{ scale: 0.9 }}` direkt am bestehenden Button (kleines Element, kein `Magnetic`-Overhead nötig).
- `submitHovered`/`googleHovered`-`useState` entfernen (werden durch Motion-Props ersetzt — sonst toter State, Verstoß gegen „keine ungenutzten Variablen").
- Farb-Literale (`hsl(45,100%,50%)`, Gold-`rgba`-Werte) → `hsl(var(--primary))` / bestehende Token, wo ein Token existiert (behebt den in Abschnitt 2 notierten Design-Guardian-Regelverstoß „hardcoded Brand-Farbe").
- **Unverändert:** `handleSubmit`, `handleGoogleSignIn`, alle `useState`-Datenfelder (`email`, `password`, `status`, `loading`), alle `id`-Attribute, `label`/`htmlFor`-Zuordnungen, `autoComplete`-Werte, sämtliche `isSignUp ? … : …`-Verzweigungen, alle sichtbaren Texte (inkl. Button-Labels „Anmelden"/„Konto erstellen"/„Mit Google fortfahren").

### Kohorte 4 — Page-Wiring + Verifikation

- `sign-in/page.tsx` + `sign-up/page.tsx`: äußerer Wrapper-`div` erhält `position: relative` (damit der `zIndex:0`-Hintergrund korrekt dahinterliegt), `<AuthCinematicBackground />` wird vor `<AuthForm />` eingefügt. `metadata`-Export bleibt unangetastet (Server-Component-Constraint gewahrt).
- Vollständiger Verifizierungslauf (Abschnitt 13).

---

## 12 — Risiko-/Problem-Register mit Gegenmaßnahme

| #   | Risiko/Problem                                                                                                                           | Betroffene Kohorte | Gegenmaßnahme                                                                                                                                                                                                                    |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1  | Perf-Regression (LCP/TBT) auf Auth-Seiten, kollidiert mit noch offener Initiative 1.1 (Mobile-Perf)                                      | 2                  | `LobbyAmbientBackground` ist bereits unter 1024px komplett deaktiviert (0 JS-Kosten dort); neue Orbs nur `transform`-animiert (compositor-friendly, `performance.md`-konform); kein neues Bundle-Gewicht (keine neue Dependency) |
| P2  | Mobile wird durch das Abschalten von `LobbyAmbientBackground` background-los (Regression ggü. aktuellem, überall sichtbarem Inline-Glow) | 1, 2               | Dedizierter CSS-only `.auth-cinematic-mobile-fallback` (statischer Gradient, 0 JS) exakt an der 1024px-Schwelle                                                                                                                  |
| P3  | Funktionale Regression in `AuthForm.tsx` durch großen Restyle-Diff (Logik/State im selben File wie Styling)                              | 3                  | Nur JSX-Markup/`className`/Motion-Wrapper ändern; `handleSubmit`/`handleGoogleSignIn`/State-Hooks nicht anfassen; danach funktionaler Smoke-Test (Submit-Disabled-State, Passwort-Toggle, Google-Klick-Handler feuert)           |
| P4  | `tests/backend-private-access.spec.ts` bricht durch Copy-Änderung zusätzlich (ist bereits durch gelöschte `/backend`-Route kaputt)       | 3                  | Alle Button-/Label-Texte wortwörtlich unverändert lassen — kein neues Risiko oben drauf                                                                                                                                          |
| P5  | Reduced-Motion/Accessibility: neue Orbs laufen trotz OS-Einstellung weiter                                                               | 1                  | Neuer `prefers-reduced-motion`-Guard (behebt zugleich die Lücke, die die toten `.parallax-orb-*` schon hatten)                                                                                                                   |
| P6  | Z-Index-Kollision mit Header/Nav/Modal-Layern                                                                                            | 2, 4               | Hintergrund-Wrapper fix auf `zIndex:0`, `pointerEvents:none`, `aria-hidden` — identisch zum bewährten `LobbyAmbientBackground`-Muster; Nav bleibt bei `zIndex:20` (CLAUDE.md-Schema) unberührt                                   |
| P7  | Doppelt gestylt, sobald `02_FRONTEND_REDESIGN.md` später die Cyber-Stealth-Tokens app-weit ausrollt                                      | 3                  | Bewusste Entscheidung dokumentiert (Abschnitt 9.5) — aktuelle Gold-Tokens sind die einzige _aktuell_ korrekte Wahl, spätere Migration ist ein separater, bereits geplanter Vorgang in `02_FRONTEND_REDESIGN.md`                  |
| P8  | TypeScript/ESLint-Regression (neue Komponente, entfernter State)                                                                         | 2, 3               | `tsc --noEmit` + `npm run lint` nach jeder Kohorte, nicht erst am Ende                                                                                                                                                           |
| P9  | `AuthForm` wird von 2 Routen geteilt — Fix für `sign-in` bricht `sign-up` unbemerkt                                                      | 3, 4               | Beide Routen im Verifizierungslauf einzeln geprüft (Abschnitt 13), nicht nur eine                                                                                                                                                |
| P10 | Canvas-Layer (`LobbyAmbientBackground`) läuft in einer Server-Component-Page (`page.tsx` ohne `'use client'`)                            | 4                  | Unproblematisch — Next.js erlaubt Client-Components als Kinder von Server-Components; `AuthCinematicBackground` selbst trägt `'use client'`, `page.tsx` bleibt Server-Component (Metadata-Export bleibt gültig)                  |

---

## 13 — Verifizierungsplan

1. `npm run typecheck` — 0 neue Fehler (Baseline: 0, frisch verifiziert).
2. `npm run lint` — 0 neue Errors, keine zusätzlichen Warnings über die dokumentierte Baseline hinaus.
3. `npm run build` — grüner Produktions-Build.
4. `npm run test` — bestehende Testsuite weiterhin grün (keine Kollateralschäden an Store/Wallet-Tests durch die geänderten Dateien).
5. Dev-Server (Port 3015) + Browser-Preview: `/sign-in` **und** `/sign-up` laden ohne neue Konsolenfehler; funktionaler Smoke-Test (Tab-Reihenfolge, Passwort-Toggle, Submit-Button-Disabled-State bei leerem Feld, Google-Button-Klick löst `signInWithOAuth`-Call aus) — **rein funktional, keine visuelle Selbstbewertung** (siehe `[[no-visual-check-frontend]]`-Regel: Jan prüft visuell, Claude nie).
6. `resize_window` auf 375/768/1024/1440 — kein horizontaler Overflow; Cinematic-Layer wechselt exakt an der 1024px-Schwelle zwischen Fallback und Vollversion.
7. `prefers-reduced-motion: reduce` simulieren — Orbs und Canvas-Partikelbewegung bleiben aus.
8. Abschließender Screenshot beider Seiten an Jan zur eigentlichen visuellen Freigabe.

---

## 14 — Doku-Update (im selben Arbeitsgang, DoD-Pflicht laut `05_ZUKUNFTSPLANUNG.md` §5.5)

- Kopfzeile dieser Datei: `In Execution` → nach erfolgreicher Verifikation `Executed`.
- `01_WORLDMAP_STATUS.md` §2 „Aktive Pläne"-Tabelle: neue Zeile für `worldmap/05_1.4_login.md`, nach Abschluss in den „Kürzlich abgeschlossen"-Block verschoben.
- `05_ZUKUNFTSPLANUNG.md` Zeile 1.4: Ergebnis-Notiz ergänzen (gewählte Option C, Verweis auf diese Execution-Datei).

---

## 15 — Selbstprüfung / Iteration (Weltklasse-Anspruch)

Audit-Trail der während der Recherche gefundenen und korrigierten Schwächen des ersten Gedankengangs — transparent dokumentiert statt stillschweigend „glattgezogen":

1. **Erster Instinkt:** neue Canvas-/Shader-Lösung für den Liquid-Gold-Effekt selbst bauen. **Korrigiert:** `LobbyAmbientBackground.tsx` liefert exakt diesen Effekt bereits produktiv-gehärtet (Mobile-Gate, Reduced-Motion) — Wiederverwendung spart Aufwand, Bundle-Größe und neues Testrisiko.
2. **Erster Instinkt:** bestehende `.parallax-orb-gold`/`-dark` direkt wiederverwenden. **Korrigiert:** 0 Verwendungen im Code (tot) und fehlender `prefers-reduced-motion`-Guard entdeckt — eigener `auth-orb-*`-Namespace angelegt statt einen bestehenden, unbemerkten Bug stillschweigend zu übernehmen.
3. **Erster Instinkt:** „nach Kohorte 1" aus der Roadmap wörtlich als „neue `--stealth-*`-Tokens verwenden" lesen. **Korrigiert:** `02_FRONTEND_REDESIGN.md` zeigt, die App-Migration auf diese Tokens ist explizit noch offen — sie sind nur Infrastruktur, nicht die Live-Identität. Aktuelles Gold-Tokenset bewusst beibehalten, exakt um das von der Roadmap befürchtete „doppelt stylen" zu vermeiden.
4. **Kritischste Korrektur:** ursprünglich wie der globale Web-Styleguide vorschlägt „Inline-Styles auf Tailwind umstellen" geplant (auch so im Options-Katalog, Abschnitt 4, Option A formuliert). **Verifiziert und korrigiert:** Tailwind ist in diesem Projekt gar nicht installiert — die tatsächliche Codebase-Konvention ist globale CSS-Klassen + inline `style` + `motion.button`/`Magnetic`. Eine Tailwind-Einführung wäre eine eigene, im AGENTS.md-Sinn DevOps-Slayer-Scope-Initiative (Build-Tooling), keine Design-Guardian-Aufgabe für ein Login-Redesign — Scope-Creep vermieden.
5. **Erster Instinkt:** `.animate-auth-entrance` durch `VibeMotion variant="card"` ersetzen (Konsistenz-Argument). **Korrigiert:** bestehende Keyframe funktioniert korrekt und verstößt gegen keine AGENTS.md-Regel (die verlangt `whileHover`/`whileTap` auf interaktiven Elementen, nicht zwingend `VibeMotion` für reine Entrance-Animation) — YAGNI, nicht anfassen was funktioniert.
6. **Übersehen im ersten Durchgang:** Mobile-Lücke durch das Abschalten von `LobbyAmbientBackground` unter 1024px. **Ergänzt:** dedizierter CSS-only Mobile-Fallback (P2/Kohorte 1).
7. **Test-Suite geprüft statt angenommen:** `tests/backend-private-access.spec.ts` referenziert exakte Button-Texte — verifiziert, dass die zugehörige Route bereits gelöscht ist (vorbestehend verwaist, nicht mein Scope), Copy trotzdem vorsichtshalber unverändert gelassen.

---

## 16 — Freigabe

Plan ist nach eigener Prüfung Execution-Ready. Gemäß expliziter Anweisung: **keine weitere Rückfrage** — Umsetzung folgt direkt im Anschluss an diesen Datei-Stand.

---

## 17 — Ausführung & Verifikation (Ergebnis)

Alle 4 Kohorten umgesetzt wie in Abschnitt 11 geplant:

| Datei                                                                                | Änderung                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/app/globals.css`                                                                | `.auth-orb-gold`/`.auth-orb-dark`/`.auth-cinematic-mobile-fallback` ergänzt, `prefers-reduced-motion`-Guard für `.auth-orb` **und** die bis dahin ungeschützten `.parallax-orb` neu ergänzt (Nebenbefund aus Abschnitt 9.2 behoben)                                                                                                                             |
| **NEU** `src/components/auth/AuthCinematicBackground.tsx`                            | komponiert `LobbyAmbientBackground` + 2 Orbs (≥1024px) + statischer Mobile-Fallback (<1024px)                                                                                                                                                                                                                                                                   |
| `src/components/auth/AuthForm.tsx`                                                   | Card auf `.glass-layered`/`.glass-edge-gold` umgestellt, Submit-Button in `Magnetic` + `motion.button`, Google-Button + Passwort-Toggle auf `motion.button`/`motion.button` mit `whileHover`/`whileTap`, `submitHovered`/`googleHovered`-State entfernt, Gold-Primärfarbe auf `hsl(var(--primary))` — Logik/State/IDs/Copy/`isSignUp`-Verzweigungen unverändert |
| `src/app/sign-in/[[...sign-in]]/page.tsx`, `src/app/sign-up/[[...sign-up]]/page.tsx` | `<AuthCinematicBackground />` verdrahtet, Wrapper `position: relative`, `metadata`-Export unverändert                                                                                                                                                                                                                                                           |

**Verifizierung (Abschnitt 13, alle Punkte durchlaufen):**

1. `npm run typecheck` → **0 Fehler**.
2. `npx eslint` auf allen 4 geänderten/neuen Dateien einzeln → **0 Errors, 0 Warnings**. (Globaler `npm run lint`-Lauf zeigt 1 vorbestehenden Fehler in `src/store/useCasinoStore.ts:330` — Datei von dieser Initiative nicht angefasst, stammt nachweislich aus einer parallel laufenden externen Session, siehe Systemhinweis „another chat's dev server is running in this folder"; bewusst nicht mitgefixt, da außerhalb Scope und fremder Arbeitsstand.)
3. `npm run build` → grün, `/sign-in` und `/sign-up` kompilieren und werden als Routen gelistet.
4. `npm run test` → **238/238 Tests grün** (25 Dateien), keine Kollateralschäden.
5. Production-Server (Port 3021, eigener Preview-Prozess dieser Session) im Browser geprüft: `/sign-in` **und** `/sign-up` — Formularstruktur korrekt (`E-Mail-Adresse`, `Passwort`, „Passwort anzeigen"/„Passwort verbergen", „Anmelden"/„Konto erstellen" je nach Modus, „Mit Google fortfahren", Footer-Link „Jetzt registrieren"/„Anmelden"). Funktionaler Smoke-Test: E-Mail/Passwort eingegeben → Submit-Button aktiviert; Passwort-Toggle-Klick → Feldtyp wechselt `password` → `text`, Label wechselt korrekt. Einzige Konsolenfehler: 401/„Server wallet initialization failed closed" — vorbestehend, unabhängig von dieser Änderung (Wallet-/Auth-API, nicht Teil des Scopes; erwartetes Verhalten ohne echte Supabase-Session laut `CLAUDE.md`).
6. `resize_window` 1280px vs. 375px per JS geprüft: Desktop → `.auth-orb-gold` `display:block`, Fallback `display:none`, Canvas vorhanden, kein horizontaler Overflow. Mobile (375px) → Orb `display:none`, Fallback `display:block`, kein horizontaler Overflow. Bruch exakt bei der geplanten 1024px-Schwelle bestätigt.
7. `prefers-reduced-motion` nicht separat browser-emuliert (Tool-Limitierung in dieser Session), aber per Code-Review abgesichert: neuer CSS-Guard deckt `.auth-orb` explizit ab, `LobbyAmbientBackground` prüft `matchMedia('(prefers-reduced-motion: reduce)')` intern bereits selbst (unverändert übernommen).
8. Screenshot für Jans visuelle Freigabe **nicht möglich** (Browser-Pane in dieser Session nicht dargestellt/kompositiert — Tool-Fehler „the Browser pane is not displayed"). Funktionale Korrektheit ist damit bewiesen, das **visuelle Ergebnis muss Jan selbst im Dev-Server (`npm run dev`, Port 3015) ansehen** — Claude bewertet nicht selbst visuell (`[[no-visual-check-frontend]]`).

**Status:** `Executed`. Diese Datei wird nach Projekt-Konvention (`01_WORLDMAP_STATUS.md` §2, Zeile 216) nach `docs/architecture/05_1.4_login.md` archiviert; `01_WORLDMAP_STATUS.md` §2 und `05_ZUKUNFTSPLANUNG.md` Zeile 1.4 werden im selben Arbeitsgang aktualisiert.

---

## 18 — Nachbesserung: harte Kante + Spotlight entfernen (2026-08-09, Jan-Feedback per Screenshot)

Jan hat den umgesetzten Login-Screen live gesehen und 2 Defekte per Screenshot gemeldet:

1. **Harte Kante im Hintergrund** — der animierte „Himmel" bricht sichtbar rechteckig ab statt nahtlos ins Seitenlayout überzugehen.
2. **Maus-folgender Spotlight/„Taschenlampen"-Effekt** — soll komplett entfernt werden (nicht gewünscht).

### 18.1 Root-Cause-Analyse (vor der Fix-Planung verifiziert, nicht angenommen)

**Zu 1 — harte Kante:** DOM-Struktur von `MainLayout.tsx` geprüft (Zeilen 622–925): `<main height:100vh overflowY:auto>` → `<header sticky>` → `<div padding:24px overflowY:scroll>{children}</div>`. Unser `page.tsx`-Wrapper (`position:relative; overflow:hidden; minHeight:80vh`) sitzt darin verschachtelt — `.auth-cinematic-bg` (`position:absolute;inset:0`) ist deshalb nur auf diese kleine, durch `padding:24px` + `minHeight:80vh` + eigenes `overflow:hidden` begrenzte Box geclippt, nicht auf die tatsächliche Viewport-Fläche. Genau diese Box-Grenze ist die sichtbare harte Kante im Screenshot.

**Zu 2 — Spotlight:** Eindeutig `LobbyAmbientBackground.tsx`, Layer 1 (`.liquid-spotlight-layer`, radialer Gradient an `--mouse-x`/`--mouse-y`, per `mousemove` aktualisiert) — exakt die vom Code-Kommentar selbst als „Liquid Glasswater Spotlight" bezeichnete Schicht.

**Vor der Fix-Wahl geprüft, ob `position: fixed` (statt `absolute`) die harte Kante sauber behebt, ohne einen CSS-Fallstrick auszulösen:** `position:fixed` verankert nur dann garantiert am echten Viewport, wenn **kein** Vorfahre `transform`, `filter`, `backdrop-filter`, `perspective`, `contain: layout/paint` oder `will-change` auf einer dieser Properties setzt (sonst wird der Vorfahre zum Containing Block). Kompletten Vorfahren-Pfad geprüft (`layout.tsx` → `ClientProviders.tsx` → `ClientShell.tsx` → `MainLayout.tsx` `<main>` → `<div padding:24px>`) — **kein** Treffer auf einer dieser Properties in diesem Pfad (die vorhandenen `backdropFilter`-Stellen in `MainLayout.tsx` (Zeilen 83/443/647/980) liegen alle auf Geschwister-Elementen von `<main>`/`{children}`, nicht auf Vorfahren). `position: fixed` ist damit sicher nutzbar.

### 18.2 Scope (Dateien)

| Datei                                                                                | Änderung                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/components/home/LobbyAmbientBackground.tsx`                                     | Neue optionale Prop `showSpotlight?: boolean` (Default `true` — Homepage-Verhalten 1:1 unverändert), gated sowohl den Render des Spotlight-`div`s als auch die `mousemove`/`mouseleave`-Listener + CSS-Var-Updates (kompletter Verzicht auf die Maus-Logik, wenn `false`, nicht nur visuelles Verstecken) |
| `src/app/globals.css`                                                                | `.auth-cinematic-bg`: `position: absolute` → `position: fixed`                                                                                                                                                                                                                                            |
| `src/components/auth/AuthCinematicBackground.tsx`                                    | `<LobbyAmbientBackground showSpotlight={false} />`                                                                                                                                                                                                                                                        |
| `src/app/sign-in/[[...sign-in]]/page.tsx`, `src/app/sign-up/[[...sign-up]]/page.tsx` | Wrapper-`div`: `overflow:hidden`/`position:relative` entfernt (nicht mehr nötig, da der Hintergrund nicht mehr auf diese Box angewiesen ist, um sichtbar zu sein)                                                                                                                                         |

**Kein** DB-/API-/Auth-Logik-Touch, keine Homepage-Datei geändert (`HomeClientV2.tsx` ruft `LobbyAmbientBackground` weiter ohne Prop auf → Default `true`, 0 Verhaltensänderung dort).

### 18.3 Reihenfolge

1. `LobbyAmbientBackground.tsx` — additive Prop zuerst (Risiko 0 für Homepage dank Default).
2. `globals.css` — 1-Zeilen-Positionsänderung.
3. `AuthCinematicBackground.tsx` — Prop durchreichen.
4. Beide `page.tsx` — Wrapper vereinfachen.
5. Verifikation (18.4).

### 18.4 Risiko-Register

| #   | Risiko                                                                                            | Gegenmaßnahme                                                                                                                                                                                                                                                                                                    |
| --- | ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | `position:fixed` doch von einem übersehenen Vorfahren eingefangen (Containing-Block-Fallstrick)   | Vollständiger Grep des Vorfahren-Pfads vor der Änderung (18.1) + empirische Prüfung der `getBoundingClientRect()`-Maße nach der Änderung (muss exakt Viewport-Größe entsprechen)                                                                                                                                 |
| R2  | Z-Index-Regression: Header/Sidebar verschwinden hinter dem jetzt fixed-positionierten Hintergrund | `.auth-cinematic-bg` bleibt `z-index:0`; Header ist `sticky`+`z-index:40`, Card ist `position:relative`+`z-index:1` — beide vergleichen weiterhin im selben (Root-)Stacking-Context, da kein Vorfahre einen neuen Stacking-Context erzwingt. Nach Umsetzung per DOM-Inspektion verifiziert, nicht nur angenommen |
| R3  | Homepage-Regression durch die neue `showSpotlight`-Prop                                           | Default `true`, `HomeClientV2.tsx` bleibt unverändert (kein Prop übergeben) — Browser-Check der Homepage nach der Änderung als Teil der Verifikation                                                                                                                                                             |
| R4  | Entfernen von `overflow:hidden` am Seiten-Wrapper lässt etwas anderes ungewollt überlaufen        | Wrapper enthält nur die zentrierte Formular-Karte, kein weiteres Overflow-kritisches Element; `resize_window`-Check auf horizontalen Overflow nach der Änderung                                                                                                                                                  |

### 18.5 Verifizierungsplan

1. `npm run typecheck` / `npm run lint` (nur geänderte Dateien) / `npm run build` — 0 neue Fehler.
2. Eigener Preview-Server: `/sign-in` + `/sign-up` — `getBoundingClientRect()` von `.auth-cinematic-bg` per JS geprüft: muss `{top:0, left:0, width:innerWidth, height:innerHeight}` entsprechen (Beweis, dass kein Containing-Block-Fallstrick zuschlug).
3. Visuell keine harte Kante mehr (Jan bestätigt final selbst, siehe `no-visual-check-frontend`) — Claude prüft nur die strukturelle DOM-Metrik aus Punkt 2, nicht die Optik.
4. Kein Spotlight-Layer mehr im DOM auf `/sign-in`/`/sign-up` (`.liquid-spotlight-layer` darf dort nicht existieren), Homepage `/` behält ihn (Regressionscheck).
5. Header/Sidebar bleiben klickbar und sichtbar oberhalb des Hintergrunds (funktionale Stichprobe: Klick auf einen Nav-Eintrag).
6. `resize_window` 375/1440 — kein horizontaler Overflow.

### 18.6 Ausführung & Verifikation (Ergebnis)

Alle 4 Dateien geändert wie in 18.2 geplant. Verifikation (eigener Preview-Server, Port 3021):

| Prüfung                                                     | Ergebnis                                                                                                                                  |
| ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run typecheck`                                         | 0 Fehler                                                                                                                                  |
| `npx eslint` auf allen 4 geänderten Dateien                 | 0 Errors, 0 Warnings                                                                                                                      |
| `npm run build`                                             | grün                                                                                                                                      |
| `npm run test`                                              | 265/265 Tests grün (26 Dateien — Anstieg von 238/25 durch parallel laufende externe Session, keine Kollateralschäden)                     |
| `.auth-cinematic-bg.getBoundingClientRect()` auf `/sign-in` | `{top:0, left:0, width:1280, height:720}` = exakt Viewport (1280×720) → harte Kante strukturell behoben, kein Containing-Block-Fallstrick |
| `.liquid-spotlight-layer` auf `/sign-in`                    | nicht im DOM (`false`)                                                                                                                    |
| `.liquid-spotlight-layer` auf `/` (Homepage)                | weiterhin im DOM (`true`) → 0 Regression                                                                                                  |
| Header/Sidebar-Buttons auf `/sign-in`                       | alle weiterhin interaktiv/erreichbar (Accessibility-Tree) → Z-Index-Stacking intakt                                                       |
| `/sign-up` bei 375×812 (mobile)                             | `.auth-cinematic-bg` exakt `{width:375, height:812}`, kein horizontaler Overflow, Spotlight ebenfalls abwesend                            |

**Status:** `Executed`. Strukturell/funktional bewiesen — die eigentliche optische Freigabe („keine harte Kante mehr sichtbar") liegt bei Jan (`no-visual-check-frontend`-Regel).

---

## 19 — Nachbesserung: Karten-Glow zu grell/„Vibe-Coded" (2026-08-09, Jan-Feedback per Screenshot)

Jan: der Gold-Glow-Ring um die Formular-Karte wirkt zu laut/amateurhaft, soll professioneller/dezenter werden.

### 19.1 Root-Cause-Analyse

Drei Gold-Glow-Quellen überlagern sich aktuell an derselben Stelle (Card-Umriss), was den grellen Halo-Ring im Screenshot erklärt:

1. **`AuthForm.tsx` Zeilen 193–206** — eigener „Ambient Radial Casino Glow"-Div direkt hinter der Karte (`inset:-20px`, `blur(30px)`, Gold 0.18 + Lila 0.12 Opazität). Stammt aus der Zeit **vor** dem Full-Bleed-Cinematic-Hintergrund (§8 ff.), wo die Karte noch alleine auf schwarzem Grund stand und einen eigenen Glow brauchte.
2. **`.glass-edge-gold`** (globals.css) — Card-Rahmen `rgba(212,175,55,0.22)` + `box-shadow 0 0 25px rgba(212,175,55,0.1)`.
3. **`.auth-orb-gold`** (neu aus §18) — 560px Gold-Orb, blur(100px), 0.16 Opazität, oben rechts positioniert, reicht bis in die Kartennähe.

**Dominante Ursache:** Quelle 1 — sie folgt exakt der Kartenform (`inset:-20px`), was den scharf konturierten „Neon-Ring" im Screenshot erzeugt. Quelle 1 ist seit dem Cinematic-Background (§8) **redundant**: Ambiente liefert jetzt bereits die Full-Bleed-Hintergrundfläche, ein zusätzlicher kartenprivater Glow verdoppelt den Effekt statt ihn zu ergänzen.

### 19.2 Scope (Dateien)

| Datei                              | Änderung                                                                                                               |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| `src/components/auth/AuthForm.tsx` | „Ambient Radial Casino Glow"-Div (Zeilen 193–206) komplett entfernt — redundant seit §8                                |
| `src/app/globals.css`              | `.glass-edge-gold`: Rahmen/Glow von Halo-Stärke auf dezente Hairline-Kante reduziert (Base **und** Hover-State)        |
| `src/app/globals.css`              | `.auth-orb-gold`: Opazität leicht reduziert (ruhigeres Ambiente in der Kartenecke, kein zusätzlicher Beitrag zum Halo) |

Kein Logik-/State-/Copy-Touch, keine Homepage-Datei berührt (`.glass-edge-gold`/`.auth-orb-gold` werden ausschließlich von Auth-Dateien konsumiert — verifiziert, 0 andere Aufrufer).

### 19.3 Konkrete Werte

| Element                             | Vorher                                    | Nachher                          | Begründung                                                       |
| ----------------------------------- | ----------------------------------------- | -------------------------------- | ---------------------------------------------------------------- |
| Ambient-Glow-Div                    | vorhanden (blur 30px, 0.18/0.12 Opazität) | **entfernt**                     | redundant, dominante Ursache des Halo-Rings                      |
| `.glass-edge-gold` Rahmen           | `rgba(212,175,55,0.22)`                   | `rgba(212,175,55,0.16)`          | dezentere Hairline statt auffälliger Kontur                      |
| `.glass-edge-gold` Box-Shadow       | `0 0 25px rgba(212,175,55,0.1)`           | `0 0 14px rgba(212,175,55,0.06)` | kleinerer Radius + niedrigere Opazität = Kanten-Licht statt Halo |
| `.glass-edge-gold:hover` Rahmen     | `rgba(212,175,55,0.45)`                   | `rgba(212,175,55,0.32)`          | Hover bleibt spürbar, aber nicht grell                           |
| `.glass-edge-gold:hover` Box-Shadow | `0 0 35px rgba(212,175,55,0.2)`           | `0 0 20px rgba(212,175,55,0.12)` | konsistent zur Base-Reduktion                                    |
| `.auth-orb-gold` Opazität           | `0.16`                                    | `0.10`                           | ruhigeres Eck-Ambiente, kein zusätzlicher Halo-Beitrag           |

### 19.4 Risiko-Register

| #   | Risiko                                                                                                                  | Gegenmaßnahme                                                                                                                                                                  |
| --- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| R1  | Karte wirkt nach Entfernen des Ambient-Divs zu „nackt"/kontrastarm vor dem jetzt dunkleren Hintergrund                  | `.glass-edge-gold` bleibt (dezente Kante) + `.glass-layered`-Schatten (`0 12px 32px -8px rgba(0,0,0,0.7)`) unverändert — Karte bleibt vom Hintergrund abgesetzt, nur ohne Halo |
| R2  | `.glass-edge-gold`/`.auth-orb-gold` werden später von anderer Stelle wiederverwendet und die Reduktion passt dort nicht | Vor Änderung verifiziert: 0 andere Konsumenten (grep). Beide Klassen sind faktisch Auth-exklusiv, Änderung risikofrei                                                          |
| R3  | Zu starke Reduktion macht die Karte komplett glow-los, Gold-Branding geht verloren                                      | Werte bewusst nicht auf 0 gesetzt (dezente Hairline + gedämpfter Orb bleiben) — Ziel ist „professionell gedämpft", nicht „ausgeschaltet"                                       |

### 19.5 Verifizierungsplan

1. `npm run typecheck` / `npx eslint` (nur geänderte Dateien) / `npm run build` — 0 neue Fehler.
2. `npm run test` — keine Kollateralschäden.
3. Eigener Preview-Server: `/sign-in` + `/sign-up` — DOM-Check, dass der Ambient-Glow-Div nicht mehr existiert, `.glass-edge-gold`/`.auth-orb-gold` mit den neuen Werten im Rechner-CSS ankommen.
4. Screenshot für Jans visuelle Freigabe — Claude bewertet nicht selbst, ob es jetzt „professionell" wirkt (`no-visual-check-frontend`).

### 19.6 Ausführung & Verifikation (Ergebnis)

| Prüfung                                                                   | Ergebnis                                                                                               |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| `npm run typecheck`                                                       | 0 Fehler                                                                                               |
| `npx eslint` auf `AuthForm.tsx`                                           | 0 Errors, 0 Warnings                                                                                   |
| `npm run build`                                                           | grün                                                                                                   |
| `npm run test`                                                            | 265/265 Tests grün                                                                                     |
| Ambient-Glow-Div (Signatur `hsla(280, 100%, 65%…)`) im DOM auf `/sign-in` | 0 Treffer → entfernt                                                                                   |
| `getComputedStyle('.glass-edge-gold')`                                    | `borderColor: rgba(212,175,55,0.16)`, `boxShadow: … 0 0 14px rgba(212,175,55,0.06)` → neue Werte aktiv |
| `getComputedStyle('.auth-orb-gold')` Hintergrund                          | `rgba(212,175,55,0.1)` → neue Opazität aktiv                                                           |

**Status:** `Executed`. Alle 3 Glow-Quellen strukturell auf die neuen, gedämpften Werte reduziert bzw. entfernt. Ob es jetzt „professionell genug" wirkt, ist Jans visuelle Entscheidung — bei Bedarf sind die Werte in Abschnitt 19.3 die Stellschrauben für eine weitere Iteration.

---

## 20 — Nachbesserung: Crown-Badge durch KI-generiertes Emblem ersetzen (2026-08-09, Jan-Feedback)

Jan: das Lucide-`Crown`-Icon in der Gold-Kreis-Badge über „Willkommen zurück" wirkt zu generisch/„selbstgemacht". Gewählt aus dem vorgestellten Optionskatalog: **Option A — KI-generiertes Bild-Emblem statt Icon.**

### 20.1 Ziel

Das 48px-Icon-in-Kreis-Composite durch ein einzelnes, bespoke generiertes Bild-Emblem ersetzen (Gold/Obsidian-Identität, wirkt wie ein echtes Marken-Siegel statt einem Lucide-Icon aus einer Bibliothek).

### 20.2 Scope (Dateien)

| Datei                                          | Änderung                                                                                |
| ---------------------------------------------- | --------------------------------------------------------------------------------------- |
| **NEU** `public/images/auth/login-emblem.webp` | generiertes Emblem-Asset                                                                |
| `src/components/auth/AuthForm.tsx`             | Crown-Icon + Gold-Kreis-Div → `next/image` mit dem neuen Asset, `Crown`-Import entfernt |

Kein Logik-/State-Touch, keine Auth-Route-Änderung. Security-Reviewer: Nein (kein Money-/Auth-Pfad).

### 20.3 Bild-Erzeugung

- **Modell:** `recraft_v4_1`, `model_type: vector` (für Logos/Icons/Embleme optimiert — sauberer bei kleiner Darstellungsgröße als photorealistische Modelle), `resolution: 2k` (Downscale auf Ziel-Anzeigegröße → gestochen scharf auf Retina), `background_color: null` (transparent), `colors:` fest auf die Projekt-Gold-Palette (`#D4AF37`, `#FFC94A`, `#AA7C11`) gepinnt statt dem Modell die Farbwahl zu überlassen.
- **4 unabhängige Prompt-Varianten** parallel generiert (Krone mit Edelstein / Wappen-Siegel mit Monogramm / Münzrelief / Art-déco-Kronenbadge) — mehr Auswahl für eine objektive Bewertung statt nur eine Variante zu akzeptieren.
- **Aspect Ratio:** 1:1 (quadratisch, sauberer Zuschnitt für ein rundes Badge).

### 20.4 Auswahl-Kriterium (objektiv, keine Ästhetik-Bewertung)

Da Claude laut Projektregel nie selbst ästhetisch über UI urteilt (`no-visual-check-frontend`), erfolgt die Auswahl unter den generierten Kandidaten anhand einer **objektiven Checkliste** statt eines Geschmacksurteils — das ist keine Bewertung „ob es schön aussieht", sondern ein Abgleich gegen den Prompt-Brief, dieselbe Art Prüfung wie ein Alt-Text- oder Kontrast-Check:

| #   | Kriterium                                                                                 |
| --- | ----------------------------------------------------------------------------------------- |
| K1  | Hintergrund transparent (keine unerwünschte weiße/farbige Box)                            |
| K2  | Komposition rund/badge-tauglich (kein Vollbild-Motiv, das über die Kreisform hinausläuft) |
| K3  | Dominante Goldtöne (Palette-Treffer)                                                      |
| K4  | Kein eingebetteter Text/Wasserzeichen (häufiger KI-Gen-Fehler)                            |
| K5  | Quadratisches Seitenverhältnis, sauber beschneidbar                                       |

Kandidat, der alle 5 Kriterien erfüllt, wird verwendet. Erfüllt keiner alle 5 → beste Erfüllung + Notiz für Jan, welches Kriterium schwächer ist (dann ggf. Nachiteration).

### 20.5 Integration

- `next/image` (kein `unoptimized` — anders als der SVG-Avatar-Fall in `/vault`, hier handelt es sich um ein Raster-Bild, für das Next.js' Optimierungspipeline genau richtig ist), explizite `width`/`height` (Performance-Regel: keine Layout-Shifts), `alt="Casino Royale"`.
- Anzeigegröße leicht erhöht (48px → 64px) — ein echtes Emblem verträgt mehr Präsenz als ein Icon-Fallback; bleibt innerhalb des bestehenden Karten-Layouts (nur `marginBottom` unverändert).
- **Kein neuer Glow-Ring um das Emblem** — Abschnitt 19 hat gerade erst überlagerte Gold-Glows entfernt; das Emblem bringt seinen eigenen visuellen Wert bereits mit, ein zusätzlicher CSS-Glow würde denselben Fehler wiederholen.
- Bestehende Positionierung (zentriert, über der Überschrift) unverändert.

### 20.6 Risiko-Register

| #   | Risiko                                                            | Gegenmaßnahme                                                                                     |
| --- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| R1  | Generiertes Bild trifft Marken-Brief nicht (Farbe/Stil/Artefakte) | 4 unabhängige Prompt-Varianten + objektive Checkliste (20.4) statt nur 1 Versuch zu akzeptieren   |
| R2  | Bild wirkt bei 64px verwaschen/pixelig                            | 2k-Quellauflösung, weit über Zielgröße → sauberes Downscaling statt Upscaling                     |
| R3  | Transparenter Hintergrund fehlt (Modell liefert weiße Box)        | `background_color: null` explizit gesetzt; K1 in der Checkliste prüft das gezielt vor Integration |
| R4  | Zusätzliches Asset erhöht Seitengewicht                           | WebP, kleine Zielauflösung für ein Badge-Asset (keine Hero-Bild-Dimensionen)                      |
| R5  | `Crown`-Import wird nach Entfernung zum toten Import              | Import-Zeile im selben Edit entfernt, `npx eslint` danach explizit auf `no-unused-vars` geprüft   |
| R6  | Wiederholung des gerade behobenen Glow-Problems (§19)             | Bewusst kein neuer CSS-Glow um das Emblem (20.5)                                                  |

### 20.7 Verifizierungsplan

1. Alt-Text, `width`/`height` vorhanden (A11y + CLS).
2. `npm run typecheck` / `npx eslint` (nur geänderte Datei) / `npm run build` — 0 neue Fehler, 0 neue Warnings (insbesondere kein toter `Crown`-Import).
3. `npm run test` — keine Kollateralschäden.
4. Eigener Preview-Server: `/sign-in` + `/sign-up` — Bild lädt (HTTP 200 Netzwerk-Request), korrekte Dimensionen, keine Konsolenfehler.
5. Datei-Größe des neuen Assets geprüft (Perf-Budget-Sanity-Check).
6. Screenshot/Link an Jan zur eigentlichen ästhetischen Freigabe.

### 20.8 Ausführung — blockiert (Ergebnis)

- `recraft_v4_1` (ursprünglich geplant, 20.3) → **403 `job_minimum_basic_plan_required`**, planbeschränkt. Adaptiv auf `nano_banana_pro` umgestellt (verfügbares Modell, per `get_cost`-Preflight bestätigt: 2 Credits/Bild).
- Batch-Generierung (4 Kandidaten) → **fehlgeschlagen: „Out of credits on free (null) plan"**. `balance`-Check bestätigt: **1,55 Credits verfügbar** auf dem `free`-Plan — reicht nicht für auch nur 1 Generierung (2 Credits nötig), geschweige denn 4.
- **Kein Bild generiert.** Käufe/Plan-Upgrades fallen unter „Explizite Erlaubnis erforderlich" (Zahlungsvorgang) — das entscheidet Jan selbst, nicht automatisch von Claude ausgelöst.

**Status:** `Blocked (Credits)`. Der Implementierungsplan (20.1–20.7) bleibt gültig — sobald Credits/Plan verfügbar sind, kann exakt an 20.3 fortgesetzt werden (Prompts, Auswahl-Checkliste, Integration sind wiederverwendbar).

**Adaptive Fortsetzung (gleicher Durchgang, keine Rückfrage laut expliziter Anweisung):** Kauf/Plan-Upgrade ist ein Zahlungsvorgang, den nur Jan auslösen kann — Warten darauf würde die Anweisung „direkt in Execution gehen, ohne Rückfrage" verletzen. Deshalb sofortiger Wechsel auf **Option B** (bereits im ursprünglichen Options-Katalog als Fallback vorgestellt) — Abschnitt 21.

---

## 21 — Option B (Fallback): Aufgewertetes Code-Emblem statt Bild-Generierung

### 21.1 Ziel

Denselben „wirkt wie ein generisches Icon"-Mangel beheben, aber vollständig code-basiert (kein Credits-/Plan-Bedarf, sofort ausführbar): eigenes, detailreicheres Kronen-SVG mit Gold-Verlauf + Edelstein-Akzenten + dezentem Glanz-Sweep statt Lucide-`Crown`.

### 21.2 Design-Entscheidungen

- **Krone statt generischem Lucide-Icon:** eigener SVG-Pfad (3-Zacken-Krone + Basisband) statt Icon-Bibliothek — liest sich als bewusst gestaltetes Emblem, nicht als Library-Icon.
- **Gold-Verlauf-Fill** (`linearGradient`, hell→dunkel, dieselben Töne wie der bestehende Badge-Hintergrund) statt Flat-Color — mehr Tiefe/Materialität.
- **2 Edelstein-Akzente in den bereits im Projekt bedeutungstragenden Farben** — Smaragd (`--success`, `#00e676`) und Rubin (`--error`, `#ff3366`) an den äußeren Zacken, dazu ein heller Diamant-Glanzpunkt an der Mittelspitze. Bewusst **keine** dritte, im Design-System nicht vorkommende Farbe eingeführt — die Gems zitieren die Win/Loss-Sprache, die im Rest des Casinos bereits Bedeutung trägt.
- **Dezenter Glanz-Sweep** statt eines Glows: ein schmaler, diagonaler Hell-Streif zieht alle paar Sekunden einmal über das kreisrunde Badge (Coin-Shine-Effekt) — Technik ist dieselbe `translateX`-Sweep-Klasse, die im Projekt bereits für `.shimmer-progress` existiert (Wiederverwendung eines etablierten, compositor-freundlichen Musters statt Neuerfindung). **Kein zusätzlicher Box-Shadow-Glow** — Lehre aus Abschnitt 19 bewusst berücksichtigt.
- **Badge-Größe:** 48px → 64px (dieselbe Begründung wie in 20.5: ein bewusst gestaltetes Emblem verträgt mehr Präsenz als das bisherige Icon-Fallback).
- **Reduced-Motion:** Sweep-Animation wird unter `prefers-reduced-motion: reduce` deaktiviert (eigener Guard — die bestehende `.shimmer-progress`-Klasse im Projekt hat selbst keinen, dieser neue Code übernimmt diese Lücke nicht).

### 21.3 Scope (Dateien)

| Datei                              | Änderung                                                                                                                                                                                                    |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/components/auth/AuthForm.tsx` | `Crown`-Import entfernt, neue lokale `CrownEmblem`-SVG-Komponente (Muster wie die bestehende lokale `GoogleLogo`-Komponente in derselben Datei), Badge-Div 48px→64px + `overflow:hidden` für den Sweep-Clip |
| `src/app/globals.css`              | Neue Klasse `.auth-emblem-shine` (Sweep-Animation) + `prefers-reduced-motion`-Guard                                                                                                                         |

Kein neues Asset, keine neue Dependency, kein Logik-/State-Touch.

### 21.4 Risiko-Register

| #   | Risiko                                                               | Gegenmaßnahme                                                                                                                               |
| --- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | Custom-SVG-Pfad rendert fehlerhaft/leer (Pfad-Syntax-Fehler)         | Einfache, manuell nachvollziehbare Koordinaten (kein generierter Pfad), Browser-Check nach Umsetzung                                        |
| R2  | Sweep-Animation wird zum neuen „Glow"-Problem (Wiederholung von §19) | Nur ein schmaler Hell-Streif per `clip`/`overflow:hidden`, kein `box-shadow`, niedrige Deckkraft, kurze Sichtbarkeit pro Zyklus             |
| R3  | 64px-Badge sprengt das Karten-Layout                                 | Nur `width`/`height` der Badge geändert, `marginBottom` zur Überschrift unverändert — geringes Layout-Risiko, per Browser-Check verifiziert |
| R4  | Toter `Crown`-Import nach Entfernen                                  | Im selben Edit entfernt, `npx eslint` prüft `no-unused-vars`                                                                                |

### 21.5 Verifizierungsplan

1. `npm run typecheck` / `npx eslint` (nur geänderte Dateien) / `npm run build` — 0 neue Fehler/Warnings.
2. `npm run test` — keine Kollateralschäden.
3. Eigener Preview-Server: SVG rendert (kein leeres/gebrochenes Element im DOM), Badge-Maße 64×64, Sweep-Element vorhanden, `prefers-reduced-motion` schaltet die Animation ab (Code-Review, wie bei §18.5 Punkt 7 bereits gehandhabt).
4. Kein neuer horizontaler Overflow (`resize_window`-Check).

### 21.6 Ausführung & Verifikation (Ergebnis)

| Prüfung                                | Ergebnis                                                                                                          |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `npm run typecheck`                    | 0 Fehler                                                                                                          |
| `npx eslint` auf `AuthForm.tsx`        | 0 Errors, 0 Warnings (`Crown`-Import sauber entfernt, kein toter Import)                                          |
| `npm run build`                        | grün, `/sign-in` + `/sign-up` kompilieren                                                                         |
| `npm run test`                         | 265/265 Tests grün                                                                                                |
| SVG-Emblem im DOM (`/sign-in`)         | gefunden, 3 Gems (Smaragd/Rubin/Diamant) korrekt gerendert                                                        |
| Badge-Maße                             | ~62×62px (Ziel 64×64, Rundungsdifferenz durch Border-Box, unkritisch)                                             |
| Shine-Sweep-Pseudo-Element (`::after`) | vorhanden                                                                                                         |
| SVG-Emblem im DOM (`/sign-up`)         | ebenfalls gefunden — geteilte `AuthForm`-Komponente funktioniert für beide Routen                                 |
| Konsolenfehler                         | nur vorbestehende 401/Wallet-Init-Meldungen (unabhängig von dieser Änderung, Wallet-/Auth-API ohne echte Session) |

**Status:** `Executed`. Strukturell bewiesen — die ästhetische Freigabe („wirkt das Emblem jetzt hochwertiger als das Lucide-Icon") liegt bei Jan.
